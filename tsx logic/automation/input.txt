```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
	Easing,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene1_PuraniPehchaan',
	durationInSeconds: 15,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene1_PuraniPehchaan: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera slow zoom in
	const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.08], {
		extrapolateRight: 'clamp',
	});

	// Screen shake on "भारी बात"
	const shakeX =
		frame < 20 ? Math.sin(frame * 2.5) * interpolate(frame, [0, 20], [8, 0], {extrapolateRight: 'clamp'}) : 0;
	const shakeY =
		frame < 20 ? Math.cos(frame * 3.1) * interpolate(frame, [0, 20], [5, 0], {extrapolateRight: 'clamp'}) : 0;

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			{/* Animated Gradient Background */}
			<AbsoluteFill
				style={{
					transform: `scale(${cameraZoom}) translate(${shakeX}px, ${shakeY}px)`,
				}}
			>
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(ellipse at ${50 + Math.sin(frame * 0.008) * 15}% ${50 + Math.cos(frame * 0.006) * 10}%, #2D1A3E 0%, #1C1C1C 50%, #0D0D0D 100%)`,
					}}
				/>
				{/* Secondary gradient pulse */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 80% 20%, rgba(255,193,7,0.06) 0%, transparent 50%)`,
						opacity: 0.5 + Math.sin(frame * 0.03) * 0.3,
					}}
				/>

				{/* Shape Layer - Hexagons and diamonds drifting */}
				{[...Array(6)].map((_, i) => {
					const seed = seededRandom(i + 700);
					const size = 120 + seed * 200;
					const startX = seed * 1920;
					const startY = seededRandom(i + 701) * 1080;
					const drift = frame * (0.15 + seed * 0.3);
					const rot = frame * (0.08 + seededRandom(i + 702) * 0.15);
					const opacity = 0.03 + seededRandom(i + 703) * 0.05;
					const isHexagon = i % 2 === 0;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: startX + Math.sin(drift * 0.02 + i) * 40,
								top: startY + Math.cos(drift * 0.015 + i) * 30,
								width: size,
								height: size,
								transform: `rotate(${rot}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
								opacity,
							}}
						>
							{isHexagon ? (
								<svg viewBox="0 0 100 100" width="100%" height="100%">
									<polygon
										points="50,2 93,25 93,75 50,98 7,75 7,25"
										fill="none"
										stroke="#FFC107"
										strokeWidth="1.5"
									/>
								</svg>
							) : (
								<svg viewBox="0 0 100 100" width="100%" height="100%">
									<rect
										x="15"
										y="15"
										width="70"
										height="70"
										fill="none"
										stroke="#9E9E9E"
										strokeWidth="1"
										transform="rotate(45 50 50)"
									/>
								</svg>
							)}
						</div>
					);
				})}

				{/* Motion Texture - Diagonal scan lines */}
				{[...Array(20)].map((_, i) => {
					const y = (i * 60 + frame * 0.5) % 1200 - 60;
					return (
						<div
							key={`line-${i}`}
							style={{
								position: 'absolute',
								left: 0,
								top: y,
								width: '100%',
								height: 1,
								background: `linear-gradient(90deg, transparent 0%, rgba(245,245,245,0.02) 30%, rgba(245,245,245,0.04) 50%, rgba(245,245,245,0.02) 70%, transparent 100%)`,
								transform: `rotate(-2deg)`,
							}}
						/>
					);
				})}

				{/* Floating Particles */}
				{[...Array(35)].map((_, i) => {
					const seed = seededRandom(i + 71);
					const x = seed * 1920;
					const y = seededRandom(i + 72) * 1080;
					const size = 2 + seededRandom(i + 73) * 4;
					const speed = 0.3 + seed * 0.6;
					const floatY = Math.sin(frame * speed * 0.05 + i * 2) * 25;
					const floatX = Math.cos(frame * speed * 0.03 + i * 1.5) * 15;
					const particleOp = 0.15 + Math.sin(frame * 0.04 + i) * 0.12;

					return (
						<div
							key={`particle-${i}`}
							style={{
								position: 'absolute',
								left: x + floatX,
								top: y + floatY,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: i % 3 === 0 ? '#FFC107' : '#F5F5F5',
								opacity: particleOp,
							}}
						/>
					);
				})}

				{/* Main Content - Shattered Mirror Concept */}
				<AbsoluteFill
					style={{
						display: 'flex',
						flexDirection: 'column',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Broken Mirror SVG */}
					<div
						style={{
							position: 'absolute',
							left: '10%',
							top: '15%',
							transform: `scale(${sp(5)}) rotate(${interpolate(sp(5), [0, 1], [-15, 0])}deg)`,
							opacity: sp(5),
						}}
					>
						<svg width="280" height="360" viewBox="0 0 280 360">
							{/* Mirror frame */}
							<rect
								x="10"
								y="10"
								width="260"
								height="340"
								rx="20"
								fill="none"
								stroke="#FFC107"
								strokeWidth="4"
								opacity="0.7"
							/>
							{/* Crack lines */}
							<line
								x1="140"
								y1="10"
								x2="80"
								y2="180"
								stroke="#F5F5F5"
								strokeWidth="2"
								opacity={sp(25)}
							/>
							<line
								x1="140"
								y1="10"
								x2="220"
								y2="200"
								stroke="#F5F5F5"
								strokeWidth="2"
								opacity={sp(30)}
							/>
							<line
								x1="80"
								y1="180"
								x2="30"
								y2="350"
								stroke="#F5F5F5"
								strokeWidth="1.5"
								opacity={sp(35)}
							/>
							<line
								x1="80"
								y1="180"
								x2="180"
								y2="350"
								stroke="#F5F5F5"
								strokeWidth="1.5"
								opacity={sp(35)}
							/>
							<line
								x1="220"
								y1="200"
								x2="260"
								y2="350"
								stroke="#F5F5F5"
								strokeWidth="1.5"
								opacity={sp(40)}
							/>
							{/* Reflection shimmer */}
							<rect
								x="50"
								y="50"
								width="180"
								height="260"
								rx="5"
								fill={`url(#mirrorGrad)`}
								opacity={0.08 + Math.sin(frame * 0.04) * 0.04}
							/>
							<defs>
								<linearGradient id="mirrorGrad" x1="0%" y1="0%" x2="100%" y2="100%">
									<stop offset="0%" stopColor="#F5F5F5" />
									<stop offset="50%" stopColor="#FFC107" />
									<stop offset="100%" stopColor="#F5F5F5" />
								</linearGradient>
							</defs>
						</svg>
					</div>

					{/* Title Text */}
					<div
						style={{
							position: 'absolute',
							right: '8%',
							top: '12%',
							transform: `translateX(${interpolate(sp(15), [0, 1], [100, 0])}px)`,
							opacity: sp(15),
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 120,
								color: '#FFC107',
								lineHeight: 1.1,
								textShadow: '0 0 60px rgba(255,193,7,0.3)',
							}}
						>
							पुरानी
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 130,
								color: '#F5F5F5',
								lineHeight: 1.1,
								marginTop: -10,
							}}
						>
							पहचान
						</div>
					</div>

					{/* Subtitle */}
					<div
						style={{
							position: 'absolute',
							right: '8%',
							top: '50%',
							maxWidth: 700,
							transform: `translateY(${interpolate(sp(35), [0, 1], [40, 0])}px)`,
							opacity: sp(35),
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 48,
								color: '#F5F5F5',
								lineHeight: 1.5,
								fontWeight: 600,
							}}
						>
							जो अब आपकी{' '}
							<span style={{color: '#ff0000'}}>नहीं रही</span>
						</div>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 44,
								color: '#9E9E9E',
								lineHeight: 1.5,
								marginTop: 10,
								opacity: sp(50),
							}}
						>
							लेकिन जबर्दस्ती जिन्दा रखे हुए हैं
						</div>
					</div>

					{/* Warning Icon Pulse */}
					<div
						style={{
							position: 'absolute',
							left: '15%',
							bottom: '10%',
							opacity: sp(60),
							transform: `scale(${1 + Math.sin(frame * 0.06) * 0.08})`,
						}}
					>
						<svg width="80" height="80" viewBox="0 0 24 24" fill="none">
							<path
								d="M12 9v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
								stroke="#ff0000"
								strokeWidth="2"
								strokeLinecap="round"
							/>
						</svg>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - contemplating */}
				<div
					style={{
						position: 'absolute',
						right: '5%',
						bottom: '8%',
						transform: `translateY(${interpolate(sp(45), [0, 1], [60, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
						opacity: sp(45),
					}}
				>
					<svg width="120" height="180" viewBox="0 0 120 180">
						{/* Golden glow */}
						<circle cx="60" cy="35" r="40" fill="#FFC107" opacity="0.08" />
						{/* Head */}
						<circle
							cx="60"
							cy="35"
							r="18"
							fill="none"
							stroke="#FFC107"
							strokeWidth="3"
							transform={`rotate(${Math.sin(frame * 0.04) * 3} 60 35)`}
						/>
						{/* Golden glasses */}
						<circle cx="52" cy="33" r="7" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="68" cy="33" r="7" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="59" y1="33" x2="61" y2="33" stroke="#FFC107" strokeWidth="2" />
						{/* Body */}
						<line x1="60" y1="53" x2="60" y2="110" stroke="#F5F5F5" strokeWidth="3" />
						{/* Arms - thinking pose, hand on chin */}
						<line x1="60" y1="70" x2="35" y2="90" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="60" y1="70" x2="80" y2="60" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="80" y1="60" x2="72" y2="42" stroke="#F5F5F5" strokeWidth="3" />
						{/* Legs */}
						<line x1="60" y1="110" x2="40" y2="155" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="60" y1="110" x2="80" y2="155" stroke="#F5F5F5" strokeWidth="3" />
						{/* Feet */}
						<line x1="40" y1="155" x2="30" y2="155" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="80" y1="155" x2="90" y2="155" stroke="#F5F5F5" strokeWidth="3" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: interpolate(frame, [0, 30], [0, 0.3], {
							extrapolateRight: 'clamp',
						}),
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene1_PuraniPehchaan;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene2_PadNahiParichai',
	durationInSeconds: 14,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene2_PadNahiParichai: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera pan right
	const cameraPanX = interpolate(frame, [0, durationInFrames], [20, -20], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill
				style={{
					transform: `translateX(${cameraPanX}px)`,
				}}
			>
				{/* Gradient Background - deep navy to charcoal */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(135deg, #0D1B2A 0%, #1B2838 30%, #2D2D2D 70%, #1C1C1C 100%)`,
					}}
				/>
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at ${30 + Math.sin(frame * 0.01) * 10}% 40%, rgba(255,193,7,0.08) 0%, transparent 45%)`,
					}}
				/>

				{/* Shape Layer - Floating badges/shields */}
				{[...Array(5)].map((_, i) => {
					const seed = seededRandom(i + 720);
					const size = 100 + seed * 180;
					const x = seed * 1920;
					const y = seededRandom(i + 721) * 1080;
					const rot = frame * (0.05 + seededRandom(i + 722) * 0.1);
					const opacity = 0.03 + seededRandom(i + 723) * 0.04;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.01 + i) * 30,
								top: y + Math.cos(frame * 0.008 + i) * 20,
								width: size,
								height: size,
								transform: `rotate(${rot}deg)`,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								{/* Shield shape */}
								<path
									d="M50 5 L90 25 L90 60 Q90 85 50 95 Q10 85 10 60 L10 25 Z"
									fill="none"
									stroke="#FFC107"
									strokeWidth="1.5"
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Concentric circles radiating */}
				{[...Array(8)].map((_, i) => {
					const radius = 80 + i * 120 + (frame * 0.3);
					const opacity = interpolate(
						radius % 960,
						[0, 480, 960],
						[0.04, 0.02, 0],
						{extrapolateRight: 'clamp'}
					);
					return (
						<div
							key={`circle-${i}`}
							style={{
								position: 'absolute',
								left: '50%',
								top: '50%',
								width: radius * 2,
								height: radius * 2,
								marginLeft: -radius,
								marginTop: -radius,
								borderRadius: '50%',
								border: `1px solid rgba(245,245,245,${opacity})`,
							}}
						/>
					);
				})}

				{/* Floating Particles */}
				{[...Array(30)].map((_, i) => {
					const seed = seededRandom(i + 730);
					const x = seed * 1920;
					const y = seededRandom(i + 731) * 1080;
					const size = 2 + seededRandom(i + 732) * 3;
					const floatY = Math.sin(frame * 0.04 + i * 1.7) * 20;
					const floatX = Math.cos(frame * 0.03 + i * 2.1) * 12;
					const particleOp = 0.1 + Math.sin(frame * 0.05 + i) * 0.1;

					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: x + floatX,
								top: y + floatY,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: i % 4 === 0 ? '#FFC107' : '#F5F5F5',
								opacity: particleOp,
							}}
						/>
					);
				})}

				{/* Main Content - Timeline of Roles */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Timeline vertical line */}
					<div
						style={{
							position: 'absolute',
							left: '15%',
							top: '15%',
							width: 3,
							height: `${interpolate(sp(10), [0, 1], [0, 70])}%`,
							background: 'linear-gradient(to bottom, #FFC107, #9E9E9E, transparent)',
						}}
					/>

					{/* Role Cards staggered along timeline */}
					{[
						{label: 'अधिकारी', years: '30 बरस', delay: 15, icon: '🏛️'},
						{label: 'प्राध्यापक', years: '—', delay: 30, icon: '📚'},
						{label: 'अभियंता', years: '—', delay: 45, icon: '⚙️'},
					].map((role, i) => (
						<div
							key={`role-${i}`}
							style={{
								position: 'absolute',
								left: '18%',
								top: `${22 + i * 22}%`,
								display: 'flex',
								alignItems: 'center',
								gap: 20,
								transform: `translateX(${interpolate(sp(role.delay), [0, 1], [80, 0])}px)`,
								opacity: sp(role.delay),
							}}
						>
							{/* Dot on timeline */}
							<div
								style={{
									position: 'absolute',
									left: -35,
									width: 16,
									height: 16,
									borderRadius: '50%',
									backgroundColor: '#FFC107',
									boxShadow: '0 0 15px rgba(255,193,7,0.5)',
								}}
							/>
							{/* Role icon */}
							<div
								style={{
									width: 70,
									height: 70,
									borderRadius: 15,
									backgroundColor: '#2D2D2D',
									border: '2px solid #FFC107',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
								}}
							>
								<svg width="36" height="36" viewBox="0 0 24 24" fill="none">
									<path
										d="M12 14l9-5-9-5-9 5 9 5zm0 0l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z"
										stroke="#FFC107"
										strokeWidth="2"
										strokeLinejoin="round"
									/>
								</svg>
							</div>
							{/* Role text */}
							<div>
								<div
									style={{
										fontFamily: 'Yantra One',
										fontSize: 52,
										color: '#F5F5F5',
										lineHeight: 1.2,
									}}
								>
									{role.label}
								</div>
								{role.years !== '—' && (
									<div
										style={{
											fontFamily: 'Noto Sans Devanagari',
											fontSize: 30,
											color: '#FFC107',
											marginTop: 2,
										}}
									>
										{role.years}
									</div>
								)}
							</div>
						</div>
					))}

					{/* Right side - "ये सिर्फ पद नहीं..." */}
					<div
						style={{
							position: 'absolute',
							right: '8%',
							top: '20%',
							maxWidth: 650,
							opacity: sp(55),
							transform: `translateY(${interpolate(sp(55), [0, 1], [30, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 90,
								color: '#FFC107',
								lineHeight: 1.15,
							}}
						>
							ये पद नहीं
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 100,
								color: '#F5F5F5',
								lineHeight: 1.15,
								marginTop: 5,
							}}
						>
							परिचय था
						</div>
					</div>

					{/* Sub text */}
					<div
						style={{
							position: 'absolute',
							right: '8%',
							bottom: '18%',
							maxWidth: 600,
							opacity: sp(75),
							transform: `translateY(${interpolate(sp(75), [0, 1], [25, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 44,
								color: '#9E9E9E',
								lineHeight: 1.6,
							}}
						>
							भाषा थी, दुनिया से जुड़ने का तरीका था
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - proud standing pose */}
				<div
					style={{
						position: 'absolute',
						left: '42%',
						bottom: '5%',
						opacity: sp(40),
						transform: `translateY(${interpolate(sp(40), [0, 1], [50, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="100" height="160" viewBox="0 0 100 160">
						<circle cx="50" cy="30" r="35" fill="#FFC107" opacity="0.06" />
						<circle
							cx="50"
							cy="30"
							r="16"
							fill="none"
							stroke="#FFC107"
							strokeWidth="3"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 50 30)`}
						/>
						<circle cx="43" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="57" cy="28" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="49" y1="28" x2="51" y2="28" stroke="#FFC107" strokeWidth="2" />
						<line x1="50" y1="46" x2="50" y2="100" stroke="#F5F5F5" strokeWidth="3" />
						{/* Arms wide - proud */}
						<line x1="50" y1="62" x2="20" y2="55" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="50" y1="62" x2="80" y2="55" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="50" y1="100" x2="35" y2="140" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="50" y1="100" x2="65" y2="140" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="35" y1="140" x2="28" y2="140" stroke="#F5F5F5" strokeWidth="3" />
						<line x1="65" y1="140" x2="72" y2="140" stroke="#F5F5F5" strokeWidth="3" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: interpolate(frame, [0, 30], [0, 0.3], {
							extrapolateRight: 'clamp',
						}),
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene2_PadNahiParichai;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene3_JawabBadal',
	durationInSeconds: 11,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene3_JawabBadal: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera zoom in slightly
	const cameraScale = interpolate(frame, [0, durationInFrames], [1, 1.06], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill style={{transform: `scale(${cameraScale})`}}>
				{/* Gradient Background - warm to cold transition */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(180deg, #1C1C1C 0%, #2D1A1A 40%, #1A1A2D 70%, #1C1C1C 100%)`,
					}}
				/>
				{/* Shifting light */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at ${50 + Math.sin(frame * 0.015) * 20}% 50%, rgba(255,193,7,0.05) 0%, transparent 40%)`,
					}}
				/>

				{/* Shape Layers - Question marks floating */}
				{[...Array(7)].map((_, i) => {
					const seed = seededRandom(i + 740);
					const size = 60 + seed * 100;
					const x = seed * 1920;
					const y = seededRandom(i + 741) * 1080;
					const rot = frame * (0.06 + seededRandom(i + 742) * 0.1);
					const opacity = 0.04 + seededRandom(i + 743) * 0.04;
					const floatY = Math.sin(frame * 0.02 + i) * 15;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y + floatY,
								transform: `rotate(${rot}deg)`,
								opacity,
								fontFamily: 'Inter',
								fontSize: size,
								color: '#F5F5F5',
								fontWeight: 700,
							}}
						>
							?
						</div>
					);
				})}

				{/* Motion Texture - Horizontal gradient bars */}
				{[...Array(12)].map((_, i) => {
					const y = 50 + i * 85;
					const width = interpolate(
						frame,
						[i * 5, i * 5 + 40],
						[0, 300 + seededRandom(i + 750) * 400],
						{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
					);
					return (
						<div
							key={`bar-${i}`}
							style={{
								position: 'absolute',
								right: 0,
								top: y,
								width,
								height: 2,
								background: `linear-gradient(270deg, rgba(245,245,245,0.04) 0%, transparent 100%)`,
							}}
						/>
					);
				})}

				{/* Particles */}
				{[...Array(25)].map((_, i) => {
					const seed = seededRandom(i + 760);
					const x = seed * 1920;
					const y = seededRandom(i + 761) * 1080;
					const size = 2 + seededRandom(i + 762) * 3;
					const floatY = Math.sin(frame * 0.045 + i * 2) * 18;
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y + floatY,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: i % 5 === 0 ? '#FFC107' : '#F5F5F5',
								opacity: 0.12 + Math.sin(frame * 0.04 + i) * 0.08,
							}}
						/>
					);
				})}

				{/* Main Content - Speech bubble comparison */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Left side - BEFORE (filled speech bubble) */}
					<div
						style={{
							position: 'absolute',
							left: '5%',
							top: '18%',
							opacity: sp(10),
							transform: `scale(${sp(10)})`,
						}}
					>
						{/* Speech bubble SVG */}
						<div
							style={{
								position: 'relative',
								backgroundColor: '#2D2D2D',
								border: '3px solid #FFC107',
								borderRadius: 30,
								padding: '35px 45px',
								maxWidth: 550,
							}}
						>
							{/* Tail */}
							<div
								style={{
									position: 'absolute',
									bottom: -20,
									left: 60,
									width: 0,
									height: 0,
									borderLeft: '15px solid transparent',
									borderRight: '15px solid transparent',
									borderTop: '20px solid #FFC107',
								}}
							/>
							<div
								style={{
									fontFamily: 'Noto Sans Devanagari',
									fontSize: 38,
									color: '#9E9E9E',
									marginBottom: 10,
								}}
							>
								"आप क्या करते हैं?"
							</div>
							<div
								style={{
									fontFamily: 'Yantra One',
									fontSize: 56,
									color: '#FFC107',
									lineHeight: 1.3,
									opacity: sp(25),
								}}
							>
								"मैं अधिकारी हूँ"
							</div>
						</div>
						{/* Label */}
						<div
							style={{
								fontFamily: 'Inter',
								fontSize: 28,
								color: '#9E9E9E',
								marginTop: 35,
								textAlign: 'center',
								opacity: sp(20),
								fontWeight: 600,
							}}
						>
							✅ पहले — जवाब तैयार
						</div>
					</div>

					{/* Arrow in center */}
					<div
						style={{
							position: 'absolute',
							left: '48%',
							top: '45%',
							opacity: sp(50),
							transform: `scale(${sp(50)})`,
						}}
					>
						<svg width="80" height="80" viewBox="0 0 24 24" fill="none">
							<path
								d="M5 12h14m0 0l-7-7m7 7l-7 7"
								stroke="#ff0000"
								strokeWidth="3"
								strokeLinecap="round"
								strokeLinejoin="round"
							/>
						</svg>
					</div>

					{/* Right side - AFTER (empty/confused speech bubble) */}
					<div
						style={{
							position: 'absolute',
							right: '5%',
							top: '18%',
							opacity: sp(60),
							transform: `scale(${sp(60)})`,
						}}
					>
						<div
							style={{
								position: 'relative',
								backgroundColor: 'rgba(45,45,45,0.5)',
								border: '3px dashed #9E9E9E',
								borderRadius: 30,
								padding: '35px 45px',
								maxWidth: 550,
							}}
						>
							<div
								style={{
									position: 'absolute',
									bottom: -20,
									left: 60,
									width: 0,
									height: 0,
									borderLeft: '15px solid transparent',
									borderRight: '15px solid transparent',
									borderTop: '20px solid #9E9E9E',
								}}
							/>
							<div
								style={{
									fontFamily: 'Noto Sans Devanagari',
									fontSize: 38,
									color: '#9E9E9E',
									marginBottom: 10,
								}}
							>
								"आप क्या करते हैं?"
							</div>
							<div
								style={{
									fontFamily: 'Yantra One',
									fontSize: 56,
									color: '#ff0000',
									lineHeight: 1.3,
									opacity: sp(75),
								}}
							>
								"......"
							</div>
						</div>
						<div
							style={{
								fontFamily: 'Inter',
								fontSize: 28,
								color: '#ff0000',
								marginTop: 35,
								textAlign: 'center',
								opacity: sp(70),
								fontWeight: 600,
							}}
						>
							❌ बाद — खामोशी
						</div>
					</div>

					{/* Bottom text */}
					<div
						style={{
							position: 'absolute',
							bottom: '10%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(85), [0, 1], [30, 0])}px)`,
							opacity: sp(85),
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 48,
								color: '#F5F5F5',
								textAlign: 'center',
								fontWeight: 700,
							}}
						>
							बहुत से लोग इस{' '}
							<span style={{color: '#ff0000'}}>बदलाव को स्वीकार</span>{' '}
							नहीं कर पाते
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - confused */}
				<div
					style={{
						position: 'absolute',
						left: '46%',
						bottom: '22%',
						opacity: sp(55),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="80" height="130" viewBox="0 0 80 130">
						<circle cx="40" cy="25" r="30" fill="#FFC107" opacity="0.06" />
						<circle
							cx="40"
							cy="25"
							r="14"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 4} 40 25)`}
						/>
						<circle cx="34" cy="23" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
						<circle cx="46" cy="23" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="39" y1="23" x2="41" y2="23" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="40" y1="39" x2="40" y2="80" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Shrug arms */}
						<line x1="40" y1="55" x2="15" y2="45" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="40" y1="55" x2="65" y2="45" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Hands up = confused */}
						<line x1="15" y1="45" x2="10" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="65" y1="45" x2="70" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="40" y1="80" x2="28" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="40" y1="80" x2="52" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene3_JawabBadal;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene4_KritrimPehchaan',
	durationInSeconds: 12,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene4_KritrimPehchaan: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera tilt
	const cameraTilt = interpolate(frame, [0, durationInFrames], [1, -1], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill
				style={{
					transform: `rotate(${cameraTilt * 0.3}deg)`,
				}}
			>
				{/* Gradient Background - flashy to dark */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(160deg, #2D1A0A 0%, #1C1C1C 40%, #1A0A2D 80%, #1C1C1C 100%)`,
					}}
				/>
				{/* Spotlight effect */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at ${45 + Math.sin(frame * 0.02) * 15}% ${35 + Math.cos(frame * 0.015) * 10}%, rgba(255,193,7,0.1) 0%, transparent 35%)`,
					}}
				/>

				{/* Shape Layers - Stars and triangles (status symbols) */}
				{[...Array(6)].map((_, i) => {
					const seed = seededRandom(i + 780);
					const size = 80 + seed * 150;
					const x = seed * 1920;
					const y = seededRandom(i + 781) * 1080;
					const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1);
					const opacity = 0.03 + seededRandom(i + 782) * 0.04;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.01 + i) * 25,
								top: y + Math.cos(frame * 0.008 + i) * 20,
								width: size,
								height: size,
								transform: `rotate(${rot}deg)`,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								{i % 2 === 0 ? (
									<polygon
										points="50,5 61,35 95,35 68,57 79,91 50,70 21,91 32,57 5,35 39,35"
										fill="none"
										stroke="#FFC107"
										strokeWidth="1"
									/>
								) : (
									<polygon
										points="50,10 90,90 10,90"
										fill="none"
										stroke="#9E9E9E"
										strokeWidth="1"
									/>
								)}
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Gold dust falling */}
				{[...Array(40)].map((_, i) => {
					const seed = seededRandom(i + 790);
					const x = seed * 1920;
					const fallSpeed = 0.8 + seededRandom(i + 791) * 1.5;
					const y = ((seededRandom(i + 792) * 1080 + frame * fallSpeed) % 1200) - 60;
					const size = 1 + seededRandom(i + 793) * 3;
					const sparkle = Math.sin(frame * 0.1 + i * 3) > 0.5 ? 0.4 : 0.1;

					return (
						<div
							key={`dust-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.02 + i) * 8,
								top: y,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: '#FFC107',
								opacity: sparkle,
							}}
						/>
					);
				})}

				{/* Main Content - Status items grid with X marks */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Title */}
					<div
						style={{
							position: 'absolute',
							top: '8%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(5), [0, 1], [-50, 0])}px)`,
							opacity: sp(5),
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 90,
								color: '#ff0000',
								textAlign: 'center',
								textShadow: '0 0 40px rgba(255,0,0,0.3)',
							}}
						>
							कृत्रिम पहचान
						</div>
					</div>

					{/* Status items in 2x2 grid */}
					{[
						{label: 'महंगे कपड़े', icon: 'shirt', delay: 20},
						{label: 'बड़ी गाड़ी', icon: 'car', delay: 35},
						{label: 'भव्य आयोजन', icon: 'party', delay: 50},
						{label: 'उंची जीवन शैली', icon: 'crown', delay: 65},
					].map((item, i) => {
						const col = i % 2;
						const row = Math.floor(i / 2);
						const xPos = 15 + col * 45;
						const yPos = 32 + row * 30;

						return (
							<div
								key={`status-${i}`}
								style={{
									position: 'absolute',
									left: `${xPos}%`,
									top: `${yPos}%`,
									width: 380,
									opacity: sp(item.delay),
									transform: `scale(${sp(item.delay)})`,
								}}
							>
								{/* Card */}
								<div
									style={{
										backgroundColor: 'rgba(45,45,45,0.8)',
										border: '2px solid rgba(255,193,7,0.3)',
										borderRadius: 20,
										padding: '25px 30px',
										display: 'flex',
										alignItems: 'center',
										gap: 20,
										position: 'relative',
									}}
								>
									{/* Icon */}
									<div
										style={{
											width: 60,
											height: 60,
											borderRadius: 12,
											backgroundColor: 'rgba(255,193,7,0.1)',
											display: 'flex',
											alignItems: 'center',
											justifyContent: 'center',
											flexShrink: 0,
										}}
									>
										<svg width="32" height="32" viewBox="0 0 24 24" fill="none">
											{item.icon === 'shirt' && (
												<path d="M20.38 3.46L16 2 12 5 8 2 3.62 3.46a1 1 0 00-.72.84L2 13l4 1v8h12v-8l4-1-.9-8.7a1 1 0 00-.72-.84z" stroke="#FFC107" strokeWidth="2" />
											)}
											{item.icon === 'car' && (
												<path d="M5 17h14M5 17a2 2 0 01-2-2V9a2 2 0 012-2h1l2-3h8l2 3h1a2 2 0 012 2v6a2 2 0 01-2 2M5 17v1a1 1 0 001 1h1a1 1 0 001-1v-1m10 0v1a1 1 0 001 1h1a1 1 0 001-1v-1" stroke="#FFC107" strokeWidth="2" />
											)}
											{item.icon === 'party' && (
												<path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke="#FFC107" strokeWidth="2" />
											)}
											{item.icon === 'crown' && (
												<path d="M2 20h20L19 8l-4 4-3-6-3 6-4-4-3 12z" stroke="#FFC107" strokeWidth="2" strokeLinejoin="round" />
											)}
										</svg>
									</div>
									{/* Label */}
									<div
										style={{
											fontFamily: 'Noto Sans Devanagari',
											fontSize: 40,
											color: '#F5F5F5',
											fontWeight: 600,
										}}
									>
										{item.label}
									</div>

									{/* Red X overlay - appears after */}
									<div
										style={{
											position: 'absolute',
											top: -10,
											right: -10,
											opacity: sp(item.delay + 20),
											transform: `scale(${sp(item.delay + 20)})`,
										}}
									>
										<svg width="45" height="45" viewBox="0 0 24 24">
											<circle cx="12" cy="12" r="11" fill="#ff0000" opacity="0.9" />
											<path d="M8 8l8 8M16 8l-8 8" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
										</svg>
									</div>
								</div>
							</div>
						);
					})}

					{/* Bottom punchline */}
					<div
						style={{
							position: 'absolute',
							bottom: '8%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(90), [0, 1], [30, 0])}px)`,
							opacity: sp(90),
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 46,
								color: '#FFC107',
								textAlign: 'center',
								fontWeight: 700,
							}}
						>
							सिर्फ इसलिए कि{' '}
							<span style={{color: '#F5F5F5'}}>लोगों की नजर में बड़े दिखें</span>
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - showing off (sunglasses, arms spread) */}
				<div
					style={{
						position: 'absolute',
						left: '47%',
						bottom: '18%',
						opacity: sp(40),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="90" height="140" viewBox="0 0 90 140">
						<circle cx="45" cy="28" r="32" fill="#FFC107" opacity="0.05" />
						<circle
							cx="45"
							cy="28"
							r="15"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 45 28)`}
						/>
						{/* Sunglasses (not Buffett glasses - different) */}
						<rect x="33" y="24" width="11" height="8" rx="2" fill="none" stroke="#FFC107" strokeWidth="2" />
						<rect x="47" y="24" width="11" height="8" rx="2" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="44" y1="28" x2="47" y2="28" stroke="#FFC107" strokeWidth="2" />
						{/* Body */}
						<line x1="45" y1="43" x2="45" y2="88" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Arms spread showing off */}
						<line x1="45" y1="58" x2="10" y2="50" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="58" x2="80" y2="50" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Legs */}
						<line x1="45" y1="88" x2="30" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="88" x2="60" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene4_KritrimPehchaan;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene5_BankAdhikari',
	durationInSeconds: 14,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene5_BankAdhikari: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera orbit slow
	const orbitAngle = interpolate(frame, [0, durationInFrames], [0, 3], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill
				style={{
					transform: `perspective(1200px) rotateY(${orbitAngle}deg)`,
				}}
			>
				{/* Gradient - Split screen: warm left, cold right */}
				<div
					style={{
						position: 'absolute',
						width: '50%',
						height: '100%',
						left: 0,
						background: `linear-gradient(180deg, #2D2010 0%, #1C1C1C 100%)`,
					}}
				/>
				<div
					style={{
						position: 'absolute',
						width: '50%',
						height: '100%',
						right: 0,
						background: `linear-gradient(180deg, #101828 0%, #1C1C1C 100%)`,
					}}
				/>
				{/* Divider line */}
				<div
					style={{
						position: 'absolute',
						left: '50%',
						top: '5%',
						width: 2,
						height: `${interpolate(sp(15), [0, 1], [0, 90])}%`,
						background: 'linear-gradient(to bottom, #FFC107, #9E9E9E, transparent)',
					}}
				/>

				{/* Shape Layers - Clocks drifting */}
				{[...Array(4)].map((_, i) => {
					const seed = seededRandom(i + 800);
					const size = 100 + seed * 140;
					const x = seed * 1920;
					const y = seededRandom(i + 801) * 1080;
					const rot = frame * 0.15;
					const opacity = 0.04 + seededRandom(i + 802) * 0.03;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.01 + i) * 20,
								top: y + Math.cos(frame * 0.008 + i) * 15,
								width: size,
								height: size,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								<circle cx="50" cy="50" r="45" fill="none" stroke="#9E9E9E" strokeWidth="1" />
								<line
									x1="50"
									y1="50"
									x2="50"
									y2="20"
									stroke="#FFC107"
									strokeWidth="1.5"
									transform={`rotate(${rot * 2} 50 50)`}
								/>
								<line
									x1="50"
									y1="50"
									x2="70"
									y2="50"
									stroke="#9E9E9E"
									strokeWidth="1"
									transform={`rotate(${rot * 0.5} 50 50)`}
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - vertical lines like prison bars fading */}
				{[...Array(15)].map((_, i) => {
					const x = 960 + (i - 7) * 35;
					const opacity = interpolate(
						Math.abs(i - 7),
						[0, 7],
						[0.06, 0.01],
						{extrapolateRight: 'clamp'}
					);
					return (
						<div
							key={`vline-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: 0,
								width: 1,
								height: '100%',
								backgroundColor: `rgba(245,245,245,${opacity})`,
							}}
						/>
					);
				})}

				{/* Particles */}
				{[...Array(28)].map((_, i) => {
					const seed = seededRandom(i + 810);
					const x = seed * 1920;
					const y = seededRandom(i + 811) * 1080;
					const size = 2 + seededRandom(i + 812) * 3;
					const floatY = Math.sin(frame * 0.04 + i * 1.8) * 15;
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y + floatY,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: x < 960 ? '#FFC107' : '#9E9E9E',
								opacity: 0.12,
							}}
						/>
					);
				})}

				{/* Main Content */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* LEFT SIDE - अतीत भरा हुआ */}
					<div
						style={{
							position: 'absolute',
							left: '5%',
							top: '8%',
							width: '40%',
							opacity: sp(10),
							transform: `translateX(${interpolate(sp(10), [0, 1], [-60, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 70,
								color: '#FFC107',
								marginBottom: 30,
							}}
						>
							अतीत
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 50,
								color: '#F5F5F5',
								marginBottom: 15,
							}}
						>
							भरा हुआ
						</div>

						{/* Past items stacking */}
						{['बैंक अधिकारी', 'बड़ा पद', 'सम्मान', 'ताकत'].map(
							(item, i) => (
								<div
									key={`past-${i}`}
									style={{
										fontFamily: 'Noto Sans Devanagari',
										fontSize: 36,
										color: '#FFC107',
										padding: '8px 20px',
										backgroundColor: 'rgba(255,193,7,0.08)',
										borderLeft: '3px solid #FFC107',
										marginTop: 10,
										opacity: sp(20 + i * 12),
										transform: `translateX(${interpolate(sp(20 + i * 12), [0, 1], [40, 0])}px)`,
									}}
								>
									{item}
								</div>
							)
						)}
					</div>

					{/* RIGHT SIDE - वर्तमान खाली */}
					<div
						style={{
							position: 'absolute',
							right: '5%',
							top: '8%',
							width: '40%',
							opacity: sp(70),
							transform: `translateX(${interpolate(sp(70), [0, 1], [60, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 70,
								color: '#9E9E9E',
								marginBottom: 30,
							}}
						>
							वर्तमान
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 50,
								color: '#ff0000',
								marginBottom: 15,
							}}
						>
							खाली
						</div>

						{/* Empty boxes */}
						{[0, 1, 2, 3].map((i) => (
							<div
								key={`empty-${i}`}
								style={{
									height: 50,
									borderLeft: '3px dashed #9E9E9E',
									marginTop: 10,
									padding: '8px 20px',
									opacity: sp(80 + i * 8),
									display: 'flex',
									alignItems: 'center',
								}}
							>
								<div
									style={{
										fontFamily: 'Noto Sans Devanagari',
										fontSize: 36,
										color: '#9E9E9E',
										opacity: 0.4,
									}}
								>
									...
								</div>
							</div>
						))}
					</div>

					{/* Center - "7 बरस बाद भी" */}
					<div
						style={{
							position: 'absolute',
							bottom: '10%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(100), [0, 1], [25, 0])}px)`,
							opacity: sp(100),
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 48,
								color: '#F5F5F5',
								textAlign: 'center',
								fontWeight: 700,
								backgroundColor: 'rgba(255,0,0,0.15)',
								padding: '12px 40px',
								borderRadius: 15,
								border: '2px solid rgba(255,0,0,0.3)',
							}}
						>
							सात बरस बाद भी — हर बातचीत में{' '}
							<span style={{color: '#FFC107'}}>पुराने पद</span> का जिक्र
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - talking/boasting pose */}
				<div
					style={{
						position: 'absolute',
						left: '22%',
						bottom: '10%',
						opacity: sp(50),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="90" height="140" viewBox="0 0 90 140">
						<circle cx="45" cy="25" r="30" fill="#FFC107" opacity="0.06" />
						<circle
							cx="45"
							cy="25"
							r="14"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 3} 45 25)`}
						/>
						<circle cx="38" cy="23" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="52" cy="23" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="45" y1="23" x2="45" y2="23" stroke="#FFC107" strokeWidth="2" />
						{/* Mouth open - talking */}
						<ellipse cx="45" cy="33" rx="4" ry={2 + Math.sin(frame * 0.15) * 1.5} fill="none" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="45" y1="39" x2="45" y2="85" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* One arm pointing up, one gesturing */}
						<line x1="45" y1="55" x2="20" y2="40" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="20" y1="40" x2="15" y2="30" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="55" x2="72" y2="65" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="85" x2="32" y2="120" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="85" x2="58" y2="120" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene5_BankAdhikari;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene6_NaiPehchaan',
	durationInSeconds: 12,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene6_NaiPehchaan: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera zoom out revealing
	const cameraScale = interpolate(frame, [0, 60], [1.15, 1], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill style={{transform: `scale(${cameraScale})`}}>
				{/* Gradient - Dawn/sunrise feel = new beginning */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(170deg, #1C1C1C 0%, #2D1A0A 30%, #3D2A10 50%, #2D2D2D 80%, #1C1C1C 100%)`,
					}}
				/>
				{/* Warm glow rising from bottom */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(ellipse at 50% 100%, rgba(255,193,7,0.12) 0%, transparent 50%)`,
						opacity: interpolate(frame, [0, 90], [0.3, 1], {extrapolateRight: 'clamp'}),
					}}
				/>

				{/* Shape Layers - Butterflies / metamorphosis shapes */}
				{[...Array(5)].map((_, i) => {
					const seed = seededRandom(i + 830);
					const size = 80 + seed * 120;
					const x = 200 + seed * 1500;
					const y = seededRandom(i + 831) * 800;
					const rot = Math.sin(frame * 0.03 + i) * 20;
					const opacity = 0.04 + seededRandom(i + 832) * 0.04;
					const floatY = Math.sin(frame * 0.02 + i * 2) * 20;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y + floatY,
								width: size,
								height: size,
								transform: `rotate(${rot}deg)`,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								{/* Butterfly wing shape */}
								<path
									d="M50 50 Q30 10 10 30 Q30 50 50 50 Q30 50 10 70 Q30 90 50 50 Q70 10 90 30 Q70 50 50 50 Q70 50 90 70 Q70 90 50 50"
									fill="none"
									stroke="#FFC107"
									strokeWidth="1"
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Rising streaks of light */}
				{[...Array(15)].map((_, i) => {
					const x = 100 + i * 120;
					const riseProgress = interpolate(
						frame,
						[i * 8, i * 8 + 60],
						[1080, -100],
						{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
					);
					return (
						<div
							key={`streak-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: riseProgress,
								width: 2,
								height: 80,
								background: `linear-gradient(to top, transparent, rgba(255,193,7,0.06), transparent)`,
								borderRadius: 2,
							}}
						/>
					);
				})}

				{/* Particles - warmer tones */}
				{[...Array(30)].map((_, i) => {
					const seed = seededRandom(i + 840);
					const x = seed * 1920;
					const y = seededRandom(i + 841) * 1080;
					const size = 2 + seededRandom(i + 842) * 4;
					const floatY = Math.sin(frame * 0.04 + i * 1.5) * 20;
					const floatX = Math.cos(frame * 0.025 + i * 2) * 10;
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: x + floatX,
								top: y + floatY,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: '#FFC107',
								opacity: 0.1 + Math.sin(frame * 0.05 + i) * 0.08,
							}}
						/>
					);
				})}

				{/* Main Content - Chain breaking into butterfly */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Left - chains/costs of old identity */}
					<div
						style={{
							position: 'absolute',
							left: '5%',
							top: '15%',
							opacity: sp(8),
							transform: `translateX(${interpolate(sp(8), [0, 1], [-50, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 40,
								color: '#ff0000',
								fontWeight: 700,
								marginBottom: 20,
							}}
						>
							पुरानी पहचान = पुराने खर्चे
						</div>
						{/* Chain links */}
						<svg width="350" height="200" viewBox="0 0 350 200">
							{[0, 1, 2, 3].map((j) => (
								<g key={`chain-${j}`} opacity={sp(15 + j * 10)}>
									<ellipse
										cx={50 + j * 80}
										cy={100}
										rx="35"
										ry="20"
										fill="none"
										stroke="#9E9E9E"
										strokeWidth="3"
									/>
									{/* Breaking effect on later chains */}
									{j >= 2 && (
										<line
											x1={50 + j * 80 - 20}
											y1={85}
											x2={50 + j * 80 + 20}
											y2={115}
											stroke="#ff0000"
											strokeWidth="4"
											opacity={sp(60 + j * 10)}
										/>
									)}
								</g>
							))}
						</svg>
					</div>

					{/* Center arrow transition */}
					<div
						style={{
							position: 'absolute',
							left: '50%',
							top: '35%',
							transform: `translateX(-50%)`,
							opacity: sp(55),
						}}
					>
						<svg width="120" height="60" viewBox="0 0 120 60">
							<path
								d="M10 30 L80 30 M65 15 L80 30 L65 45"
								stroke="#FFC107"
								strokeWidth="4"
								strokeLinecap="round"
								fill="none"
							/>
						</svg>
					</div>

					{/* Right - New identity */}
					<div
						style={{
							position: 'absolute',
							right: '5%',
							top: '10%',
							maxWidth: 650,
							opacity: sp(60),
							transform: `translateX(${interpolate(sp(60), [0, 1], [50, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 100,
								color: '#FFC107',
								lineHeight: 1.15,
								textShadow: '0 0 50px rgba(255,193,7,0.2)',
							}}
						>
							नई पहचान
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 60,
								color: '#F5F5F5',
								lineHeight: 1.3,
								marginTop: 10,
								opacity: sp(75),
							}}
						>
							गढ़ने का मौका
						</div>
					</div>

					{/* Bottom subtitle */}
					<div
						style={{
							position: 'absolute',
							bottom: '10%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(90), [0, 1], [20, 0])}px)`,
							opacity: sp(90),
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 46,
								color: '#F5F5F5',
								textAlign: 'center',
								fontWeight: 600,
							}}
						>
							वो पहचान जो{' '}
							<span style={{color: '#FFC107'}}>इनसान होने</span> पर टिकी हो
						</div>
					</div>

					{/* Sunrise icon */}
					<div
						style={{
							position: 'absolute',
							right: '15%',
							bottom: '25%',
							opacity: sp(80),
							transform: `scale(${sp(80)})`,
						}}
					>
						<svg width="120" height="80" viewBox="0 0 120 80">
							{/* Sun rising */}
							<circle
								cx="60"
								cy="60"
								r="25"
								fill="none"
								stroke="#FFC107"
								strokeWidth="3"
							/>
							{/* Rays */}
							{[0, 30, 60, 90, 120, 150, 180].map((angle) => {
								const rad = (angle * Math.PI) / 180;
								return (
									<line
										key={`ray-${angle}`}
										x1={60 + Math.cos(rad) * 30}
										y1={60 - Math.sin(rad) * 30}
										x2={60 + Math.cos(rad) * 45}
										y2={60 - Math.sin(rad) * 45}
										stroke="#FFC107"
										strokeWidth="2"
										strokeLinecap="round"
										opacity={0.5 + Math.sin(frame * 0.06 + angle) * 0.3}
									/>
								);
							})}
							{/* Horizon line */}
							<line x1="10" y1="60" x2="110" y2="60" stroke="#9E9E9E" strokeWidth="1" />
						</svg>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - arms open, welcoming new identity */}
				<div
					style={{
						position: 'absolute',
						left: '44%',
						bottom: '15%',
						opacity: sp(50),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="100" height="150" viewBox="0 0 100 150">
						<circle cx="50" cy="28" r="35" fill="#FFC107" opacity="0.08" />
						<circle
							cx="50"
							cy="28"
							r="15"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 50 28)`}
						/>
						<circle cx="43" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="57" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="49" y1="26" x2="51" y2="26" stroke="#FFC107" strokeWidth="2" />
						{/* Smile */}
						<path d="M42 33 Q50 40 58 33" fill="none" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="50" y1="43" x2="50" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Arms open wide - welcoming */}
						<line x1="50" y1="58" x2="15" y2="45" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="58" x2="85" y2="45" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="90" x2="35" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="90" x2="65" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene6_NaiPehchaan;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene7_PehlaSawaal',
	durationInSeconds: 12,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene7_PehlaSawaal: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera slow pan up
	const cameraPanY = interpolate(frame, [0, durationInFrames], [15, -15], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill style={{transform: `translateY(${cameraPanY}px)`}}>
				{/* Gradient - thoughtful purple-blue */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(200deg, #1A1A2D 0%, #1C1C1C 40%, #2D1A2D 70%, #1C1C1C 100%)`,
					}}
				/>
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 70% 30%, rgba(233,215,0,0.05) 0%, transparent 40%)`,
					}}
				/>

				{/* Shape Layers - Hourglass shapes */}
				{[...Array(4)].map((_, i) => {
					const seed = seededRandom(i + 850);
					const size = 100 + seed * 150;
					const x = 150 + seed * 1600;
					const y = seededRandom(i + 851) * 900;
					const rot = frame * 0.04 * (i % 2 === 0 ? 1 : -1);
					const opacity = 0.03 + seededRandom(i + 852) * 0.04;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.01 + i) * 20,
								top: y + Math.cos(frame * 0.008 + i) * 15,
								width: size,
								height: size,
								transform: `rotate(${rot}deg)`,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								<polygon
									points="20,10 80,10 60,50 80,90 20,90 40,50"
									fill="none"
									stroke="#e9d700"
									strokeWidth="1.5"
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Grid dots */}
				{[...Array(100)].map((_, i) => {
					const col = i % 10;
					const row = Math.floor(i / 10);
					const x = 160 + col * 170;
					const y = 80 + row * 100;
					const delay = (col + row) * 3;
					const dotOp = interpolate(
						frame,
						[delay, delay + 20],
						[0, 0.04],
						{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
					);
					return (
						<div
							key={`dot-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y,
								width: 3,
								height: 3,
								borderRadius: '50%',
								backgroundColor: '#F5F5F5',
								opacity: dotOp,
							}}
						/>
					);
				})}

				{/* Particles */}
				{[...Array(25)].map((_, i) => {
					const seed = seededRandom(i + 860);
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: seed * 1920,
								top: seededRandom(i + 861) * 1080 + Math.sin(frame * 0.04 + i) * 18,
								width: 2 + seededRandom(i + 862) * 3,
								height: 2 + seededRandom(i + 862) * 3,
								borderRadius: '50%',
								backgroundColor: i % 4 === 0 ? '#e9d700' : '#F5F5F5',
								opacity: 0.1 + Math.sin(frame * 0.05 + i) * 0.08,
							}}
						/>
					);
				})}

				{/* Main Content - Big Question with scale */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Big question mark background */}
					<div
						style={{
							position: 'absolute',
							left: '5%',
							top: '10%',
							fontFamily: 'Inter',
							fontSize: 500,
							fontWeight: 900,
							color: '#FFC107',
							opacity: 0.04 + Math.sin(frame * 0.02) * 0.02,
							lineHeight: 1,
						}}
					>
						?
					</div>

					{/* Question #1 Badge */}
					<div
						style={{
							position: 'absolute',
							left: '8%',
							top: '12%',
							opacity: sp(5),
							transform: `scale(${sp(5)})`,
						}}
					>
						<div
							style={{
								width: 80,
								height: 80,
								borderRadius: '50%',
								backgroundColor: '#FFC107',
								display: 'flex',
								alignItems: 'center',
								justifyContent: 'center',
								fontFamily: 'Inter',
								fontSize: 40,
								fontWeight: 900,
								color: '#1C1C1C',
							}}
						>
							1
						</div>
					</div>

					{/* Scale/Balance SVG */}
					<div
						style={{
							position: 'absolute',
							left: '50%',
							top: '20%',
							transform: `translateX(-50%) scale(${sp(15)})`,
							opacity: sp(15),
						}}
					>
						<svg width="500" height="280" viewBox="0 0 500 280">
							{/* Center pole */}
							<line x1="250" y1="20" x2="250" y2="250" stroke="#9E9E9E" strokeWidth="3" />
							{/* Base */}
							<rect x="200" y="245" width="100" height="15" rx="5" fill="#9E9E9E" />
							{/* Beam - tilting based on frame */}
							{(() => {
								const tiltAngle = interpolate(frame, [60, 120], [0, -8], {
									extrapolateLeft: 'clamp',
									extrapolateRight: 'clamp',
								});
								return (
									<g transform={`rotate(${tiltAngle} 250 40)`}>
										<line x1="80" y1="40" x2="420" y2="40" stroke="#FFC107" strokeWidth="4" />
										{/* Left pan - "पहले की जिन्दगी" */}
										<line x1="80" y1="40" x2="80" y2="100" stroke="#9E9E9E" strokeWidth="2" />
										<line x1="60" y1="40" x2="100" y2="40" stroke="#9E9E9E" strokeWidth="2" />
										<path d="M50 100 Q80 120 110 100" fill="none" stroke="#9E9E9E" strokeWidth="2" />
										{/* Right pan - "अब की जिन्दगी" */}
										<line x1="420" y1="40" x2="420" y2="100" stroke="#9E9E9E" strokeWidth="2" />
										<line x1="400" y1="40" x2="440" y2="40" stroke="#9E9E9E" strokeWidth="2" />
										<path d="M390 100 Q420 120 450 100" fill="none" stroke="#FFC107" strokeWidth="2" />
									</g>
								);
							})()}
						</svg>
					</div>

					{/* Labels for scale */}
					<div
						style={{
							position: 'absolute',
							left: '8%',
							top: '50%',
							opacity: sp(40),
							transform: `translateY(${interpolate(sp(40), [0, 1], [20, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 42,
								color: '#9E9E9E',
								fontWeight: 600,
								padding: '10px 25px',
								backgroundColor: 'rgba(158,158,158,0.08)',
								borderRadius: 12,
							}}
						>
							पहले जीते थे
						</div>
					</div>

					<div
						style={{
							position: 'absolute',
							right: '8%',
							top: '50%',
							opacity: sp(55),
							transform: `translateY(${interpolate(sp(55), [0, 1], [20, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 42,
								color: '#FFC107',
								fontWeight: 700,
								padding: '10px 25px',
								backgroundColor: 'rgba(255,193,7,0.1)',
								borderRadius: 12,
								border: '2px solid rgba(255,193,7,0.3)',
							}}
						>
							अब जीनी है ✓
						</div>
					</div>

					{/* Bottom question */}
					<div
						style={{
							position: 'absolute',
							bottom: '10%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(75), [0, 1], [25, 0])}px)`,
							opacity: sp(75),
							maxWidth: 1000,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 56,
								color: '#F5F5F5',
								textAlign: 'center',
								lineHeight: 1.4,
							}}
						>
							20 बरस पहले{' '}
							<span style={{color: '#FFC107'}}>सही थी</span> — अब भी{' '}
							<span style={{color: '#ff0000'}}>सही है क्या?</span>
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - scratching head thinking */}
				<div
					style={{
						position: 'absolute',
						right: '3%',
						bottom: '5%',
						opacity: sp(45),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="90" height="140" viewBox="0 0 90 140">
						<circle cx="45" cy="28" r="30" fill="#FFC107" opacity="0.06" />
						<circle
							cx="45"
							cy="28"
							r="14"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 3} 45 28)`}
						/>
						<circle cx="38" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="52" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="45" y1="26" x2="45" y2="26" stroke="#FFC107" strokeWidth="2" />
						<line x1="45" y1="42" x2="45" y2="88" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Hand on head - thinking */}
						<line x1="45" y1="58" x2="25" y2="70" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="58" x2="65" y2="48" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="65" y1="48" x2="55" y2="20" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="88" x2="33" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="88" x2="57" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene7_PehlaSawaal;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene8_AsliKeemat',
	durationInSeconds: 11,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene8_AsliKeemat: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera zoom + slight shake
	const camZ = interpolate(frame, [0, durationInFrames], [1, 1.05], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill style={{transform: `scale(${camZ})`}}>
				{/* Gradient - Dark with red undertone */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(ellipse at ${40 + Math.sin(frame * 0.01) * 10}% ${50 + Math.cos(frame * 0.008) * 8}%, #2D1A1A 0%, #1C1C1C 50%, #0D0D0D 100%)`,
					}}
				/>

				{/* Shape Layers - Pie chart segments drifting */}
				{[...Array(5)].map((_, i) => {
					const seed = seededRandom(i + 870);
					const size = 100 + seed * 160;
					const x = seed * 1920;
					const y = seededRandom(i + 871) * 1080;
					const rot = frame * 0.06 * (i % 2 === 0 ? 1 : -1);
					const opacity = 0.03 + seededRandom(i + 872) * 0.04;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.01 + i) * 20,
								top: y + Math.cos(frame * 0.008 + i) * 15,
								width: size,
								height: size,
								transform: `rotate(${rot}deg)`,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								<circle cx="50" cy="50" r="40" fill="none" stroke="#ff0000" strokeWidth="1" />
								<path
									d={`M50 50 L50 10 A40 40 0 0 1 ${50 + 40 * Math.cos((i * 72 * Math.PI) / 180)} ${50 - 40 * Math.sin((i * 72 * Math.PI) / 180)} Z`}
									fill="none"
									stroke="#FFC107"
									strokeWidth="1"
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Dissolving pixels */}
				{[...Array(50)].map((_, i) => {
					const seed = seededRandom(i + 880);
					const x = seed * 1920;
					const y = seededRandom(i + 881) * 1080;
					const size = 4 + seededRandom(i + 882) * 8;
					const delay = seed * 60;
					const dissolve = interpolate(
						frame,
						[delay, delay + 40, delay + 80],
						[0, 0.05, 0],
						{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
					);
					return (
						<div
							key={`pixel-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y,
								width: size,
								height: size,
								backgroundColor: '#ff0000',
								opacity: dissolve,
							}}
						/>
					);
				})}

				{/* Particles */}
				{[...Array(25)].map((_, i) => {
					const seed = seededRandom(i + 890);
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: seed * 1920,
								top: seededRandom(i + 891) * 1080 + Math.sin(frame * 0.04 + i) * 15,
								width: 2 + seededRandom(i + 892) * 3,
								height: 2 + seededRandom(i + 892) * 3,
								borderRadius: '50%',
								backgroundColor: i % 3 === 0 ? '#ff0000' : '#F5F5F5',
								opacity: 0.1,
							}}
						/>
					);
				})}

				{/* Main Content - Four cost pillars */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Question Badge */}
					<div
						style={{
							position: 'absolute',
							left: '5%',
							top: '8%',
							opacity: sp(5),
							transform: `scale(${sp(5)})`,
						}}
					>
						<div
							style={{
								width: 80,
								height: 80,
								borderRadius: '50%',
								backgroundColor: '#ff0000',
								display: 'flex',
								alignItems: 'center',
								justifyContent: 'center',
								fontFamily: 'Inter',
								fontSize: 40,
								fontWeight: 900,
								color: '#F5F5F5',
							}}
						>
							2
						</div>
					</div>

					{/* Title */}
					<div
						style={{
							position: 'absolute',
							left: '13%',
							top: '8%',
							opacity: sp(8),
							transform: `translateX(${interpolate(sp(8), [0, 1], [40, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 80,
								color: '#F5F5F5',
								lineHeight: 1.2,
							}}
						>
							असली <span style={{color: '#ff0000'}}>कीमत</span> क्या है?
						</div>
					</div>

					{/* Four pillars of cost */}
					{[
						{label: 'पैसा', icon: '₹', color: '#FFC107', delay: 25},
						{label: 'समय', icon: '⏱', color: '#ff0000', delay: 40},
						{label: 'ध्यान', icon: '🎯', color: '#e9d700', delay: 55},
						{label: 'ऊर्जा', icon: '⚡', color: '#FFC107', delay: 70},
					].map((item, i) => {
						const pillarHeight = interpolate(
							sp(item.delay),
							[0, 1],
							[0, 250 + (i === 1 ? 80 : i === 3 ? 60 : 0)]
						);
						const xPos = 12 + i * 22;

						return (
							<div
								key={`pillar-${i}`}
								style={{
									position: 'absolute',
									left: `${xPos}%`,
									bottom: '15%',
									display: 'flex',
									flexDirection: 'column',
									alignItems: 'center',
									gap: 10,
								}}
							>
								{/* Pillar bar */}
								<div
									style={{
										width: 120,
										height: pillarHeight,
										background: `linear-gradient(to top, ${item.color}, ${item.color}88)`,
										borderRadius: '10px 10px 0 0',
										opacity: 0.8,
										position: 'relative',
									}}
								>
									{/* Icon on top */}
									<div
										style={{
											position: 'absolute',
											top: -50,
											left: '50%',
											transform: 'translateX(-50%)',
											opacity: sp(item.delay + 15),
										}}
									>
										<svg width="44" height="44" viewBox="0 0 24 24" fill="none">
											{item.icon === '₹' && (
												<text
													x="12"
													y="18"
													textAnchor="middle"
													fill={item.color}
													fontSize="20"
													fontWeight="bold"
													fontFamily="Inter"
												>
													₹
												</text>
											)}
											{item.icon === '⏱' && (
												<>
													<circle cx="12" cy="13" r="9" stroke={item.color} strokeWidth="2" fill="none" />
													<line x1="12" y1="13" x2="12" y2="8" stroke={item.color} strokeWidth="2" />
													<line x1="12" y1="13" x2="16" y2="13" stroke={item.color} strokeWidth="2" />
												</>
											)}
											{item.icon === '🎯' && (
												<>
													<circle cx="12" cy="12" r="10" stroke={item.color} strokeWidth="1.5" fill="none" />
													<circle cx="12" cy="12" r="6" stroke={item.color} strokeWidth="1.5" fill="none" />
													<circle cx="12" cy="12" r="2" fill={item.color} />
												</>
											)}
											{item.icon === '⚡' && (
												<path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" stroke={item.color} strokeWidth="2" fill="none" />
											)}
										</svg>
									</div>
								</div>
								{/* Label */}
								<div
									style={{
										fontFamily: 'Yantra One',
										fontSize: 40,
										color: item.color,
										opacity: sp(item.delay + 10),
									}}
								>
									{item.label}
								</div>
							</div>
						);
					})}

					{/* Bottom text - eating life silently */}
					<div
						style={{
							position: 'absolute',
							bottom: '5%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(90), [0, 1], [20, 0])}px)`,
							opacity: sp(90),
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 44,
								color: '#F5F5F5',
								textAlign: 'center',
								fontWeight: 600,
							}}
						>
							जिन्दगी का कितना हिस्सा{' '}
							<span style={{color: '#ff0000'}}>चुपचाप खा रही है?</span>
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - energy drained */}
				<div
					style={{
						position: 'absolute',
						right: '4%',
						bottom: '18%',
						opacity: sp(50),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="80" height="130" viewBox="0 0 80 130">
						<circle cx="40" cy="25" r="28" fill="#FFC107" opacity="0.05" />
						<circle
							cx="40"
							cy="25"
							r="13"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 40 25)`}
						/>
						<circle cx="34" cy="23" r="5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="46" cy="23" r="5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="39" y1="23" x2="41" y2="23" stroke="#FFC107" strokeWidth="2" />
						{/* Tired mouth */}
						<path d="M34 31 Q40 28 46 31" fill="none" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="40" y1="38" x2="40" y2="78" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Arms drooping */}
						<line x1="40" y1="52" x2="20" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="40" y1="52" x2="60" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="40" y1="78" x2="28" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="40" y1="78" x2="52" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene8_AsliKeemat;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene9_TeesraSawaal',
	durationInSeconds: 12,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene9_TeesraSawaal: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera zoom out - liberation feel
	const camScale = interpolate(frame, [0, 60], [1.1, 1], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill style={{transform: `scale(${camScale})`}}>
				{/* Gradient - hopeful green-gold */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(150deg, #1C1C1C 0%, #1A2D1A 35%, #2D2D1A 60%, #1C1C1C 100%)`,
					}}
				/>
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 60% 50%, rgba(255,193,7,0.08) 0%, transparent 45%)`,
						opacity: 0.6 + Math.sin(frame * 0.02) * 0.3,
					}}
				/>

				{/* Shape Layers - Open doors/gates */}
				{[...Array(4)].map((_, i) => {
					const seed = seededRandom(i + 900);
					const size = 120 + seed * 160;
					const x = seed * 1800;
					const y = seededRandom(i + 901) * 900;
					const opacity = 0.03 + seededRandom(i + 902) * 0.04;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y + Math.sin(frame * 0.015 + i) * 15,
								width: size,
								height: size * 1.4,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 140" width="100%" height="100%">
								{/* Door arch shape */}
								<path
									d="M10 140 L10 40 Q10 10 50 10 Q90 10 90 40 L90 140"
									fill="none"
									stroke="#FFC107"
									strokeWidth="1.5"
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Expanding rings from center */}
				{[...Array(6)].map((_, i) => {
					const baseRadius = ((frame * 1.5 + i * 50) % 600);
					const opacity = interpolate(baseRadius, [0, 300, 600], [0.06, 0.03, 0], {
						extrapolateRight: 'clamp',
					});
					return (
						<div
							key={`ring-${i}`}
							style={{
								position: 'absolute',
								left: '50%',
								top: '50%',
								width: baseRadius * 2,
								height: baseRadius * 2,
								marginLeft: -baseRadius,
								marginTop: -baseRadius,
								borderRadius: '50%',
								border: `1px solid rgba(255,193,7,${opacity})`,
							}}
						/>
					);
				})}

				{/* Particles - upward floating = liberation */}
				{[...Array(30)].map((_, i) => {
					const seed = seededRandom(i + 910);
					const x = seed * 1920;
					const baseY = 1080 - ((frame * (0.5 + seed * 1) + seededRandom(i + 911) * 1080) % 1200);
					const size = 2 + seededRandom(i + 912) * 4;
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.03 + i) * 15,
								top: baseY,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: '#FFC107',
								opacity: 0.12 + Math.sin(frame * 0.04 + i) * 0.08,
							}}
						/>
					);
				})}

				{/* Main Content */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Question Badge 3 */}
					<div
						style={{
							position: 'absolute',
							left: '5%',
							top: '8%',
							opacity: sp(5),
							transform: `scale(${sp(5)})`,
						}}
					>
						<div
							style={{
								width: 80,
								height: 80,
								borderRadius: '50%',
								backgroundColor: '#FFC107',
								display: 'flex',
								alignItems: 'center',
								justifyContent: 'center',
								fontFamily: 'Inter',
								fontSize: 40,
								fontWeight: 900,
								color: '#1C1C1C',
								boxShadow: '0 0 30px rgba(255,193,7,0.3)',
							}}
						>
							3
						</div>
					</div>

					{/* Big question */}
					<div
						style={{
							position: 'absolute',
							left: '13%',
							top: '6%',
							maxWidth: 700,
							opacity: sp(8),
							transform: `translateX(${interpolate(sp(8), [0, 1], [50, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 72,
								color: '#F5F5F5',
								lineHeight: 1.25,
							}}
						>
							छोड़ दूँ तो{' '}
							<span style={{color: '#FFC107'}}>बदले में</span>{' '}
							क्या मिलेगा?
						</div>
					</div>

					{/* Benefits cards - staggered reveal */}
					{[
						{text: 'ज्यादा लचीलापन', emoji: '🔓', delay: 30},
						{text: 'कम तनाव', emoji: '😌', delay: 45},
						{text: 'सच्ची आजादी', emoji: '🕊️', delay: 60},
					].map((item, i) => (
						<div
							key={`benefit-${i}`}
							style={{
								position: 'absolute',
								right: '8%',
								top: `${30 + i * 18}%`,
								opacity: sp(item.delay),
								transform: `translateX(${interpolate(sp(item.delay), [0, 1], [80, 0])}px)`,
							}}
						>
							<div
								style={{
									display: 'flex',
									alignItems: 'center',
									gap: 20,
									backgroundColor: 'rgba(255,193,7,0.08)',
									border: '2px solid rgba(255,193,7,0.25)',
									borderRadius: 20,
									padding: '18px 35px',
								}}
							>
								{/* Checkmark */}
								<svg width="40" height="40" viewBox="0 0 24 24" fill="none">
									<circle cx="12" cy="12" r="10" fill="#FFC107" opacity="0.2" />
									<path d="M8 12l3 3 5-6" stroke="#FFC107" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
								</svg>
								<div
									style={{
										fontFamily: 'Yantra One',
										fontSize: 48,
										color: '#F5F5F5',
									}}
								>
									{item.text}
								</div>
							</div>
						</div>
					))}

					{/* Bottom punchline */}
					<div
						style={{
							position: 'absolute',
							bottom: '8%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(80), [0, 1], [25, 0])}px)`,
							opacity: sp(80),
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 56,
								color: '#FFC107',
								textAlign: 'center',
								textShadow: '0 0 40px rgba(255,193,7,0.2)',
							}}
						>
							छोड़ना नुकसान नहीं —{' '}
							<span style={{color: '#F5F5F5'}}>मौका लगने लगता है</span>
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - free/flying pose */}
				<div
					style={{
						position: 'absolute',
						left: '5%',
						bottom: '15%',
						opacity: sp(45),
						transform: `translateY(${Math.sin(frame * 0.05) * 3 - interpolate(frame, [0, 120], [0, 10], {extrapolateRight: 'clamp'})}px)`,
					}}
				>
					<svg width="100" height="150" viewBox="0 0 100 150">
						<circle cx="50" cy="30" r="35" fill="#FFC107" opacity="0.08" />
						<circle
							cx="50"
							cy="30"
							r="15"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 50 30)`}
						/>
						<circle cx="43" cy="28" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="57" cy="28" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="49" y1="28" x2="51" y2="28" stroke="#FFC107" strokeWidth="2" />
						{/* Big smile */}
						<path d="M42 35 Q50 43 58 35" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="50" y1="45" x2="50" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Arms up - victory/freedom */}
						<line x1="50" y1="58" x2="25" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="58" x2="75" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Legs apart - jumping */}
						<line x1="50" y1="90" x2="30" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="90" x2="70" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene9_TeesraSawaal;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene10_60to90Din',
	durationInSeconds: 12,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene10_60to90Din: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera pan left
	const camPanX = interpolate(frame, [0, durationInFrames], [-15, 15], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill style={{transform: `translateX(${camPanX}px)`}}>
				{/* Gradient - Lab/experiment feel */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(135deg, #1C1C2D 0%, #1C1C1C 45%, #2D2D1C 80%, #1C1C1C 100%)`,
					}}
				/>
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 50% 60%, rgba(233,215,0,0.06) 0%, transparent 40%)`,
					}}
				/>

				{/* Shape Layers - Calendar/test tube shapes */}
				{[...Array(5)].map((_, i) => {
					const seed = seededRandom(i + 920);
					const size = 80 + seed * 130;
					const x = seed * 1920;
					const y = seededRandom(i + 921) * 1080;
					const rot = frame * 0.05 * (i % 2 === 0 ? 1 : -1);
					const opacity = 0.04 + seededRandom(i + 922) * 0.03;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.01 + i) * 20,
								top: y + Math.cos(frame * 0.008 + i) * 15,
								width: size,
								height: size,
								transform: `rotate(${rot}deg)`,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								{/* Calendar grid */}
								<rect x="10" y="10" width="80" height="80" rx="5" fill="none" stroke="#e9d700" strokeWidth="1" />
								<line x1="10" y1="30" x2="90" y2="30" stroke="#e9d700" strokeWidth="0.5" />
								{[0, 1, 2].map((r) =>
									[0, 1, 2, 3].map((c) => (
										<rect
											key={`cell-${r}-${c}`}
											x={15 + c * 18}
											y={35 + r * 18}
											width="14"
											height="14"
											rx="2"
											fill="none"
											stroke="#e9d700"
											strokeWidth="0.3"
										/>
									))
								)}
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Countdown dots */}
				{[...Array(90)].map((_, i) => {
					const col = i % 15;
					const row = Math.floor(i / 15);
					const x = 300 + col * 90;
					const y = 200 + row * 90;
					const delay = i * 0.8;
					const filledProgress = interpolate(
						frame,
						[delay, delay + 10],
						[0, 1],
						{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
					);
					const isHighlight = i < 60 || (i >= 60 && i < 90);
					return (
						<div
							key={`countdown-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y,
								width: 8,
								height: 8,
								borderRadius: '50%',
								backgroundColor: i < 60 ? '#FFC107' : '#e9d700',
								opacity: filledProgress * 0.15,
							}}
						/>
					);
				})}

				{/* Particles */}
				{[...Array(20)].map((_, i) => {
					const seed = seededRandom(i + 930);
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: seed * 1920,
								top: seededRandom(i + 931) * 1080 + Math.sin(frame * 0.04 + i) * 15,
								width: 2 + seededRandom(i + 932) * 3,
								height: 2 + seededRandom(i + 932) * 3,
								borderRadius: '50%',
								backgroundColor: '#e9d700',
								opacity: 0.12,
							}}
						/>
					);
				})}

				{/* Main Content */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Big number badge */}
					<div
						style={{
							position: 'absolute',
							left: '5%',
							top: '10%',
							opacity: sp(5),
							transform: `scale(${sp(5)})`,
						}}
					>
						<div
							style={{
								fontFamily: 'JetBrains Mono',
								fontSize: 200,
								fontWeight: 900,
								color: '#FFC107',
								opacity: 0.15,
								lineHeight: 1,
							}}
						>
							60-90
						</div>
					</div>

					{/* Title */}
					<div
						style={{
							position: 'absolute',
							left: '8%',
							top: '15%',
							opacity: sp(10),
							transform: `translateY(${interpolate(sp(10), [0, 1], [-30, 0])}px)`,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 100,
								color: '#F5F5F5',
								lineHeight: 1.2,
							}}
						>
							<span style={{color: '#FFC107'}}>60-90</span> दिन
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 70,
								color: '#e9d700',
								lineHeight: 1.3,
								marginTop: 5,
							}}
						>
							का प्रयोग
						</div>
					</div>

					{/* Steps/Process - horizontal flow */}
					<div
						style={{
							position: 'absolute',
							bottom: '22%',
							left: '5%',
							right: '5%',
							display: 'flex',
							alignItems: 'center',
							justifyContent: 'space-between',
						}}
					>
						{[
							{step: '1', text: 'चीज़ चुनो', delay: 30},
							{step: '→', text: '', delay: 42},
							{step: '2', text: 'इस्तेमाल बंद करो', delay: 50},
							{step: '→', text: '', delay: 62},
							{step: '3', text: '60-90 दिन रुको', delay: 70},
							{step: '→', text: '', delay: 82},
							{step: '4', text: 'फैसला लो', delay: 90},
						].map((item, i) => (
							<div
								key={`step-${i}`}
								style={{
									opacity: sp(item.delay),
									transform: `scale(${sp(item.delay)})`,
									display: 'flex',
									flexDirection: 'column',
									alignItems: 'center',
								}}
							>
								{item.text ? (
									<>
										<div
											style={{
												width: 65,
												height: 65,
												borderRadius: '50%',
												backgroundColor: '#FFC107',
												display: 'flex',
												alignItems: 'center',
												justifyContent: 'center',
												fontFamily: 'Inter',
												fontSize: 32,
												fontWeight: 900,
												color: '#1C1C1C',
												marginBottom: 12,
											}}
										>
											{item.step}
										</div>
										<div
											style={{
												fontFamily: 'Noto Sans Devanagari',
												fontSize: 32,
												color: '#F5F5F5',
												fontWeight: 600,
												textAlign: 'center',
												maxWidth: 180,
											}}
										>
											{item.text}
										</div>
									</>
								) : (
									<div
										style={{
											fontFamily: 'Inter',
											fontSize: 40,
											color: '#FFC107',
											fontWeight: 700,
										}}
									>
										→
									</div>
								)}
							</div>
						))}
					</div>

					{/* Bottom text */}
					<div
						style={{
							position: 'absolute',
							bottom: '6%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(100), [0, 1], [20, 0])}px)`,
							opacity: sp(100),
						}}
					>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 42,
								color: '#9E9E9E',
								textAlign: 'center',
							}}
						>
							कमी खलती है या{' '}
							<span style={{color: '#FFC107', fontWeight: 700}}>
								राहत मिलती है?
							</span>
						</div>
					</div>
				</AbsoluteFill>

				{/* Stick Figure with clipboard/notepad */}
				<div
					style={{
						position: 'absolute',
						right: '4%',
						top: '12%',
						opacity: sp(45),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="100" height="150" viewBox="0 0 100 150">
						<circle cx="50" cy="28" r="30" fill="#FFC107" opacity="0.06" />
						<circle
							cx="50"
							cy="28"
							r="14"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 50 28)`}
						/>
						<circle cx="43" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="57" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="49" y1="26" x2="51" y2="26" stroke="#FFC107" strokeWidth="2" />
						<line x1="50" y1="42" x2="50" y2="90" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Holding clipboard */}
						<line x1="50" y1="58" x2="30" y2="70" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="58" x2="70" y2="65" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Clipboard */}
						<rect x="20" y="68" width="25" height="30" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="25" y1="76" x2="40" y2="76" stroke="#FFC107" strokeWidth="1" />
						<line x1="25" y1="82" x2="38" y2="82" stroke="#FFC107" strokeWidth="1" />
						<line x1="25" y1="88" x2="35" y2="88" stroke="#FFC107" strokeWidth="1" />
						<line x1="50" y1="90" x2="38" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="90" x2="62" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene10_60to90Din;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene11_AsliAmiri',
	durationInSeconds: 10,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene11_AsliAmiri: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Screen shake on quote reveal
	const shakeIntensity = interpolate(frame, [60, 70, 80], [0, 4, 0], {
		extrapolateLeft: 'clamp',
		extrapolateRight: 'clamp',
	});
	const shakeX = Math.sin(frame * 3) * shakeIntensity;

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill
				style={{
					transform: `translateX(${shakeX}px)`,
				}}
			>
				{/* Gradient - Premium gold/black */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(ellipse at 50% 50%, #2D2A1A 0%, #1C1C1C 50%, #0D0D0D 100%)`,
					}}
				/>
				{/* Golden light center */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 50% 45%, rgba(255,193,7,0.1) 0%, transparent 40%)`,
						opacity: 0.6 + Math.sin(frame * 0.025) * 0.3,
					}}
				/>

				{/* Shape Layers - Diamond shapes (wealth symbol) */}
				{[...Array(6)].map((_, i) => {
					const seed = seededRandom(i + 940);
					const size = 60 + seed * 100;
					const x = seed * 1920;
					const y = seededRandom(i + 941) * 1080;
					const rot = 45 + frame * 0.05 * (i % 2 === 0 ? 1 : -1);
					const opacity = 0.04 + seededRandom(i + 942) * 0.04;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.012 + i) * 20,
								top: y + Math.cos(frame * 0.009 + i) * 15,
								width: size,
								height: size,
								border: `1px solid rgba(255,193,7,${opacity})`,
								transform: `rotate(${rot}deg)`,
							}}
						/>
					);
				})}

				{/* Particles - Golden shimmer */}
				{[...Array(35)].map((_, i) => {
					const seed = seededRandom(i + 950);
					const x = seed * 1920;
					const y = seededRandom(i + 951) * 1080;
					const size = 2 + seededRandom(i + 952) * 4;
					const twinkle = Math.sin(frame * 0.08 + i * 2.5) > 0.3 ? 0.3 : 0.05;
					const floatY = Math.sin(frame * 0.04 + i) * 15;
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y + floatY,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: '#FFC107',
								opacity: twinkle,
							}}
						/>
					);
				})}

				{/* Main Content - Quote Card */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* ProBuffet HQ badge */}
					<div
						style={{
							position: 'absolute',
							top: '10%',
							left: '50%',
							transform: `translateX(-50%) scale(${sp(5)})`,
							opacity: sp(5),
						}}
					>
						<div
							style={{
								display: 'flex',
								alignItems: 'center',
								gap: 15,
								backgroundColor: 'rgba(255,193,7,0.1)',
								border: '2px solid #FFC107',
								borderRadius: 50,
								padding: '10px 35px',
							}}
						>
							<div
								style={{
									width: 40,
									height: 40,
									borderRadius: '50%',
									backgroundColor: '#FFC107',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									fontFamily: 'Inter',
									fontSize: 20,
									fontWeight: 900,
									color: '#1C1C1C',
								}}
							>
								PB
							</div>
							<div
								style={{
									fontFamily: 'Inter',
									fontSize: 28,
									fontWeight: 700,
									color: '#FFC107',
								}}
							>
								ProBuffet HQ
							</div>
						</div>
					</div>

					{/* Quote marks */}
					<div
						style={{
							position: 'absolute',
							top: '25%',
							left: '10%',
							fontFamily: 'Inter',
							fontSize: 250,
							color: '#FFC107',
							opacity: 0.08,
							lineHeight: 1,
						}}
					>
						"
					</div>

					{/* Quote text */}
					<div
						style={{
							position: 'absolute',
							top: '30%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(20), [0, 1], [40, 0])}px)`,
							opacity: sp(20),
							maxWidth: 1200,
							textAlign: 'center',
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 70,
								color: '#F5F5F5',
								lineHeight: 1.4,
							}}
						>
							असली अमीरी वो{' '}
							<span style={{color: '#9E9E9E', textDecoration: 'line-through'}}>
								नहीं
							</span>{' '}
							जो
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 70,
								color: '#9E9E9E',
								lineHeight: 1.4,
								opacity: sp(35),
							}}
						>
							बाहर से दिखती है
						</div>
					</div>

					{/* Second part of quote */}
					<div
						style={{
							position: 'absolute',
							top: '62%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(55), [0, 1], [30, 0])}px)`,
							opacity: sp(55),
							maxWidth: 1200,
							textAlign: 'center',
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 80,
								color: '#FFC107',
								lineHeight: 1.4,
								textShadow: '0 0 60px rgba(255,193,7,0.3)',
							}}
						>
							असली अमीरी वो है जो
						</div>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 80,
								color: '#FFC107',
								lineHeight: 1.4,
								opacity: sp(70),
							}}
						>
							भीतर से महसूस होती है
						</div>
					</div>

					{/* Closing quote */}
					<div
						style={{
							position: 'absolute',
							bottom: '12%',
							right: '10%',
							fontFamily: 'Inter',
							fontSize: 250,
							color: '#FFC107',
							opacity: 0.08,
							lineHeight: 1,
						}}
					>
						"
					</div>
				</AbsoluteFill>

				{/* Stick Figure - Buffett with heart glow */}
				<div
					style={{
						position: 'absolute',
						right: '5%',
						bottom: '5%',
						opacity: sp(40),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="90" height="140" viewBox="0 0 90 140">
						<circle cx="45" cy="25" r="35" fill="#FFC107" opacity="0.1" />
						<circle
							cx="45"
							cy="25"
							r="14"
							fill="none"
							stroke="#FFC107"
							strokeWidth="3"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 45 25)`}
						/>
						<circle cx="38" cy="23" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="52" cy="23" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="45" y1="23" x2="45" y2="23" stroke="#FFC107" strokeWidth="2" />
						{/* Content smile */}
						<path d="M38 30 Q45 37 52 30" fill="none" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="45" y1="39" x2="45" y2="85" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Hand on heart */}
						<line x1="45" y1="55" x2="35" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="55" x2="65" y2="70" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Heart glow on chest */}
						<circle
							cx="42"
							cy="62"
							r="6"
							fill="#ff0000"
							opacity={0.15 + Math.sin(frame * 0.08) * 0.1}
						/>
						<line x1="45" y1="85" x2="33" y2="120" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="85" x2="57" y2="120" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene11_AsliAmiri;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene12_WaqtSampatti',
	durationInSeconds: 13,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene12_WaqtSampatti: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera zoom in dramatic
	const camZ = interpolate(frame, [0, durationInFrames], [1, 1.12], {
		extrapolateRight: 'clamp',
	});

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill style={{transform: `scale(${camZ})`}}>
				{/* Gradient - Deep space/time feel */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(ellipse at 50% 50%, #1A1A2D 0%, #0D0D1A 40%, #0A0A0A 100%)`,
					}}
				/>
				{/* Pulsing glow */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 50% 50%, rgba(255,193,7,${0.05 + Math.sin(frame * 0.03) * 0.03}) 0%, transparent 35%)`,
					}}
				/>

				{/* Shape Layers - Clock hands spinning */}
				{[...Array(4)].map((_, i) => {
					const seed = seededRandom(i + 960);
					const size = 150 + seed * 200;
					const x = seed * 1600 + 100;
					const y = seededRandom(i + 961) * 800 + 100;
					const opacity = 0.03 + seededRandom(i + 962) * 0.03;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y,
								width: size,
								height: size,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								<circle cx="50" cy="50" r="45" fill="none" stroke="#FFC107" strokeWidth="0.8" />
								<circle cx="50" cy="50" r="35" fill="none" stroke="#FFC107" strokeWidth="0.5" />
								{/* Hour marks */}
								{[...Array(12)].map((_, h) => {
									const angle = (h * 30 * Math.PI) / 180;
									return (
										<line
											key={`mark-${h}`}
											x1={50 + Math.cos(angle) * 38}
											y1={50 + Math.sin(angle) * 38}
											x2={50 + Math.cos(angle) * 43}
											y2={50 + Math.sin(angle) * 43}
											stroke="#FFC107"
											strokeWidth="0.5"
										/>
									);
								})}
								{/* Moving hands */}
								<line
									x1="50"
									y1="50"
									x2={50 + Math.cos((frame * 0.1 + i) % 360 * Math.PI / 180) * 25}
									y2={50 + Math.sin((frame * 0.1 + i) % 360 * Math.PI / 180) * 25}
									stroke="#FFC107"
									strokeWidth="0.8"
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Sand falling (hourglass) */}
				{[...Array(60)].map((_, i) => {
					const seed = seededRandom(i + 970);
					const x = 860 + (seed - 0.5) * 200;
					const fallSpeed = 1 + seededRandom(i + 971) * 2;
					const y = ((seededRandom(i + 972) * 500 + frame * fallSpeed) % 600) + 240;
					const size = 1 + seededRandom(i + 973) * 2;

					return (
						<div
							key={`sand-${i}`}
							style={{
								position: 'absolute',
								left: x,
								top: y,
								width: size,
								height: size,
								borderRadius: '50%',
								backgroundColor: '#FFC107',
								opacity: y > 400 && y < 700 ? 0.2 : 0.05,
							}}
						/>
					);
				})}

				{/* Particles */}
				{[...Array(20)].map((_, i) => {
					const seed = seededRandom(i + 980);
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: seed * 1920,
								top: seededRandom(i + 981) * 1080 + Math.sin(frame * 0.03 + i) * 12,
								width: 2 + seededRandom(i + 982) * 3,
								height: 2 + seededRandom(i + 982) * 3,
								borderRadius: '50%',
								backgroundColor: '#F5F5F5',
								opacity: 0.08,
							}}
						/>
					);
				})}

				{/* Main Content */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Large hourglass center */}
					<div
						style={{
							position: 'absolute',
							left: '50%',
							top: '50%',
							transform: `translate(-50%, -50%) scale(${sp(10)})`,
							opacity: sp(10) * 0.15,
						}}
					>
						<svg width="400" height="500" viewBox="0 0 100 130">
							<path
								d="M20 5 L80 5 L80 15 Q80 50 50 65 Q20 50 20 15 Z"
								fill="none"
								stroke="#FFC107"
								strokeWidth="2"
							/>
							<path
								d="M20 125 L80 125 L80 115 Q80 80 50 65 Q20 80 20 115 Z"
								fill="none"
								stroke="#FFC107"
								strokeWidth="2"
							/>
						</svg>
					</div>

					{/* Text - Hero */}
					<div
						style={{
							position: 'absolute',
							top: '10%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(15), [0, 1], [-40, 0])}px)`,
							opacity: sp(15),
							textAlign: 'center',
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 60,
								color: '#9E9E9E',
								lineHeight: 1.3,
							}}
						>
							सबसे कीमती संपत्ती
						</div>
					</div>

					<div
						style={{
							position: 'absolute',
							top: '25%',
							left: '50%',
							transform: `translateX(-50%) scale(${sp(30)})`,
							opacity: sp(30),
							textAlign: 'center',
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 180,
								color: '#FFC107',
								lineHeight: 1,
								textShadow: '0 0 80px rgba(255,193,7,0.3)',
							}}
						>
							समय
						</div>
					</div>

					{/* Three facts about time */}
					{[
						{text: 'वापस नहीं आती', icon: '↩️', delay: 55},
						{text: 'उधार नहीं मिलती', icon: '🤝', delay: 70},
						{text: 'खरीदी नहीं जा सकती', icon: '💰', delay: 85},
					].map((item, i) => (
						<div
							key={`fact-${i}`}
							style={{
								position: 'absolute',
								bottom: `${30 - i * 10}%`,
								left: '50%',
								transform: `translateX(-50%) translateY(${interpolate(sp(item.delay), [0, 1], [20, 0])}px)`,
								opacity: sp(item.delay),
							}}
						>
							<div
								style={{
									display: 'flex',
									alignItems: 'center',
									gap: 18,
								}}
							>
								<svg width="30" height="30" viewBox="0 0 24 24" fill="none">
									<path d="M18 6L6 18M6 6l12 12" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
								</svg>
								<div
									style={{
										fontFamily: 'Noto Sans Devanagari',
										fontSize: 44,
										color: '#F5F5F5',
										fontWeight: 600,
									}}
								>
									{item.text}
								</div>
							</div>
						</div>
					))}
				</AbsoluteFill>

				{/* Stick Figure - looking at watch/time */}
				<div
					style={{
						position: 'absolute',
						left: '5%',
						bottom: '5%',
						opacity: sp(40),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="90" height="140" viewBox="0 0 90 140">
						<circle cx="45" cy="28" r="30" fill="#FFC107" opacity="0.06" />
						<circle
							cx="45"
							cy="28"
							r="14"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 45 28)`}
						/>
						<circle cx="38" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="52" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="45" y1="26" x2="45" y2="26" stroke="#FFC107" strokeWidth="2" />
						<line x1="45" y1="42" x2="45" y2="88" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Looking at wrist watch */}
						<line x1="45" y1="60" x2="25" y2="52" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="25" y1="52" x2="28" y2="42" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Watch on wrist */}
						<circle cx="28" cy="42" r="6" fill="none" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="45" y1="60" x2="65" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="88" x2="33" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="45" y1="88" x2="57" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 30%, rgba(0,0,0,0.7) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene12_WaqtSampatti;
```

```tsx
import React from 'react';
import {
	useCurrentFrame,
	useVideoConfig,
	interpolate,
	spring,
	AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
	id: 'Chapter7Scene13_KasautiParakh',
	durationInSeconds: 15,
	fps: 30,
	width: 1920,
	height: 1080,
};

const seededRandom = (seed: number): number => {
	const x = Math.sin(seed * 9999) * 10000;
	return x - Math.floor(x);
};

const Chapter7Scene13_KasautiParakh: React.FC = () => {
	const frame = useCurrentFrame();
	const {fps, durationInFrames} = useVideoConfig();

	const sp = (delay: number, config?: object) =>
		spring({
			frame: Math.max(0, frame - delay),
			fps,
			config: {damping: 12, mass: 0.4, stiffness: 120, ...config},
		});

	const exitOp = interpolate(
		frame,
		[durationInFrames - 15, durationInFrames],
		[1, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);

	// Camera orbit + zoom
	const camRotY = interpolate(frame, [0, durationInFrames], [-2, 2], {
		extrapolateRight: 'clamp',
	});
	const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], {
		extrapolateRight: 'clamp',
	});

	// Impact shake at "दुश्मन"
	const impactFrame = 300; // ~10sec mark
	const shakeAmt = interpolate(
		frame,
		[impactFrame, impactFrame + 5, impactFrame + 15],
		[0, 6, 0],
		{extrapolateLeft: 'clamp', extrapolateRight: 'clamp'}
	);
	const shakeX = Math.sin(frame * 3.5) * shakeAmt;
	const shakeY = Math.cos(frame * 4.2) * shakeAmt * 0.6;

	return (
		<AbsoluteFill
			style={{
				backgroundColor: '#1C1C1C',
				overflow: 'hidden',
				opacity: exitOp,
			}}
		>
			<AbsoluteFill
				style={{
					transform: `perspective(1200px) rotateY(${camRotY}deg) scale(${camZoom}) translate(${shakeX}px, ${shakeY}px)`,
				}}
			>
				{/* Gradient - Intense dark with red warning */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `linear-gradient(160deg, #1C1C1C 0%, #2D1A1A 35%, #1C1C1C 55%, #1A1A2D 80%, #0D0D0D 100%)`,
					}}
				/>
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background: `radial-gradient(circle at 50% 50%, rgba(255,0,0,0.04) 0%, transparent 40%)`,
						opacity: 0.5 + Math.sin(frame * 0.03) * 0.3,
					}}
				/>

				{/* Shape Layers - Warning triangles */}
				{[...Array(5)].map((_, i) => {
					const seed = seededRandom(i + 990);
					const size = 100 + seed * 150;
					const x = seed * 1800;
					const y = seededRandom(i + 991) * 900;
					const rot = frame * 0.04 * (i % 2 === 0 ? 1 : -1);
					const opacity = 0.03 + seededRandom(i + 992) * 0.04;

					return (
						<div
							key={`shape-${i}`}
							style={{
								position: 'absolute',
								left: x + Math.sin(frame * 0.01 + i) * 20,
								top: y + Math.cos(frame * 0.008 + i) * 15,
								width: size,
								height: size,
								transform: `rotate(${rot}deg)`,
								opacity,
							}}
						>
							<svg viewBox="0 0 100 100" width="100%" height="100%">
								<polygon
									points="50,10 90,85 10,85"
									fill="none"
									stroke={i % 2 === 0 ? '#ff0000' : '#FFC107'}
									strokeWidth="1.5"
								/>
							</svg>
						</div>
					);
				})}

				{/* Motion Texture - Pulsing radar rings */}
				{[...Array(5)].map((_, i) => {
					const radius = ((frame * 2 + i * 80) % 800);
					const opacity = interpolate(radius, [0, 400, 800], [0.06, 0.02, 0], {
						extrapolateRight: 'clamp',
					});
					return (
						<div
							key={`radar-${i}`}
							style={{
								position: 'absolute',
								left: '50%',
								top: '50%',
								width: radius * 2,
								height: radius * 2,
								marginLeft: -radius,
								marginTop: -radius,
								borderRadius: '50%',
								border: `1px solid rgba(255,0,0,${opacity})`,
							}}
						/>
					);
				})}

				{/* Particles */}
				{[...Array(25)].map((_, i) => {
					const seed = seededRandom(i + 1000);
					return (
						<div
							key={`p-${i}`}
							style={{
								position: 'absolute',
								left: seed * 1920,
								top:
									seededRandom(i + 1001) * 1080 +
									Math.sin(frame * 0.04 + i) * 15,
								width: 2 + seededRandom(i + 1002) * 3,
								height: 2 + seededRandom(i + 1002) * 3,
								borderRadius: '50%',
								backgroundColor: i % 4 === 0 ? '#ff0000' : '#F5F5F5',
								opacity: 0.1,
							}}
						/>
					);
				})}

				{/* Main Content */}
				<AbsoluteFill
					style={{
						display: 'flex',
						alignItems: 'center',
						justifyContent: 'center',
					}}
				>
					{/* Title - "कसौटी" */}
					<div
						style={{
							position: 'absolute',
							top: '6%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(5), [0, 1], [-40, 0])}px)`,
							opacity: sp(5),
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 90,
								color: '#FFC107',
								textAlign: 'center',
								textShadow: '0 0 40px rgba(255,193,7,0.2)',
							}}
						>
							हर फैसले की कसौटी
						</div>
					</div>

					{/* Two question cards - side by side */}
					<div
						style={{
							position: 'absolute',
							top: '28%',
							left: '5%',
							width: '42%',
							opacity: sp(20),
							transform: `translateX(${interpolate(sp(20), [0, 1], [-60, 0])}px)`,
						}}
					>
						<div
							style={{
								backgroundColor: 'rgba(255,193,7,0.06)',
								border: '2px solid rgba(255,193,7,0.3)',
								borderRadius: 25,
								padding: '30px 35px',
							}}
						>
							<div
								style={{
									fontFamily: 'Noto Sans Devanagari',
									fontSize: 42,
									color: '#FFC107',
									fontWeight: 700,
									marginBottom: 15,
								}}
							>
								✅ समय बचा रहा है?
							</div>
							<div
								style={{
									fontFamily: 'Noto Sans Devanagari',
									fontSize: 38,
									color: '#F5F5F5',
									lineHeight: 1.6,
									opacity: sp(35),
								}}
							>
								ऊर्जा लौटा रहा है?
							</div>
						</div>
					</div>

					<div
						style={{
							position: 'absolute',
							top: '28%',
							right: '5%',
							width: '42%',
							opacity: sp(45),
							transform: `translateX(${interpolate(sp(45), [0, 1], [60, 0])}px)`,
						}}
					>
						<div
							style={{
								backgroundColor: 'rgba(255,0,0,0.06)',
								border: '2px solid rgba(255,0,0,0.3)',
								borderRadius: 25,
								padding: '30px 35px',
							}}
						>
							<div
								style={{
									fontFamily: 'Noto Sans Devanagari',
									fontSize: 42,
									color: '#ff0000',
									fontWeight: 700,
									marginBottom: 15,
								}}
							>
								❌ समय खा रहा है?
							</div>
							<div
								style={{
									fontFamily: 'Noto Sans Devanagari',
									fontSize: 38,
									color: '#F5F5F5',
									lineHeight: 1.6,
									opacity: sp(60),
								}}
							>
								ऊर्जा चूस रहा है?
							</div>
						</div>
					</div>

					{/* VS divider */}
					<div
						style={{
							position: 'absolute',
							top: '35%',
							left: '50%',
							transform: `translateX(-50%) scale(${sp(30)})`,
							opacity: sp(30),
						}}
					>
						<div
							style={{
								fontFamily: 'Inter',
								fontSize: 36,
								fontWeight: 900,
								color: '#9E9E9E',
								backgroundColor: '#2D2D2D',
								borderRadius: '50%',
								width: 60,
								height: 60,
								display: 'flex',
								alignItems: 'center',
								justifyContent: 'center',
							}}
						>
							VS
						</div>
					</div>

					{/* Bottom punchline - "दुश्मन" reveal */}
					<div
						style={{
							position: 'absolute',
							bottom: '12%',
							left: '50%',
							transform: `translateX(-50%) translateY(${interpolate(sp(80), [0, 1], [30, 0])}px)`,
							opacity: sp(80),
							maxWidth: 1200,
						}}
					>
						<div
							style={{
								fontFamily: 'Yantra One',
								fontSize: 58,
								color: '#F5F5F5',
								textAlign: 'center',
								lineHeight: 1.4,
							}}
						>
							बिना सार्थक दिये —{' '}
							<span
								style={{
									color: '#ff0000',
									textShadow: '0 0 30px rgba(255,0,0,0.4)',
									fontSize: 68,
								}}
							>
								वो दुश्मन है
							</span>
						</div>
						<div
							style={{
								fontFamily: 'Noto Sans Devanagari',
								fontSize: 40,
								color: '#9E9E9E',
								textAlign: 'center',
								marginTop: 10,
								opacity: sp(95),
							}}
						>
							चाहे कितनी भी कीमती दिखे
						</div>
					</div>

					{/* Shield/Sword icon */}
					<div
						style={{
							position: 'absolute',
							bottom: '35%',
							left: '50%',
							transform: `translateX(-50%) scale(${sp(70)})`,
							opacity: sp(70),
						}}
					>
						<svg width="100" height="100" viewBox="0 0 24 24" fill="none">
							<path
								d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"
								stroke="#FFC107"
								strokeWidth="2"
								fill="none"
							/>
							<path
								d="M9 12l2 2 4-4"
								stroke="#FFC107"
								strokeWidth="2"
								strokeLinecap="round"
								strokeLinejoin="round"
							/>
						</svg>
					</div>
				</AbsoluteFill>

				{/* Stick Figure - warrior/guard pose */}
				<div
					style={{
						position: 'absolute',
						right: '3%',
						bottom: '5%',
						opacity: sp(50),
						transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
					}}
				>
					<svg width="100" height="155" viewBox="0 0 100 155">
						<circle cx="50" cy="28" r="32" fill="#FFC107" opacity="0.07" />
						<circle
							cx="50"
							cy="28"
							r="15"
							fill="none"
							stroke="#FFC107"
							strokeWidth="2.5"
							transform={`rotate(${Math.sin(frame * 0.04) * 2} 50 28)`}
						/>
						<circle cx="43" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<circle cx="57" cy="26" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2" />
						<line x1="49" y1="26" x2="51" y2="26" stroke="#FFC107" strokeWidth="2" />
						{/* Determined expression */}
						<line x1="40" y1="34" x2="50" y2="33" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="50" y1="33" x2="60" y2="34" stroke="#FFC107" strokeWidth="1.5" />
						<line x1="50" y1="43" x2="50" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Arms crossed - guarding */}
						<line x1="50" y1="60" x2="30" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="30" y1="72" x2="55" y2="70" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="60" x2="70" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="70" y1="72" x2="45" y2="70" stroke="#F5F5F5" strokeWidth="2.5" />
						{/* Strong stance legs */}
						<line x1="50" y1="95" x2="30" y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="50" y1="95" x2="70" y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="30" y1="135" x2="20" y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
						<line x1="70" y1="135" x2="80" y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
					</svg>
				</div>

				{/* Vignette */}
				<div
					style={{
						position: 'absolute',
						width: '100%',
						height: '100%',
						background:
							'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
						pointerEvents: 'none',
					}}
				/>

				{/* Watermark */}
				<div
					style={{
						position: 'absolute',
						bottom: 30,
						right: 45,
						fontFamily: 'Inter',
						fontSize: 22,
						fontWeight: 600,
						color: '#9E9E9E',
						opacity: 0.3,
					}}
				>
					ProBuffet HQ
				</div>
			</AbsoluteFill>
		</AbsoluteFill>
	);
};

export default Chapter7Scene13_KasautiParakh;
```

