```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S1_KonkretWerden',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S1_KonkretWerden: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Grid reveal
  const gridOp = interpolate(frame, [0, 25], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Step items stagger
  const step1Op = sp(20);
  const step2Op = sp(35);
  const step3Op = sp(50);
  const step4Op = sp(65);

  // Headline
  const headlineY = interpolate(sp(8), [0, 1], [60, 0]);
  const headlineOp = interpolate(sp(8), [0, 1], [0, 1]);

  // Screen flash on beat
  const flashOp = interpolate(frame, [5, 12], [0.3, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particle positions
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 7 + 1) * 1920,
    y: seededRandom(i * 7 + 2) * 1080,
    r: seededRandom(i * 7 + 3) * 4 + 2,
    speed: seededRandom(i * 7 + 4) * 0.4 + 0.2,
    phase: seededRandom(i * 7 + 5) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${camZoom})`,
        transformOrigin: 'center center',
      }}
    >
      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 20% 50%, #2a2200 0%, #1C1C1C 55%), radial-gradient(ellipse at 85% 20%, #1a1a00 0%, transparent 60%)',
        }}
      />

      {/* === GRID TEXTURE === */}
      <AbsoluteFill style={{ opacity: gridOp * 0.07 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 22 }, (_, i) => (
            <line
              key={`v${i}`}
              x1={i * 90}
              y1={0}
              x2={i * 90}
              y2={1080}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
          {Array.from({ length: 13 }, (_, i) => (
            <line
              key={`h${i}`}
              x1={0}
              y1={i * 90}
              x2={1920}
              y2={i * 90}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {/* Large rotating circle */}
          <circle
            cx={1650}
            cy={200}
            r={320}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1.5"
            opacity={0.05}
            transform={`rotate(${frame * 0.15}, 1650, 200)`}
          />
          {/* Rectangle drifting */}
          <rect
            x={-80 + frame * 0.3}
            y={700}
            width={260}
            height={260}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity={0.04}
            rx={4}
            transform={`rotate(${frame * 0.2}, ${-80 + frame * 0.3 + 130}, 830)`}
          />
          {/* Triangle top right */}
          <polygon
            points={`1820,${40 + Math.sin(frame * 0.03) * 12} 1700,240 1940,240`}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.04}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 18}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 14}
              r={p.r}
              fill="#e9d700"
              opacity={0.06 + Math.sin(frame * 0.05 + p.phase) * 0.03}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === FLASH LAYER === */}
      <AbsoluteFill
        style={{
          backgroundColor: '#e9d700',
          opacity: flashOp,
          pointerEvents: 'none',
        }}
      />

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          paddingLeft: 140,
          paddingRight: 140,
        }}
      >
        {/* Tag */}
        <div
          style={{
            opacity: headlineOp,
            transform: `translateY(${headlineY}px)`,
            marginBottom: 20,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 12,
              backgroundColor: '#e9d700',
              padding: '8px 24px',
              borderRadius: 4,
            }}
          >
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
              <path
                d="M9 11l3 3L22 4"
                stroke="#1C1C1C"
                strokeWidth="2.5"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
              <path
                d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"
                stroke="#1C1C1C"
                strokeWidth="2.5"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            </svg>
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 26,
                fontWeight: 700,
                color: '#1C1C1C',
                letterSpacing: 2,
                textTransform: 'uppercase',
              }}
            >
              SCHRITT FÜR SCHRITT
            </span>
          </div>
        </div>

        {/* Hero Headline */}
        <div
          style={{
            opacity: headlineOp,
            transform: `translateY(${headlineY * 0.6}px)`,
            marginBottom: 40,
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 120,
              color: '#F5F5F5',
              lineHeight: 1.0,
              letterSpacing: 2,
            }}
          >
            DU HAST
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 140,
              color: '#e9d700',
              lineHeight: 1.0,
              letterSpacing: 2,
            }}
          >
            20.000 €
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 100,
              color: '#F5F5F5',
              lineHeight: 1.0,
              letterSpacing: 2,
              opacity: 0.7,
            }}
          >
            WAS JETZT?
          </div>
        </div>

        {/* Step indicators */}
        <div
          style={{
            display: 'flex',
            gap: 28,
            marginTop: 10,
          }}
        >
          {[
            { num: '01', label: 'FREISTELLUNGSAUFTRAG', color: '#e9d700', op: step1Op },
            { num: '02', label: 'DEPOT STRUKTUR', color: '#F5F5F5', op: step2Op },
            { num: '03', label: 'STEUERERKLÄRUNG', color: '#F5F5F5', op: step3Op },
            { num: '04', label: 'SELBSTINVESTITION', color: '#F5F5F5', op: step4Op },
          ].map((s, i) => (
            <div
              key={i}
              style={{
                opacity: s.op,
                transform: `translateY(${interpolate(s.op, [0, 1], [30, 0])}px)`,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'flex-start',
                backgroundColor:
                  i === 0 ? 'rgba(233,215,0,0.12)' : 'rgba(245,245,245,0.05)',
                border: `1px solid ${i === 0 ? '#e9d700' : 'rgba(245,245,245,0.12)'}`,
                borderRadius: 8,
                padding: '16px 22px',
                minWidth: 200,
              }}
            >
              <span
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 36,
                  fontWeight: 700,
                  color: s.color,
                  lineHeight: 1,
                }}
              >
                {s.num}
              </span>
              <span
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 20,
                  fontWeight: 600,
                  color: '#9E9E9E',
                  marginTop: 6,
                  letterSpacing: 1,
                }}
              >
                {s.label}
              </span>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="1920"
          height="1080"
          style={{
            position: 'absolute',
            bottom: 0,
            right: 0,
          }}
        >
          <g
            transform={`translate(1680, ${720 + Math.sin(frame * 0.05) * 2})
              scale(${0.85 + sp(45) * 0.15})`}
            opacity={sp(45)}
          >
            {/* Golden glow */}
            <ellipse cx="60" cy="280" rx="55" ry="55" fill="#e9d700" opacity="0.08" />
            {/* Head */}
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            {/* Glasses */}
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            {/* Body */}
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Arms — pointing at steps */}
            <line
              x1="60"
              y1="120"
              x2={20 + Math.sin(frame * 0.06) * 5}
              y2={150 + Math.cos(frame * 0.06) * 3}
              stroke="#e9d700"
              strokeWidth="4"
              strokeLinecap="round"
            />
            <line
              x1="60"
              y1="120"
              x2={100 + Math.sin(frame * 0.06 + 1) * 5}
              y2={145 + Math.cos(frame * 0.06) * 3}
              stroke="#F5F5F5"
              strokeWidth="4"
              strokeLinecap="round"
            />
            {/* Legs */}
            <line x1="60" y1="200" x2="30" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="90" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.7) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S1_KonkretWerden;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S2_Freistellungsauftrag',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S2_Freistellungsauftrag: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Horizontal pan camera
  const camX = interpolate(frame, [0, durationInFrames], [0, -30], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Document stamp animation
  const stampScale = sp(40, { damping: 8, stiffness: 200, mass: 0.3 });
  const stampRotate = interpolate(sp(40), [0, 1], [25, -4]);

  // Text reveals
  const wordOp1 = interpolate(frame, [15, 28], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const wordOp2 = interpolate(frame, [28, 41], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const wordOp3 = interpolate(frame, [41, 54], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 11 + 1) * 1920,
    y: seededRandom(i * 11 + 2) * 1080,
    r: seededRandom(i * 11 + 3) * 3 + 1.5,
    speed: seededRandom(i * 11 + 4) * 0.35 + 0.15,
    phase: seededRandom(i * 11 + 5) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translateX(${camX}px)`,
      }}
    >
      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(135deg, #0d1a00 0%, #1C1C1C 40%, #1a1500 100%)',
        }}
      />

      {/* === DIAGONAL STRIPE TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 40 }, (_, i) => (
            <line
              key={i}
              x1={i * 55 - 200}
              y1={0}
              x2={i * 55 + 200}
              y2={1080}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {/* Big document shape */}
          <rect
            x={900}
            y={100}
            width={380}
            height={500}
            rx={12}
            fill="rgba(233,215,0,0.03)"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.12}
            transform={`rotate(${Math.sin(frame * 0.02) * 2}, 1090, 350)`}
          />
          {/* Small circle cluster */}
          <circle cx={150} cy={900} r={180} fill="none" stroke="#F5F5F5" strokeWidth="1" opacity={0.03} />
          <circle cx={150} cy={900} r={120} fill="none" stroke="#F5F5F5" strokeWidth="1" opacity={0.04} />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 15}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 12}
              r={p.r}
              fill="#e9d700"
              opacity={0.05 + Math.sin(frame * 0.04 + p.phase) * 0.02}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          paddingLeft: 140,
          paddingRight: 800,
        }}
      >
        <div>
          {/* Step badge */}
          <div
            style={{
              opacity: wordOp1,
              display: 'flex',
              alignItems: 'center',
              gap: 16,
              marginBottom: 24,
            }}
          >
            <div
              style={{
                width: 56,
                height: 56,
                borderRadius: '50%',
                backgroundColor: '#e9d700',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}
            >
              <span
                style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 28,
                  color: '#1C1C1C',
                }}
              >
                1
              </span>
            </div>
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 28,
                fontWeight: 700,
                color: '#9E9E9E',
                letterSpacing: 3,
                textTransform: 'uppercase',
              }}
            >
              ERSTENS
            </span>
          </div>

          {/* Main word */}
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 110,
              color: '#e9d700',
              lineHeight: 0.95,
              letterSpacing: 1,
              opacity: wordOp1,
              transform: `translateX(${interpolate(wordOp1, [0, 1], [-40, 0])}px)`,
              marginBottom: 8,
            }}
          >
            FREI-
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 110,
              color: '#e9d700',
              lineHeight: 0.95,
              letterSpacing: 1,
              opacity: wordOp2,
              transform: `translateX(${interpolate(wordOp2, [0, 1], [-40, 0])}px)`,
              marginBottom: 8,
            }}
          >
            STELLUNGS-
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 110,
              color: '#F5F5F5',
              lineHeight: 0.95,
              letterSpacing: 1,
              opacity: wordOp3,
              transform: `translateX(${interpolate(wordOp3, [0, 1], [-40, 0])}px)`,
              marginBottom: 32,
            }}
          >
            AUFTRAG
          </div>

          {/* Subtext */}
          <div
            style={{
              opacity: interpolate(frame, [60, 72], [0, 1], {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              }),
              display: 'flex',
              alignItems: 'center',
              gap: 14,
            }}
          >
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#e9d700" strokeWidth="2" />
              <path d="M12 8v4l3 3" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" />
            </svg>
            <span
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 36,
                color: '#F5F5F5',
                fontWeight: 400,
              }}
            >
              Klingt bürokratisch —{' '}
              <strong style={{ color: '#e9d700' }}>ist aber einfach & wirksam</strong>
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* === DOCUMENT STAMP === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            right: 160,
            top: '50%',
            transform: `translateY(-50%) scale(${stampScale}) rotate(${stampRotate}deg)`,
            transformOrigin: 'center center',
            opacity: stampScale,
          }}
        >
          <svg width="340" height="420" viewBox="0 0 340 420">
            {/* Document background */}
            <rect x="0" y="0" width="340" height="420" rx="16" fill="#2D2D2D" />
            <rect x="0" y="0" width="340" height="420" rx="16" fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.4" />
            {/* Document lines */}
            {[80, 120, 155, 190, 225, 260, 295].map((y, i) => (
              <rect
                key={i}
                x={30}
                y={y}
                width={i % 3 === 0 ? 240 : i % 3 === 1 ? 180 : 210}
                height={10}
                rx={5}
                fill="#9E9E9E"
                opacity={0.3}
              />
            ))}
            {/* Header */}
            <rect x="30" y="30" width="280" height="28" rx="4" fill="#e9d700" opacity="0.2" />
            <text x="50" y="50" fontFamily="Inter, sans-serif" fontSize="16" fill="#e9d700" fontWeight="700">
              FREISTELLUNGSAUFTRAG
            </text>
            {/* APPROVED stamp */}
            <g transform="translate(170, 210) rotate(-15)">
              <rect x="-80" y="-30" width="160" height="60" rx="8" fill="none" stroke="#e9d700" strokeWidth="3" />
              <text
                x="0"
                y="10"
                textAnchor="middle"
                fontFamily="Anton, sans-serif"
                fontSize="26"
                fill="#e9d700"
                opacity="0.8"
              >
                ABGEGEBEN
              </text>
            </g>
          </svg>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(760, ${680 + Math.sin(frame * 0.05) * 2}) scale(${sp(50) * 0.9})`}
            opacity={sp(50)}
          >
            <ellipse cx="60" cy="280" rx="50" ry="50" fill="#e9d700" opacity="0.07" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Pointing at document */}
            <line
              x1="60"
              y1="120"
              x2={120 + Math.sin(frame * 0.05) * 4}
              y2={100 + Math.cos(frame * 0.05) * 3}
              stroke="#e9d700"
              strokeWidth="4"
              strokeLinecap="round"
            />
            <line x1="60" y1="120" x2="30" y2="165" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.75) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S2_Freistellungsauftrag;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S3_Sparerpauschbetrag',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S3_Sparerpauschbetrag: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Scale zoom in slowly
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Number count-up for 1000
  const countUp1000 = Math.round(
    interpolate(frame, [20, 55], [0, 1000], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );
  const countUp2000 = Math.round(
    interpolate(frame, [50, 85], [0, 2000], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 13 + 1) * 1920,
    y: seededRandom(i * 13 + 2) * 1080,
    r: seededRandom(i * 13 + 3) * 3 + 1,
    speed: seededRandom(i * 13 + 4) * 0.3 + 0.2,
    phase: seededRandom(i * 13 + 5) * Math.PI * 2,
  }));

  const card1Op = sp(18);
  const card2Op = sp(50);
  const taglineOp = sp(80);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${camScale})`,
        transformOrigin: '40% 50%',
      }}
    >
      {/* === GRADIENT BACKGROUND — Dark Green Tax tone === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 70% 30%, #001a08 0%, #1C1C1C 60%), radial-gradient(ellipse at 15% 80%, #1a1500 0%, transparent 55%)',
        }}
      />

      {/* === CONCENTRIC CIRCLES TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {[60, 120, 180, 240, 300, 360].map((r, i) => (
            <circle
              key={i}
              cx={960}
              cy={540}
              r={r + Math.sin(frame * 0.02 + i) * 8}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <rect
            x={1500}
            y={600}
            width={300}
            height={300}
            rx={8}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.1 + 20}, 1650, 750)`}
          />
          <polygon
            points={`200,${100 + Math.sin(frame * 0.025) * 10} 80,300 320,300`}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.04}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 16}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 12}
              r={p.r}
              fill="#e9d700"
              opacity={0.055}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 0,
        }}
      >
        {/* Label */}
        <div
          style={{
            opacity: card1Op,
            transform: `translateY(${interpolate(card1Op, [0, 1], [-30, 0])}px)`,
            marginBottom: 28,
            textAlign: 'center',
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#9E9E9E',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            🇩🇪 SPARERPAUSCHBETRAG — STEUERFREI
          </span>
        </div>

        {/* Two cards side by side */}
        <div
          style={{
            display: 'flex',
            gap: 40,
            alignItems: 'stretch',
          }}
        >
          {/* Single card */}
          <div
            style={{
              opacity: card1Op,
              transform: `translateY(${interpolate(card1Op, [0, 1], [50, 0])}px) scale(${card1Op})`,
              backgroundColor: 'rgba(233,215,0,0.08)',
              border: '2px solid #e9d700',
              borderRadius: 20,
              padding: '48px 64px',
              textAlign: 'center',
              minWidth: 380,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 16 }}>
              <path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" />
              <circle cx="12" cy="7" r="4" stroke="#e9d700" strokeWidth="2" />
            </svg>
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 92,
                fontWeight: 700,
                color: '#e9d700',
                lineHeight: 1,
              }}
            >
              {countUp1000.toLocaleString('de-DE')}
            </div>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 38,
                color: '#e9d700',
                marginBottom: 12,
              }}
            >
              €
            </div>
            <div
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 26,
                color: '#F5F5F5',
                fontWeight: 600,
              }}
            >
              SINGLE
            </div>
          </div>

          {/* Married card */}
          <div
            style={{
              opacity: card2Op,
              transform: `translateY(${interpolate(card2Op, [0, 1], [50, 0])}px) scale(${card2Op})`,
              backgroundColor: 'rgba(245,245,245,0.04)',
              border: '2px solid rgba(245,245,245,0.2)',
              borderRadius: 20,
              padding: '48px 64px',
              textAlign: 'center',
              minWidth: 380,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 16 }}>
              <path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
              <circle cx="9" cy="7" r="4" stroke="#F5F5F5" strokeWidth="2" />
              <path d="M23 21v-2a4 4 0 00-3-3.87" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
              <path d="M16 3.13a4 4 0 010 7.75" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 92,
                fontWeight: 700,
                color: '#F5F5F5',
                lineHeight: 1,
              }}
            >
              {countUp2000.toLocaleString('de-DE')}
            </div>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 38,
                color: '#F5F5F5',
                marginBottom: 12,
              }}
            >
              €
            </div>
            <div
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 26,
                color: '#F5F5F5',
                fontWeight: 600,
              }}
            >
              VERHEIRATET
            </div>
          </div>
        </div>

        {/* STEUERFREI banner */}
        <div
          style={{
            opacity: taglineOp,
            transform: `translateY(${interpolate(taglineOp, [0, 1], [30, 0])}px)`,
            marginTop: 36,
            backgroundColor: '#e9d700',
            padding: '14px 52px',
            borderRadius: 8,
          }}
        >
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 44,
              color: '#1C1C1C',
              letterSpacing: 4,
            }}
          >
            KEIN CENT ANS FINANZAMT
          </span>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(1700, ${700 + Math.sin(frame * 0.05) * 2}) scale(${0.8 + sp(60) * 0.2})`}
            opacity={sp(60)}
          >
            <ellipse cx="60" cy="280" rx="50" ry="50" fill="#e9d700" opacity="0.08" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Thumbs up arms */}
            <line x1="60" y1="120" x2="20" y2="100" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            <circle cx="20" cy="95" r="8" fill="#e9d700" opacity="0.9" />
            <line x1="60" y1="120" x2="100" y2="140" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.8) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S3_Sparerpauschbetrag;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S4_FünfMinuten',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S4_FünfMinuten: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Countdown timer animation
  const timerProgress = interpolate(frame, [20, 80], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // 25% red warning
  const warningOp = sp(72);
  const warningShake = frame > 72
    ? Math.sin(frame * 0.8) * interpolate(frame, [72, 85], [8, 2], {
        extrapolateLeft: 'clamp',
        extrapolateRight: 'clamp',
      })
    : 0;

  // Bank logos stagger
  const bank1 = sp(30);
  const bank2 = sp(42);
  const bank3 = sp(54);

  // Particles
  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 17 + 1) * 1920,
    y: seededRandom(i * 17 + 2) * 1080,
    r: seededRandom(i * 17 + 3) * 3 + 1,
    speed: seededRandom(i * 17 + 4) * 0.3 + 0.15,
    phase: seededRandom(i * 17 + 5) * Math.PI * 2,
  }));

  // Timer circle
  const circumference = 2 * Math.PI * 80;
  const strokeDashoffset = circumference * (1 - timerProgress);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === GRADIENT === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(160deg, #1C1C1C 0%, #2a1500 50%, #1C1C1C 100%)',
        }}
      />

      {/* === DOT GRID TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 28 }, (_, row) =>
            Array.from({ length: 50 }, (_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 40 + 20}
                cy={row * 40 + 20}
                r={1.5}
                fill="#e9d700"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <circle
            cx={1700}
            cy={800}
            r={250}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.05}
            transform={`rotate(${frame * 0.12}, 1700, 800)`}
          />
          <rect
            x={60}
            y={60}
            width={200}
            height={200}
            rx={8}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.15 + 10}, 160, 160)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
          paddingLeft: 120,
          paddingRight: 120,
        }}
      >
        {/* LEFT — Timer + message */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'flex-start',
            gap: 32,
            maxWidth: 680,
          }}
        >
          {/* Timer circle */}
          <div
            style={{
              opacity: sp(10),
              display: 'flex',
              alignItems: 'center',
              gap: 32,
            }}
          >
            <svg width="200" height="200" viewBox="0 0 200 200">
              <circle cx="100" cy="100" r="80" fill="none" stroke="#2D2D2D" strokeWidth="10" />
              <circle
                cx="100"
                cy="100"
                r="80"
                fill="none"
                stroke="#e9d700"
                strokeWidth="10"
                strokeLinecap="round"
                strokeDasharray={circumference}
                strokeDashoffset={strokeDashoffset}
                transform="rotate(-90, 100, 100)"
              />
              <text
                x="100"
                y="90"
                textAnchor="middle"
                fontFamily="Anton, sans-serif"
                fontSize="52"
                fill="#F5F5F5"
              >
                5
              </text>
              <text
                x="100"
                y="128"
                textAnchor="middle"
                fontFamily="Inter, sans-serif"
                fontSize="22"
                fill="#9E9E9E"
                fontWeight="600"
              >
                MINUTEN
              </text>
            </svg>
            <div>
              <div
                style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 96,
                  color: '#e9d700',
                  lineHeight: 1,
                }}
              >
                DAS IST
              </div>
              <div
                style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 96,
                  color: '#F5F5F5',
                  lineHeight: 1,
                }}
              >
                ALLES.
              </div>
            </div>
          </div>

          {/* Banks */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
            {[
              { name: 'DKB', op: bank1 },
              { name: 'ING', op: bank2 },
              { name: 'COMDIRECT', op: bank3 },
            ].map((bank, i) => (
              <div
                key={i}
                style={{
                  opacity: bank.op,
                  transform: `translateX(${interpolate(bank.op, [0, 1], [-30, 0])}px)`,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 16,
                }}
              >
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                  <path
                    d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"
                    stroke="#e9d700"
                    strokeWidth="2"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                  />
                </svg>
                <span
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 34,
                    color: '#F5F5F5',
                    fontWeight: 700,
                  }}
                >
                  {bank.name}
                </span>
                <span
                  style={{
                    fontFamily: 'Open Sans, sans-serif',
                    fontSize: 26,
                    color: '#9E9E9E',
                  }}
                >
                  — online, kostenlos
                </span>
              </div>
            ))}
          </div>
        </div>

        {/* RIGHT — Warning panel */}
        <div
          style={{
            opacity: warningOp,
            transform: `translateX(${warningShake}px)`,
            backgroundColor: 'rgba(255,0,0,0.08)',
            border: '3px solid #ff0000',
            borderRadius: 20,
            padding: '44px 52px',
            maxWidth: 560,
            textAlign: 'center',
          }}
        >
          <svg
            width="64"
            height="64"
            viewBox="0 0 24 24"
            fill="none"
            style={{ marginBottom: 20 }}
          >
            <path
              d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"
              stroke="#ff0000"
              strokeWidth="2"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
            <line x1="12" y1="9" x2="12" y2="13" stroke="#ff0000" strokeWidth="2" strokeLinecap="round" />
            <line x1="12" y1="17" x2="12.01" y2="17" stroke="#ff0000" strokeWidth="2" strokeLinecap="round" />
          </svg>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 48,
              color: '#ff0000',
              letterSpacing: 1,
              marginBottom: 12,
            }}
          >
            OHNE ANTRAG
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 100,
              color: '#ff0000',
              fontWeight: 700,
              lineHeight: 1,
            }}
          >
            25%
          </div>
          <div
            style={{
              fontFamily: 'Open Sans, sans-serif',
              fontSize: 28,
              color: '#F5F5F5',
              marginTop: 12,
              lineHeight: 1.4,
            }}
          >
            Abgeltungssteuer —{' '}
            <strong style={{ color: '#ff0000' }}>automatisch einbehalten</strong>
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 24,
              color: '#9E9E9E',
              marginTop: 18,
              fontStyle: 'italic',
            }}
          >
            Du verschenkst Geld, das dir per Gesetz zusteht.
          </div>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(680, ${730 + Math.sin(frame * 0.05) * 2}) scale(${0.75 + sp(55) * 0.25})`}
            opacity={sp(55)}
          >
            <ellipse cx="60" cy="280" rx="48" ry="48" fill="#e9d700" opacity="0.07" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Arms showing alarm */}
            <line x1="60" y1="120" x2={20 + Math.sin(frame * 0.1) * 8} y2={90} stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="120" x2={100 + Math.sin(frame * 0.1 + 1) * 8} y2={90} stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 42%, rgba(0,0,0,0.8) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S4_FünfMinuten;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S5_DepotStruktur',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S5_DepotStruktur: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Pan from left
  const camX = interpolate(frame, [0, durationInFrames], [20, -20], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Chaos vs Order split reveal
  const dividerX = interpolate(sp(15), [0, 1], [960, 960]);

  // Chaotic items floating randomly
  const chaosItems = [
    { label: 'APPLE', x: 150, y: 180 },
    { label: 'FONDS', x: 320, y: 400 },
    { label: 'BITCOIN?', x: 80, y: 580 },
    { label: 'EINZELAKTIE', x: 250, y: 720 },
    { label: 'EMPFEHLUNG', x: 400, y: 280 },
  ];

  // Order items
  const orderItems = sp(45);

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 19 + 1) * 1920,
    y: seededRandom(i * 19 + 2) * 1080,
    r: seededRandom(i * 19 + 3) * 3 + 1,
    speed: seededRandom(i * 19 + 4) * 0.3 + 0.15,
    phase: seededRandom(i * 19 + 5) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translateX(${camX}px)`,
      }}
    >
      {/* === GRADIENT === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(to right, #1a0800 0%, #1C1C1C 50%, #001520 100%)',
        }}
      />

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {/* Divider line */}
          <line
            x1={960}
            y1={0}
            x2={960}
            y2={1080}
            stroke="#9E9E9E"
            strokeWidth="1"
            opacity={0.15}
            strokeDasharray="8 8"
          />
          {/* Large circle right */}
          <circle
            cx={1440}
            cy={540}
            r={280}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.1}, 1440, 540)`}
          />
          {/* Chaotic scribble left */}
          <path
            d={`M 100 300 Q ${150 + Math.sin(frame * 0.04) * 20} ${250 + Math.cos(frame * 0.03) * 15} 200 350
               Q ${250 + Math.sin(frame * 0.05) * 15} ${380 + Math.cos(frame * 0.04) * 10} 180 420`}
            fill="none"
            stroke="#ff0000"
            strokeWidth="1"
            opacity={0.08}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CHAOS SIDE (LEFT) === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        {/* Label */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 120,
            opacity: sp(10),
          }}
        >
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 54,
              color: '#ff0000',
              letterSpacing: 2,
            }}
          >
            OHNE PLAN
          </span>
        </div>

        {chaosItems.map((item, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: item.x,
              top: item.y,
              opacity: sp(10 + i * 8) * 0.7,
              transform: `rotate(${Math.sin(frame * 0.03 + i) * 12 - 8}deg) translateY(${Math.sin(frame * 0.04 + i * 1.2) * 8}px)`,
              backgroundColor: 'rgba(255,0,0,0.1)',
              border: '1px solid rgba(255,0,0,0.3)',
              borderRadius: 8,
              padding: '10px 18px',
            }}
          >
            <span
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 26,
                color: '#ff0000',
                fontWeight: 700,
              }}
            >
              {item.label}
            </span>
          </div>
        ))}

        {/* X symbol */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 200,
            opacity: sp(30),
          }}
        >
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#ff0000" strokeWidth="2" />
            <line x1="15" y1="9" x2="9" y2="15" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="9" y1="9" x2="15" y2="15" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* === ORDER SIDE (RIGHT) === */}
      <AbsoluteFill
        style={{
          opacity: orderItems,
          pointerEvents: 'none',
        }}
      >
        {/* Label */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            right: 120,
            textAlign: 'right',
          }}
        >
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 54,
              color: '#e9d700',
              letterSpacing: 2,
            }}
          >
            MIT STRUKTUR
          </span>
        </div>

        {/* Structured items */}
        {[
          { label: 'MSCI WORLD ETF', pct: '60%', color: '#e9d700', y: 200 },
          { label: 'NOTFALL-RESERVE', pct: '30%', color: '#F5F5F5', y: 330 },
          { label: 'SPARPLAN AUTO', pct: '10%', color: '#9E9E9E', y: 460 },
        ].map((item, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              right: 120,
              top: item.y,
              opacity: sp(50 + i * 12),
              transform: `translateX(${interpolate(sp(50 + i * 12), [0, 1], [40, 0])}px)`,
              display: 'flex',
              alignItems: 'center',
              gap: 16,
              backgroundColor: 'rgba(233,215,0,0.05)',
              border: `1px solid ${item.color}`,
              borderRadius: 10,
              padding: '16px 28px',
              width: 680,
            }}
          >
            <span
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 38,
                fontWeight: 700,
                color: item.color,
                minWidth: 80,
              }}
            >
              {item.pct}
            </span>
            <span
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 30,
                color: '#F5F5F5',
                fontWeight: 600,
              }}
            >
              {item.label}
            </span>
            <svg
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              style={{ marginLeft: 'auto' }}
            >
              <polyline points="20 6 9 17 4 12" stroke={item.color} strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>
        ))}

        {/* Checkmark */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            right: 220,
            opacity: sp(80),
          }}
        >
          <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#e9d700" strokeWidth="2" />
            <polyline points="9 12 11 14 15 10" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* === STEP LABEL CENTER === */}
      <div
        style={{
          position: 'absolute',
          bottom: 55,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(15),
        }}
      >
        <div
          style={{
            backgroundColor: '#e9d700',
            padding: '8px 28px',
            borderRadius: 6,
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              fontWeight: 700,
              color: '#1C1C1C',
              letterSpacing: 2,
            }}
          >
            SCHRITT 2 — DEPOT STRUKTUR
          </span>
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(920, ${720 + Math.sin(frame * 0.05) * 2}) scale(${0.75 + sp(50) * 0.25})`}
            opacity={sp(50)}
          >
            <ellipse cx="60" cy="280" rx="48" ry="48" fill="#e9d700" opacity="0.07" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* One arm each side — pointing at both panels */}
            <line x1="60" y1="120" x2={10 + Math.sin(frame * 0.05) * 5} y2="130" stroke="#ff0000" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="120" x2={110 + Math.sin(frame * 0.05) * 5} y2="130" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.78) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S5_DepotStruktur;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S6_GrundstrukturPlan',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S6_GrundstrukturPlan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Slow zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Pie chart animation
  const pieProgress = interpolate(frame, [15, 65], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // ETF segment: 60% of 360 = 216 degrees
  // Reserve segment: 35% of 360 = 126 degrees
  // Sparplan: 5% of 360 = 18 degrees
  const total = 360;
  const etfAngle = 216 * pieProgress;
  const reserveAngle = 126 * pieProgress;

  const polarToCartesian = (cx: number, cy: number, r: number, angleDeg: number) => {
    const rad = ((angleDeg - 90) * Math.PI) / 180;
    return { x: cx + r * Math.cos(rad), y: cy + r * Math.sin(rad) };
  };

  const describeArc = (cx: number, cy: number, r: number, startAngle: number, endAngle: number) => {
    const s = polarToCartesian(cx, cy, r, startAngle);
    const e = polarToCartesian(cx, cy, r, endAngle);
    const large = endAngle - startAngle > 180 ? 1 : 0;
    return `M ${cx} ${cy} L ${s.x} ${s.y} A ${r} ${r} 0 ${large} 1 ${e.x} ${e.y} Z`;
  };

  // Particles
  const particles = Array.from({ length: 15 }, (_, i) => ({
    x: seededRandom(i * 23 + 1) * 1920,
    y: seededRandom(i * 23 + 2) * 1080,
    r: seededRandom(i * 23 + 3) * 3 + 1,
    speed: seededRandom(i * 23 + 4) * 0.3 + 0.15,
    phase: seededRandom(i * 23 + 5) * Math.PI * 2,
  }));

  const card1 = sp(55);
  const card2 = sp(68);
  const card3 = sp(81);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${camScale})`,
        transformOrigin: 'center center',
      }}
    >
      {/* === GRADIENT === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 80% 60%, #001a08 0%, #1C1C1C 55%), radial-gradient(ellipse at 10% 20%, #1a1000 0%, transparent 50%)',
        }}
      />

      {/* === RADIAL LINE TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 24 }, (_, i) => {
            const angle = (i / 24) * 360;
            const rad = (angle * Math.PI) / 180;
            return (
              <line
                key={i}
                x1={540}
                y1={540}
                x2={540 + Math.cos(rad) * 600}
                y2={540 + Math.sin(rad) * 600}
                stroke="#e9d700"
                strokeWidth="1"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <rect
            x={1500}
            y={100}
            width={250}
            height={250}
            rx={8}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.12}, 1625, 225)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 13}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PIE CHART (LEFT SIDE) === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="1920"
          height="1080"
          style={{ position: 'absolute', left: 0, top: 0 }}
        >
          <g opacity={sp(15)}>
            {/* ETF slice — yellow */}
            <path
              d={describeArc(480, 540, 280, 0, etfAngle)}
              fill="#e9d700"
              opacity={0.85}
            />
            {/* Reserve slice — white */}
            <path
              d={describeArc(
                480,
                540,
                280,
                etfAngle,
                Math.min(etfAngle + reserveAngle, 360)
              )}
              fill="#F5F5F5"
              opacity={0.5}
            />
            {/* Sparplan slice — grey */}
            {pieProgress > 0.95 && (
              <path
                d={describeArc(480, 540, 280, etfAngle + reserveAngle, 360)}
                fill="#9E9E9E"
                opacity={0.4}
              />
            )}
            {/* Center hole */}
            <circle cx={480} cy={540} r={110} fill="#1C1C1C" />
            {/* Center label */}
            <text
              x={480}
              y={530}
              textAnchor="middle"
              fontFamily="Anton, sans-serif"
              fontSize="42"
              fill="#e9d700"
            >
              20K
            </text>
            <text
              x={480}
              y={575}
              textAnchor="middle"
              fontFamily="Inter, sans-serif"
              fontSize="22"
              fill="#9E9E9E"
              fontWeight="600"
            >
              GRUNDSTRUKTUR
            </text>
          </g>
        </svg>
      </AbsoluteFill>

      {/* === DETAIL CARDS (RIGHT) === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'flex-end',
          paddingRight: 120,
          gap: 28,
        }}
      >
        {/* Headline */}
        <div
          style={{
            opacity: sp(12),
            transform: `translateX(${interpolate(sp(12), [0, 1], [40, 0])}px)`,
            width: 760,
            marginBottom: 10,
          }}
        >
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 72,
              color: '#F5F5F5',
              letterSpacing: 1,
            }}
          >
            DEIN DEPOT —
          </span>
          <br />
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 72,
              color: '#e9d700',
              letterSpacing: 1,
            }}
          >
            RICHTIG AUFGETEILT
          </span>
        </div>

        {/* Card 1 */}
        {[
          {
            op: card1,
            color: '#e9d700',
            pct: '60%',
            amount: '10–12K €',
            label: 'MSCI World / FTSE All World ETF',
            sub: 'Globale Diversifikation — kein Einzelrisiko',
          },
          {
            op: card2,
            color: '#F5F5F5',
            pct: '35%',
            amount: '5–7K €',
            label: 'Tagesgeld Notfall-Reserve',
            sub: '3–4% Zinsen — sofort verfügbar',
          },
          {
            op: card3,
            color: '#9E9E9E',
            pct: '5%',
            amount: '≥50 €/Mo',
            label: 'Automatischer Sparplan',
            sub: 'Läuft immer — ohne Nachdenken',
          },
        ].map((card, i) => (
          <div
            key={i}
            style={{
              opacity: card.op,
              transform: `translateX(${interpolate(card.op, [0, 1], [50, 0])}px)`,
              display: 'flex',
              alignItems: 'center',
              gap: 24,
              backgroundColor: 'rgba(255,255,255,0.03)',
              border: `1.5px solid ${card.color === '#e9d700' ? '#e9d700' : 'rgba(245,245,245,0.15)'}`,
              borderRadius: 14,
              padding: '20px 28px',
              width: 760,
            }}
          >
            <div
              style={{
                minWidth: 72,
                textAlign: 'center',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 36,
                  fontWeight: 700,
                  color: card.color,
                }}
              >
                {card.pct}
              </div>
            </div>
            <div
              style={{
                width: '1px',
                height: 60,
                backgroundColor: card.color,
                opacity: 0.3,
              }}
            />
            <div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 30,
                  fontWeight: 700,
                  color: card.color,
                  marginBottom: 4,
                }}
              >
                {card.amount}
              </div>
              <div
                style={{
                  fontFamily: 'Open Sans, sans-serif',
                  fontSize: 24,
                  color: '#F5F5F5',
                  fontWeight: 600,
                }}
              >
                {card.label}
              </div>
              <div
                style={{
                  fontFamily: 'Open Sans, sans-serif',
                  fontSize: 20,
                  color: '#9E9E9E',
                }}
              >
                {card.sub}
              </div>
            </div>
          </div>
        ))}
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(440, ${740 + Math.sin(frame * 0.05) * 2}) scale(${0.75 + sp(45) * 0.25})`}
            opacity={sp(45)}
          >
            <ellipse cx="60" cy="280" rx="50" ry="50" fill="#e9d700" opacity="0.07" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="120" x2="20" y2="150" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line
              x1="60"
              y1="120"
              x2={100 + Math.sin(frame * 0.05) * 4}
              y2={100 + Math.cos(frame * 0.05) * 3}
              stroke="#e9d700"
              strokeWidth="4"
              strokeLinecap="round"
            />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.78) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S6_GrundstrukturPlan;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S7_AutomatisierungFeind',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S7_AutomatisierungFeind: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Tilt camera effect
  const camTilt = interpolate(frame, [0, durationInFrames], [0, 1.5], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Gearwheel rotation
  const gearRotate = frame * 1.2;
  const gearRotateReverse = -frame * 0.8;

  // Brain overload animation
  const brainPulse = 1 + Math.sin(frame * 0.15) * 0.05;

  // "IMPULS" word shake
  const impulsShake = frame > 55
    ? Math.sin(frame * 1.2) * interpolate(frame, [55, 70], [6, 2], {
        extrapolateLeft: 'clamp',
        extrapolateRight: 'clamp',
      })
    : 0;

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 29 + 1) * 1920,
    y: seededRandom(i * 29 + 2) * 1080,
    r: seededRandom(i * 29 + 3) * 3 + 1,
    speed: seededRandom(i * 29 + 4) * 0.3 + 0.15,
    phase: seededRandom(i * 29 + 5) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `rotate(${camTilt * 0.2}deg)`,
        transformOrigin: 'center center',
      }}
    >
      {/* === GRADIENT === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 0%, #1a0020 0%, #1C1C1C 55%), radial-gradient(ellipse at 50% 100%, #001510 0%, transparent 50%)',
        }}
      />

      {/* === CIRCUIT TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 12 }, (_, i) => (
            <g key={i}>
              <line
                x1={i * 170}
                y1={0}
                x2={i * 170}
                y2={1080}
                stroke="#e9d700"
                strokeWidth="1"
                strokeDasharray="4 20"
              />
              <line
                x1={0}
                y1={i * 95}
                x2={1920}
                y2={i * 95}
                stroke="#e9d700"
                strokeWidth="1"
                strokeDasharray="4 20"
              />
              <circle cx={i * 170} cy={(i % 4) * 95 + 95} r="4" fill="#e9d700" />
            </g>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Gears === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {/* Gear 1 */}
          <g transform={`translate(1500, 540) rotate(${gearRotate})`} opacity={0.06}>
            <circle cx={0} cy={0} r={200} fill="none" stroke="#e9d700" strokeWidth="12" />
            {Array.from({ length: 12 }, (_, i) => {
              const a = (i / 12) * 360;
              const rad = (a * Math.PI) / 180;
              return (
                <rect
                  key={i}
                  x={-10}
                  y={-220}
                  width={20}
                  height={40}
                  rx={4}
                  fill="#e9d700"
                  transform={`rotate(${a})`}
                />
              );
            })}
          </g>
          {/* Gear 2 */}
          <g transform={`translate(1340, 540) rotate(${gearRotateReverse})`} opacity={0.04}>
            <circle cx={0} cy={0} r={120} fill="none" stroke="#F5F5F5" strokeWidth="8" />
            {Array.from({ length: 8 }, (_, i) => {
              const a = (i / 8) * 360;
              return (
                <rect
                  key={i}
                  x={-7}
                  y={-135}
                  width={14}
                  height={28}
                  rx={3}
                  fill="#F5F5F5"
                  transform={`rotate(${a})`}
                />
              );
            })}
          </g>
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          paddingLeft: 130,
          paddingRight: 700,
        }}
      >
        {/* AUTO label */}
        <div
          style={{
            opacity: sp(10),
            transform: `translateY(${interpolate(sp(10), [0, 1], [-30, 0])}px)`,
            marginBottom: 20,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 14,
              backgroundColor: '#e9d700',
              padding: '8px 22px',
              borderRadius: 4,
            }}
          >
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <polyline points="23 4 23 10 17 10" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
              <path d="M20.49 15a9 9 0 11-2.12-9.36L23 10" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 24,
                fontWeight: 700,
                color: '#1C1C1C',
                letterSpacing: 2,
              }}
            >
              AUTOMATISIERUNG
            </span>
          </div>
        </div>

        {/* Headline */}
        <div
          style={{
            opacity: sp(15),
            transform: `translateY(${interpolate(sp(15), [0, 1], [40, 0])}px)`,
            marginBottom: 28,
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 100,
              color: '#F5F5F5',
              lineHeight: 1.0,
            }}
          >
            NIMMT DEN
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 130,
              color: '#e9d700',
              lineHeight: 1.0,
            }}
          >
            FEIND
          </div>
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 100,
              color: '#F5F5F5',
              lineHeight: 1.0,
            }}
          >
            RAUS.
          </div>
        </div>

        {/* NOT the market */}
        <div
          style={{
            opacity: sp(45),
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            marginBottom: 16,
          }}
        >
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="4.93" y1="4.93" x2="19.07" y2="19.07" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
          </svg>
          <span
            style={{
              fontFamily: 'Open Sans, sans-serif',
              fontSize: 36,
              color: '#9E9E9E',
              textDecoration: 'line-through',
            }}
          >
            Nicht: der Markt
          </span>
        </div>

        {/* THE IMPULS */}
        <div
          style={{
            opacity: sp(55),
            transform: `translateX(${impulsShake}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2" fill="#ff0000" stroke="#ff0000" strokeWidth="1" />
          </svg>
          <span
            style={{
              fontFamily: 'Open Sans, sans-serif',
              fontSize: 44,
              color: '#ff0000',
              fontWeight: 700,
            }}
          >
            Dein{' '}
            <strong
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 56,
                letterSpacing: 1,
              }}
            >
              IMPULS
            </strong>{' '}
            — es auszusetzen.
          </span>
        </div>
      </AbsoluteFill>

      {/* === BRAIN ICON (RIGHT SIDE) === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: '50%',
            transform: `translateY(-50%) scale(${brainPulse * sp(35)})`,
            opacity: sp(35),
          }}
        >
          <svg width="380" height="380" viewBox="0 0 100 100">
            {/* Stylized brain */}
            <ellipse cx="50" cy="52" rx="36" ry="30" fill="none" stroke="#F5F5F5" strokeWidth="2.5" opacity="0.6" />
            <path d="M50 22 Q60 12 70 22 Q80 32 70 42" fill="none" stroke="#F5F5F5" strokeWidth="2.5" opacity="0.6" />
            <path d="M50 22 Q40 12 30 22 Q20 32 30 42" fill="none" stroke="#F5F5F5" strokeWidth="2.5" opacity="0.6" />
            <line x1="50" y1="22" x2="50" y2="82" stroke="#F5F5F5" strokeWidth="2" opacity="0.3" strokeDasharray="3 4" />
            {/* Overload sparks */}
            {frame > 55 &&
              [0, 60, 120, 180, 240, 300].map((angle, i) => {
                const rad = (angle * Math.PI) / 180;
                const dist = 50 + Math.sin(frame * 0.2 + i) * 8;
                return (
                  <line
                    key={i}
                    x1={50 + Math.cos(rad) * 36}
                    y1={52 + Math.sin(rad) * 30}
                    x2={50 + Math.cos(rad) * dist}
                    y2={52 + Math.sin(rad) * (30 + (dist - 36) * 0.8)}
                    stroke="#ff0000"
                    strokeWidth="1.5"
                    opacity={0.7 + Math.sin(frame * 0.3 + i) * 0.3}
                  />
                );
              })}
            {/* IMPULS label */}
            <text
              x="50"
              y="57"
              textAnchor="middle"
              fontFamily="Anton, sans-serif"
              fontSize="12"
              fill="#ff0000"
              opacity={sp(55)}
            >
              IMPULS
            </text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(740, ${720 + Math.sin(frame * 0.05) * 2}) scale(${0.8 + sp(48) * 0.2})`}
            opacity={sp(48)}
          >
            <ellipse cx="60" cy="280" rx="50" ry="50" fill="#e9d700" opacity="0.07" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Calm, one arm up for auto */}
            <line x1="60" y1="120" x2="20" y2="150" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line
              x1="60"
              y1="120"
              x2="100"
              y2={90 + Math.sin(frame * 0.04) * 4}
              stroke="#e9d700"
              strokeWidth="4"
              strokeLinecap="round"
            />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.82) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S7_AutomatisierungFeind;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S8_Steuererklarung',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S8_Steuererklarung: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Counter for refund amount
  const refundCount = Math.round(
    interpolate(frame, [50, 90], [0, 400], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
      easing: (t) => t * t,
    })
  );

  // Coins falling animation
  const coinsOp = interpolate(frame, [45, 60], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // List items
  const list1 = sp(30);
  const list2 = sp(44);
  const list3 = sp(58);

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 31 + 1) * 1920,
    y: seededRandom(i * 31 + 2) * 1080,
    r: seededRandom(i * 31 + 3) * 3 + 1,
    speed: seededRandom(i * 31 + 4) * 0.3 + 0.15,
    phase: seededRandom(i * 31 + 5) * Math.PI * 2,
  }));

  // Coins
  const coins = Array.from({ length: 8 }, (_, i) => ({
    x: 1300 + seededRandom(i * 5 + 1) * 300,
    startY: -80 + seededRandom(i * 5 + 2) * -60,
    delay: i * 6,
    size: 28 + seededRandom(i * 5 + 3) * 18,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === GRADIENT === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(145deg, #1a1500 0%, #1C1C1C 45%, #001a0a 100%)',
        }}
      />

      {/* === HORIZONTAL LINE TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 30 }, (_, i) => (
            <line
              key={i}
              x1={0}
              y1={i * 38}
              x2={1920}
              y2={i * 38}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <circle
            cx={200}
            cy={200}
            r={220}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.08}, 200, 200)`}
          />
          <rect
            x={1550}
            y={700}
            width={280}
            height={280}
            rx={8}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.1 + 15}, 1690, 840)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === FALLING COINS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {coins.map((coin, i) => {
            const coinFrame = Math.max(0, frame - 45 - coin.delay);
            const coinY = interpolate(coinFrame, [0, 60], [coin.startY, 900], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            });
            const coinOp = interpolate(coinFrame, [0, 8, 50, 60], [0, 0.8, 0.8, 0], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            });
            return (
              <g key={i} opacity={coinOp}>
                <ellipse
                  cx={coin.x}
                  cy={coinY}
                  rx={coin.size / 2}
                  ry={coin.size / 5}
                  fill="#e9d700"
                  opacity={0.9}
                />
                <text
                  x={coin.x}
                  y={coinY + 3}
                  textAnchor="middle"
                  fontFamily="JetBrains Mono, monospace"
                  fontSize={coin.size * 0.4}
                  fill="#1C1C1C"
                  fontWeight="700"
                >
                  €
                </text>
              </g>
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          paddingLeft: 120,
          paddingRight: 120,
          gap: 60,
        }}
      >
        {/* LEFT */}
        <div style={{ flex: 1 }}>
          {/* Step badge */}
          <div
            style={{
              opacity: sp(10),
              display: 'flex',
              alignItems: 'center',
              gap: 14,
              marginBottom: 22,
            }}
          >
            <div
              style={{
                width: 56,
                height: 56,
                borderRadius: '50%',
                backgroundColor: '#e9d700',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}
            >
              <span
                style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 28,
                  color: '#1C1C1C',
                }}
              >
                3
              </span>
            </div>
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 26,
                fontWeight: 700,
                color: '#9E9E9E',
                letterSpacing: 3,
                textTransform: 'uppercase',
              }}
            >
              DRITTENS
            </span>
          </div>

          <div
            style={{
              opacity: sp(12),
              transform: `translateX(${interpolate(sp(12), [0, 1], [-30, 0])}px)`,
              marginBottom: 8,
            }}
          >
            <span
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 80,
                color: '#F5F5F5',
                letterSpacing: 1,
              }}
            >
              STEUER
            </span>
          </div>
          <div
            style={{
              opacity: sp(18),
              transform: `translateX(${interpolate(sp(18), [0, 1], [-30, 0])}px)`,
              marginBottom: 8,
            }}
          >
            <span
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 80,
                color: '#e9d700',
                letterSpacing: 1,
              }}
            >
              ERKLÄRUNG
            </span>
          </div>
          <div
            style={{
              opacity: sp(24),
              transform: `translateX(${interpolate(sp(24), [0, 1], [-30, 0])}px)`,
              marginBottom: 36,
            }}
          >
            <span
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 34,
                color: '#9E9E9E',
                fontStyle: 'italic',
              }}
            >
              als Werkzeug — nicht als Pflicht
            </span>
          </div>

          {/* List */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 18 }}>
            {[
              { op: list1, text: 'Kapitalverluste aus Vorjahren verrechnen', icon: '↩' },
              { op: list2, text: 'Zu viel einbehaltene Abgeltungssteuer zurückfordern', icon: '↩' },
              { op: list3, text: 'Werbungskosten & Freibeträge nutzen', icon: '↩' },
            ].map((item, i) => (
              <div
                key={i}
                style={{
                  opacity: item.op,
                  transform: `translateX(${interpolate(item.op, [0, 1], [-25, 0])}px)`,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 16,
                }}
              >
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                  <polyline points="20 6 9 17 4 12" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
                <span
                  style={{
                    fontFamily: 'Open Sans, sans-serif',
                    fontSize: 30,
                    color: '#F5F5F5',
                    fontWeight: 400,
                  }}
                >
                  {item.text}
                </span>
              </div>
            ))}
          </div>
        </div>

        {/* RIGHT — Refund counter */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            backgroundColor: 'rgba(233,215,0,0.07)',
            border: '2px solid #e9d700',
            borderRadius: 24,
            padding: '52px 64px',
            minWidth: 440,
            opacity: sp(48),
            transform: `scale(${sp(48)})`,
          }}
        >
          <svg width="56" height="56" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 16 }}>
            <line x1="12" y1="1" x2="12" y2="23" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" />
            <path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              color: '#9E9E9E',
              fontWeight: 600,
              marginBottom: 8,
              textAlign: 'center',
            }}
          >
            MÖGLICHE ERSTATTUNG
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 110,
              fontWeight: 700,
              color: '#e9d700',
              lineHeight: 1,
            }}
          >
            {refundCount}€
          </div>
          <div
            style={{
              fontFamily: 'Open Sans, sans-serif',
              fontSize: 24,
              color: '#F5F5F5',
              marginTop: 14,
              textAlign: 'center',
            }}
          >
            direkt in den{' '}
            <strong style={{ color: '#e9d700' }}>Sparplan</strong>
          </div>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(840, ${720 + Math.sin(frame * 0.05) * 2}) scale(${0.75 + sp(52) * 0.25})`}
            opacity={sp(52)}
          >
            <ellipse cx="60" cy="280" rx="48" ry="48" fill="#e9d700" opacity="0.07" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="120" x2="20" y2="130" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="120" x2="100" y2="110" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 42%, rgba(0,0,0,0.78) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S8_Steuererklarung;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S9_Selbstinvestition',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S9_Selbstinvestition: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // ROI arrow growth
  const arrowGrow = interpolate(frame, [35, 80], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Cost count up
  const costCount = Math.round(
    interpolate(frame, [25, 55], [0, 2000], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );

  // Gain count up
  const gainCount = Math.round(
    interpolate(frame, [55, 90], [0, 10000], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 37 + 1) * 1920,
    y: seededRandom(i * 37 + 2) * 1080,
    r: seededRandom(i * 37 + 3) * 3 + 1,
    speed: seededRandom(i * 37 + 4) * 0.3 + 0.15,
    phase: seededRandom(i * 37 + 5) * Math.PI * 2,
  }));

  // Skill badges
  const badges = [
    { label: 'AWS CERT.', delay: 30 },
    { label: 'DATENSICHERHEIT', delay: 40 },
    { label: 'CLOUD-INFRA', delay: 50 },
    { label: 'PROJEKTMGMT', delay: 60 },
    { label: 'SPRACHEN', delay: 70 },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === GRADIENT — Upward growth feel === */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(180deg, #001a10 0%, #1C1C1C 50%, #1a1000 100%)',
        }}
      />

      {/* === ASCENDING DIAGONAL TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 20 }, (_, i) => (
            <line
              key={i}
              x1={i * 100}
              y1={1080}
              x2={i * 100 + 400}
              y2={0}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {/* Up arrow background */}
          <polygon
            points={`960,${100 + Math.sin(frame * 0.03) * 10} 820,300 900,300 900,600 1020,600 1020,300 1100,300`}
            fill="#e9d700"
            opacity={0.025}
          />
          <circle
            cx={200}
            cy={800}
            r={200}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            opacity={0.04}
            transform={`rotate(${frame * 0.07}, 200, 800)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          paddingLeft: 120,
          paddingRight: 120,
          paddingTop: 60,
          paddingBottom: 60,
          gap: 60,
          alignItems: 'center',
        }}
      >
        {/* LEFT side */}
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 24 }}>
          {/* Step */}
          <div
            style={{
              opacity: sp(10),
              display: 'flex',
              alignItems: 'center',
              gap: 14,
            }}
          >
            <div
              style={{
                width: 56,
                height: 56,
                borderRadius: '50%',
                backgroundColor: '#e9d700',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}
            >
              <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 28, color: '#1C1C1C' }}>4</span>
            </div>
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 24,
                fontWeight: 700,
                color: '#9E9E9E',
                letterSpacing: 3,
                textTransform: 'uppercase',
              }}
            >
              VIERTENS
            </span>
          </div>

          {/* Title */}
          <div style={{ opacity: sp(14) }}>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 72,
                color: '#F5F5F5',
                lineHeight: 1.0,
              }}
            >
              ASYMMETRISCHE
            </div>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 72,
                color: '#e9d700',
                lineHeight: 1.0,
              }}
            >
              SELBSTINVESTITION
            </div>
          </div>

          {/* Skill badges */}
          <div style={{ display: 'flex', flexWrap: 'wrap', gap: 12 }}>
            {badges.map((badge, i) => (
              <div
                key={i}
                style={{
                  opacity: sp(badge.delay),
                  transform: `scale(${sp(badge.delay)})`,
                  backgroundColor: 'rgba(233,215,0,0.08)',
                  border: '1px solid rgba(233,215,0,0.35)',
                  borderRadius: 6,
                  padding: '10px 18px',
                }}
              >
                <span
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 22,
                    color: '#e9d700',
                    fontWeight: 600,
                  }}
                >
                  {badge.label}
                </span>
              </div>
            ))}
          </div>

          {/* ROI arrow chart */}
          <div style={{ opacity: sp(35), marginTop: 8 }}>
            <svg width="560" height="160" viewBox="0 0 560 160">
              {/* Baseline */}
              <line x1={0} y1={140} x2={560} y2={140} stroke="#2D2D2D" strokeWidth="2" />
              {/* Growth line */}
              <path
                d={`M 0 140 Q ${200 * arrowGrow} ${140 - 60 * arrowGrow} ${560 * arrowGrow} ${140 - 120 * arrowGrow}`}
                fill="none"
                stroke="#e9d700"
                strokeWidth="3"
                strokeLinecap="round"
              />
              {/* Arrow head */}
              <polygon
                points={`${560 * arrowGrow},${140 - 120 * arrowGrow - 14} ${560 * arrowGrow - 10},${140 - 120 * arrowGrow + 6} ${560 * arrowGrow + 10},${140 - 120 * arrowGrow + 6}`}
                fill="#e9d700"
                opacity={arrowGrow}
              />
              <text x="8" y="158" fontFamily="Inter, sans-serif" fontSize="18" fill="#9E9E9E" fontWeight="600">
                INVESTITION
              </text>
              <text
                x={`${560 * arrowGrow - 60}`}
                y={`${140 - 120 * arrowGrow - 20}`}
                fontFamily="Inter, sans-serif"
                fontSize="18"
                fill="#e9d700"
                fontWeight="700"
                opacity={arrowGrow}
              >
                RENDITE
              </text>
            </svg>
          </div>
        </div>

        {/* RIGHT — Numbers */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 24,
          }}
        >
          {/* Cost */}
          <div
            style={{
              opacity: sp(25),
              transform: `scale(${sp(25)})`,
              backgroundColor: 'rgba(255,255,255,0.04)',
              border: '1px solid rgba(245,245,245,0.2)',
              borderRadius: 16,
              padding: '32px 44px',
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 22,
                color: '#9E9E9E',
                fontWeight: 600,
                marginBottom: 8,
                letterSpacing: 2,
              }}
            >
              INVESTITION
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 72,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              {costCount.toLocaleString('de-DE')}€
            </div>
            <div
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 22,
                color: '#9E9E9E',
                marginTop: 6,
              }}
            >
              Zertifizierung
            </div>
          </div>

          {/* Arrow down */}
          <div style={{ opacity: sp(55) }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <line x1="12" y1="5" x2="12" y2="19" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" />
              <polyline points="19 12 12 19 5 12" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          {/* Gain */}
          <div
            style={{
              opacity: sp(55),
              transform: `scale(${sp(55)})`,
              backgroundColor: 'rgba(233,215,0,0.08)',
              border: '2px solid #e9d700',
              borderRadius: 16,
              padding: '32px 44px',
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 22,
                color: '#e9d700',
                fontWeight: 600,
                marginBottom: 8,
                letterSpacing: 2,
              }}
            >
              GEHALTSERHÖHUNG / JAHR
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 90,
                fontWeight: 700,
                color: '#e9d700',
              }}
            >
              +{gainCount.toLocaleString('de-DE')}€
            </div>
            <div
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 22,
                color: '#F5F5F5',
                marginTop: 6,
              }}
            >
              kein ETF der Welt schlägt das
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(870, ${700 + Math.sin(frame * 0.05) * 2}) scale(${0.75 + sp(50) * 0.25})`}
            opacity={sp(50)}
          >
            <ellipse cx="60" cy="280" rx="50" ry="50" fill="#e9d700" opacity="0.07" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Flexing arms — strong */}
            <line x1="60" y1="120" x2="15" y2="95" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="120" x2="105" y2="95" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.78) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S9_Selbstinvestition;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'C7S10_SoliditaetGewinn',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C7S10_SoliditaetGewinn: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Slow orbital camera
  const camX = Math.sin(frame * 0.015) * 12;
  const camY = Math.cos(frame * 0.015) * 6;

  // Scale of solidity block
  const blockScale = sp(30, { damping: 10, stiffness: 80, mass: 0.6 });

  // VS flash
  const vsFlash = interpolate(frame, [45, 55], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i * 41 + 1) * 1920,
    y: seededRandom(i * 41 + 2) * 1080,
    r: seededRandom(i * 41 + 3) * 4 + 1.5,
    speed: seededRandom(i * 41 + 4) * 0.3 + 0.15,
    phase: seededRandom(i * 41 + 5) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translate(${camX}px, ${camY}px)`,
      }}
    >
      {/* === GRADIENT — Deep impact === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at 50% 50%, #1a1200 0%, #1C1C1C 65%)',
        }}
      />

      {/* === CROSS HATCH TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 25 }, (_, i) => (
            <g key={i}>
              <line x1={i * 80} y1={0} x2={i * 80 + 400} y2={1080} stroke="#e9d700" strokeWidth="1" />
              <line x1={i * 80} y1={0} x2={i * 80 - 400} y2={1080} stroke="#e9d700" strokeWidth="1" />
            </g>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {/* Foundation rectangle */}
          <rect
            x={560}
            y={800}
            width={800}
            height={120}
            rx={8}
            fill="#e9d700"
            opacity={0.04 + sp(30) * 0.03}
          />
          {/* Outer rings */}
          <circle cx={960} cy={540} r={400} fill="none" stroke="#e9d700" strokeWidth="1" opacity={0.04} transform={`rotate(${frame * 0.05}, 960, 540)`} />
          <circle cx={960} cy={540} r={460} fill="none" stroke="#F5F5F5" strokeWidth="1" opacity={0.03} transform={`rotate(${-frame * 0.04}, 960, 540)`} />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 14}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 10}
              r={p.r}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CENTER CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          textAlign: 'center',
        }}
      >
        {/* Hero word */}
        <div
          style={{
            opacity: sp(12),
            transform: `scale(${blockScale})`,
            transformOrigin: 'center',
            marginBottom: 20,
          }}
        >
          <div
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 220,
              color: '#e9d700',
              lineHeight: 0.9,
              letterSpacing: 4,
              textShadow: '0 0 80px rgba(233,215,0,0.2)',
            }}
          >
            SOLID.
          </div>
        </div>

        {/* VS strip */}
        <div
          style={{
            opacity: vsFlash,
            display: 'flex',
            alignItems: 'center',
            gap: 40,
            marginBottom: 20,
          }}
        >
          <div
            style={{
              flex: 1,
              height: 2,
              background: 'linear-gradient(to right, transparent, #e9d700)',
              maxWidth: 300,
            }}
          />
          <span
            style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 44,
              color: '#9E9E9E',
              letterSpacing: 4,
            }}
          >
            VS.
          </span>
          <div
            style={{
              flex: 1,
              height: 2,
              background: 'linear-gradient(to left, transparent, #9E9E9E)',
              maxWidth: 300,
            }}
          />
        </div>

        {/* Comparison */}
        <div
          style={{
            opacity: sp(55),
            transform: `translateY(${interpolate(sp(55), [0, 1], [30, 0])}px)`,
            display: 'flex',
            gap: 40,
            alignItems: 'center',
          }}
        >
          {/* SOLID box */}
          <div
            style={{
              backgroundColor: 'rgba(233,215,0,0.1)',
              border: '2px solid #e9d700',
              borderRadius: 16,
              padding: '28px 40px',
              textAlign: 'center',
              minWidth: 340,
            }}
          >
            <svg width="44" height="44" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 12 }}>
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 44,
                color: '#e9d700',
                marginBottom: 8,
              }}
            >
              STRATEGISCH
            </div>
            <div
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 26,
                color: '#F5F5F5',
                lineHeight: 1.4,
              }}
            >
              Mit 20K Puffer denken.
              <br />
              Handeln. Wachsen.
            </div>
          </div>

          {/* REACTIVE box */}
          <div
            style={{
              backgroundColor: 'rgba(158,158,158,0.05)',
              border: '1px solid rgba(158,158,158,0.2)',
              borderRadius: 16,
              padding: '28px 40px',
              textAlign: 'center',
              minWidth: 340,
              opacity: 0.65,
            }}
          >
            <svg width="44" height="44" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 12 }}>
              <circle cx="12" cy="12" r="10" stroke="#9E9E9E" strokeWidth="2" />
              <path d="M12 8v4l3 3" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 44,
                color: '#9E9E9E',
                marginBottom: 8,
              }}
            >
              REAKTIV
            </div>
            <div
              style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 26,
                color: '#9E9E9E',
                lineHeight: 1.4,
              }}
            >
              Ohne Puffer.
              <br />
              Monat zu Monat.
            </div>
          </div>
        </div>

        {/* Final statement */}
        <div
          style={{
            opacity: sp(80),
            transform: `translateY(${interpolate(sp(80), [0, 1], [20, 0])}px)`,
            marginTop: 36,
            maxWidth: 900,
          }}
        >
          <span
            style={{
              fontFamily: 'Open Sans, sans-serif',
              fontSize: 34,
              color: '#F5F5F5',
              fontStyle: 'italic',
              lineHeight: 1.5,
            }}
          >
            Solidität ist mehr wert als die{' '}
            <strong style={{ color: '#e9d700' }}>perfekte Strategie</strong>,
            die du nie umsetzt.
          </span>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <g
            transform={`translate(1680, ${700 + Math.sin(frame * 0.05) * 2}) scale(${0.8 + sp(55) * 0.2})`}
            opacity={sp(55)}
          >
            <ellipse cx="60" cy="280" rx="50" ry="50" fill="#e9d700" opacity="0.09" />
            <circle
              cx="60"
              cy="60"
              r="30"
              stroke="#F5F5F5"
              strokeWidth="4"
              fill="none"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 60)`}
            />
            <rect x="42" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="62" y="55" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="56" y1="59" x2="62" y2="59" stroke="#e9d700" strokeWidth="2" />
            <line x1="60" y1="90" x2="60" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Both arms out — confident stance */}
            <line x1="60" y1="120" x2="15" y2="140" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="120" x2="105" y2="140" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="35" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="60" y1="200" x2="85" y2="270" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.85) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Kux legt an
      </div>
    </AbsoluteFill>
  );
};

export default C7S10_SoliditaetGewinn;
```

