```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene1_Ch7_FearToNumbers',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_Ch7_FearToNumbers: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Question mark dissolving into numbers
  const qMarkOp = interpolate(frame, [0, 30, 60, 90], [1, 1, 0.3, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const numbersOp = interpolate(frame, [40, 80], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Shatter effect frames
  const shatterProgress = interpolate(frame, [50, 90], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Headline spring
  const headlineScale = sp(20);
  const subScale = sp(55);

  // Screen flash on shatter
  const flashOp = interpolate(frame, [50, 55, 65], [0, 0.6, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Floating numbers data
  const numbers = ['₹42L', '₹18L', '₹8%', '₹60K', '₹2Cr', '₹15L', '4%'];
  const numberPositions = numbers.map((_, i) => ({
    x: 200 + seededRandom(i * 7) * 1500,
    y: 150 + seededRandom(i * 13) * 700,
    delay: 60 + i * 8,
    size: 28 + seededRandom(i * 3) * 20,
  }));

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="bg1" cx="30%" cy="50%" r="70%">
              <stop offset="0%" stopColor="#2D1A00" stopOpacity="1" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="1" />
            </radialGradient>
            <radialGradient id="bg2" cx="80%" cy="50%" r="60%">
              <stop offset="0%" stopColor="#001A2D" stopOpacity="0.8" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg1)" />
          <rect width="1920" height="1080" fill="url(#bg2)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Large drifting geometry === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle
            cx={960 + Math.sin(frame * 0.012) * 30}
            cy={540 + Math.cos(frame * 0.009) * 20}
            r={420 + Math.sin(frame * 0.007) * 15}
            fill="none"
            stroke="#e9d700"
            strokeWidth="2"
          />
          <circle
            cx={960 + Math.sin(frame * 0.015) * 20}
            cy={540 + Math.cos(frame * 0.011) * 15}
            r={300}
            fill="none"
            stroke="#FFC107"
            strokeWidth="1"
          />
          <rect
            x={100 + Math.sin(frame * 0.008) * 10}
            y={80}
            width="200"
            height="200"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            transform={`rotate(${frame * 0.2} 200 180)`}
          />
          <polygon
            points={`1700,150 1800,300 1600,300`}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            transform={`rotate(${-frame * 0.15} 1700 225)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — Grid lines === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 20 }).map((_, i) => (
            <line
              key={`h${i}`}
              x1="0"
              y1={i * 54}
              x2="1920"
              y2={i * 54}
              stroke="#F5F5F5"
              strokeWidth="1"
            />
          ))}
          {Array.from({ length: 32 }).map((_, i) => (
            <line
              key={`v${i}`}
              x1={i * 60}
              y1="0"
              x2={i * 60}
              y2="1080"
              stroke="#F5F5F5"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 35 }).map((_, i) => {
            const px = seededRandom(i * 11) * 1920;
            const py =
              ((seededRandom(i * 17) * 1080 + frame * (0.3 + seededRandom(i * 5) * 0.4)) %
                1080);
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={1 + seededRandom(i * 3) * 2}
                fill={i % 3 === 0 ? '#e9d700' : '#F5F5F5'}
                opacity={0.15 + seededRandom(i * 7) * 0.2}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === FEAR "?" DISSOLVING === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          opacity: qMarkOp,
        }}
      >
        <div
          style={{
            fontSize: 400,
            fontFamily: 'Inter',
            fontWeight: 900,
            color: '#ff0000',
            opacity: 0.25,
            filter: `blur(${shatterProgress * 8}px)`,
            transform: `scale(${1 + shatterProgress * 0.2})`,
            lineHeight: 1,
          }}
        >
          ?
        </div>
      </AbsoluteFill>

      {/* === SHATTER SHARDS === */}
      {shatterProgress > 0 && (
        <AbsoluteFill style={{ opacity: shatterProgress * (1 - shatterProgress * 1.5) }}>
          <svg width="1920" height="1080" style={{ position: 'absolute' }}>
            {Array.from({ length: 8 }).map((_, i) => {
              const angle = (i / 8) * Math.PI * 2;
              const dist = shatterProgress * 200;
              const cx = 960 + Math.cos(angle) * dist;
              const cy = 540 + Math.sin(angle) * dist;
              return (
                <polygon
                  key={i}
                  points={`${cx},${cy - 30} ${cx + 20},${cy + 20} ${cx - 20},${cy + 20}`}
                  fill="#ff0000"
                  opacity={0.6 - shatterProgress * 0.6}
                  transform={`rotate(${angle * 57 + shatterProgress * 180} ${cx} ${cy})`}
                />
              );
            })}
          </svg>
        </AbsoluteFill>
      )}

      {/* === FLOATING NUMBERS APPEARING === */}
      <AbsoluteFill style={{ opacity: numbersOp }}>
        {numberPositions.map((pos, i) => {
          const numSp = spring({
            frame: Math.max(0, frame - pos.delay),
            fps,
            config: { damping: 14, mass: 0.3, stiffness: 100 },
          });
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: pos.x,
                top: pos.y,
                fontSize: pos.size,
                fontFamily: 'JetBrains Mono',
                fontWeight: 700,
                color: i % 2 === 0 ? '#e9d700' : '#F5F5F5',
                opacity: numSp * 0.7,
                transform: `translateY(${(1 - numSp) * 20}px)`,
              }}
            >
              {numbers[i]}
            </div>
          );
        })}
      </AbsoluteFill>

      {/* === MAIN CONTENT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          paddingBottom: 60,
        }}
      >
        {/* Top label */}
        <div
          style={{
            fontSize: 32,
            fontFamily: 'Inter',
            fontWeight: 700,
            color: '#e9d700',
            letterSpacing: 8,
            textTransform: 'uppercase',
            opacity: sp(10),
            marginBottom: 24,
          }}
        >
          Chapter 7 • असली रणनीति
        </div>

        {/* Main headline */}
        <div
          style={{
            fontSize: 120,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
            lineHeight: 1.1,
            transform: `scale(${headlineScale})`,
            opacity: headlineScale,
            maxWidth: 1400,
          }}
        >
          अस्पष्ट डर को
        </div>
        <div
          style={{
            fontSize: 120,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            background: 'linear-gradient(90deg, #e9d700, #FFC107)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent',
            textAlign: 'center',
            lineHeight: 1.1,
            transform: `scale(${subScale})`,
            opacity: subScale,
            maxWidth: 1400,
          }}
        >
          असली संख्याओं से बदलो
        </div>

        {/* Divider line */}
        <div
          style={{
            marginTop: 32,
            width: `${sp(70) * 600}px`,
            height: 3,
            background: 'linear-gradient(90deg, transparent, #e9d700, transparent)',
          }}
        />

        {/* Sub text */}
        <div
          style={{
            fontSize: 44,
            fontFamily: 'Noto Sans Devanagari',
            color: '#9E9E9E',
            marginTop: 24,
            opacity: sp(80),
            transform: `translateY(${(1 - sp(80)) * 20}px)`,
            textAlign: 'center',
          }}
        >
          एहसास नहीं — तथ्य चाहिए
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE — Warren Buffett === */}
      <AbsoluteFill>
        <svg
          width="1920"
          height="1080"
          style={{ position: 'absolute', opacity: sp(40) }}
        >
          <g
            transform={`translate(${1680 + (1 - sp(40)) * 100}, ${
              820 + Math.sin(frame * 0.05) * 2
            })`}
          >
            {/* Golden glow */}
            <circle cx="0" cy="-130" r="55" fill="#e9d700" opacity="0.12" />
            {/* Head */}
            <circle
              cx="0"
              cy="-130"
              r="28"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 0 -130)`}
            />
            {/* Golden glasses */}
            <ellipse cx="-10" cy="-130" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <ellipse cx="10" cy="-130" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="1" y1="-130" x2="-1" y2="-130" stroke="#FFC107" strokeWidth="2" />
            {/* Body */}
            <line x1="0" cy="-102" x2="0" y2="-40" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms — pointing at numbers */}
            <line x1="0" y1="-80" x2="-40" y2="-55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-80" x2="35" y2="-60" stroke="#F5F5F5" strokeWidth="3" />
            {/* Legs */}
            <line x1="0" y1="-40" x2="-22" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-40" x2="22" y2="0" stroke="#F5F5F5" strokeWidth="3" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === SCREEN FLASH === */}
      <AbsoluteFill
        style={{
          backgroundColor: '#ff0000',
          opacity: flashOp,
          pointerEvents: 'none',
        }}
      />

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig1" cx="50%" cy="50%" r="70%">
              <stop offset="60%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.85" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig1)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_Ch7_FearToNumbers;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_Ch7_FogVsClarity',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_Ch7_FogVsClarity: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Split screen reveal — fog left, clarity right
  const dividerX = interpolate(frame, [20, 70], [0, 960], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: (t) => t * t * (3 - 2 * t),
  });

  // Fog blur animating
  const fogBlur = interpolate(frame, [0, 40], [20, 8], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const zoom = interpolate(frame, [0, durationInFrames], [1, 1.04], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${zoom})`,
      }}
    >
      {/* === LEFT SIDE — FOG / FEELING === */}
      <AbsoluteFill
        style={{
          clipPath: `inset(0 ${1920 - dividerX}px 0 0)`,
          background: 'linear-gradient(135deg, #1A0808 0%, #2D1010 100%)',
        }}
      >
        {/* Fog texture */}
        <AbsoluteFill style={{ filter: `blur(${fogBlur}px)`, opacity: 0.4 }}>
          <svg width="1920" height="1080" style={{ position: 'absolute' }}>
            {Array.from({ length: 6 }).map((_, i) => (
              <ellipse
                key={i}
                cx={100 + i * 130 + Math.sin(frame * 0.02 + i) * 20}
                cy={200 + i * 120 + Math.cos(frame * 0.015 + i) * 15}
                rx={200 + i * 30}
                ry={80}
                fill="#9E9E9E"
                opacity="0.3"
              />
            ))}
          </svg>
        </AbsoluteFill>

        {/* Left label */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 80,
            width: 840,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontSize: 48,
              fontFamily: 'Inter',
              fontWeight: 800,
              color: '#ff0000',
              letterSpacing: 6,
              marginBottom: 20,
              opacity: sp(30),
            }}
          >
            एहसास
          </div>
          <div
            style={{
              fontSize: 180,
              lineHeight: 1,
              filter: `blur(6px)`,
              opacity: 0.3,
            }}
          >
            <svg width="200" height="200" viewBox="0 0 100 100">
              <circle cx="50" cy="50" r="45" fill="none" stroke="#9E9E9E" strokeWidth="4" />
              <path d="M30 35 Q50 20 70 35" fill="none" stroke="#9E9E9E" strokeWidth="4" />
              <line x1="35" y1="65" x2="65" y2="65" stroke="#9E9E9E" strokeWidth="4" />
            </svg>
          </div>
          <div
            style={{
              fontSize: 50,
              fontFamily: 'Noto Sans Devanagari',
              color: '#9E9E9E',
              marginTop: 30,
              opacity: sp(40) * 0.7,
              lineHeight: 1.4,
            }}
          >
            "लगता है ठीक है..."
            <br />
            "शायद काफी है..."
            <br />
            "पता नहीं..."
          </div>
        </div>
      </AbsoluteFill>

      {/* === RIGHT SIDE — CLARITY / NUMBERS === */}
      <AbsoluteFill
        style={{
          clipPath: `inset(0 0 0 ${dividerX}px)`,
          background: 'linear-gradient(135deg, #001420 0%, #002040 100%)',
        }}
      >
        <div
          style={{
            position: 'absolute',
            top: 160,
            left: dividerX + 60,
            width: 860,
          }}
        >
          <div
            style={{
              fontSize: 48,
              fontFamily: 'Inter',
              fontWeight: 800,
              color: '#e9d700',
              letterSpacing: 6,
              marginBottom: 30,
              opacity: sp(75),
            }}
          >
            साफ तस्वीर
          </div>

          {/* Data rows */}
          {[
            { label: 'कुल बचत', value: '₹1.2 Cr', color: '#e9d700' },
            { label: 'सालाना खर्च', value: '₹8.4L', color: '#F5F5F5' },
            { label: 'पेंशन आय', value: '₹3.6L/yr', color: '#FFC107' },
            { label: 'सुरक्षित वर्ष', value: '28+', color: '#00FF88' },
          ].map((row, i) => {
            const rowSp = spring({
              frame: Math.max(0, frame - (80 + i * 12)),
              fps,
              config: { damping: 14, mass: 0.35, stiffness: 110 },
            });
            const barWidth = interpolate(frame, [90 + i * 12, 140 + i * 12], [0, 300 + i * 50], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            });
            return (
              <div
                key={i}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  marginBottom: 28,
                  opacity: rowSp,
                  transform: `translateX(${(1 - rowSp) * 40}px)`,
                }}
              >
                <div
                  style={{
                    fontSize: 36,
                    fontFamily: 'Noto Sans Devanagari',
                    color: '#9E9E9E',
                    width: 200,
                    flexShrink: 0,
                  }}
                >
                  {row.label}
                </div>
                <div
                  style={{
                    height: 8,
                    width: barWidth,
                    background: row.color,
                    borderRadius: 4,
                    margin: '0 20px',
                  }}
                />
                <div
                  style={{
                    fontSize: 52,
                    fontFamily: 'JetBrains Mono',
                    fontWeight: 700,
                    color: row.color,
                  }}
                >
                  {row.value}
                </div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* === DIVIDER LINE === */}
      <div
        style={{
          position: 'absolute',
          left: dividerX - 2,
          top: 0,
          width: 4,
          height: 1080,
          background: 'linear-gradient(180deg, transparent, #e9d700, #e9d700, transparent)',
          boxShadow: '0 0 20px #e9d700',
        }}
      />

      {/* === SHAPE LAYERS === */}
      <AbsoluteFill style={{ opacity: 0.04, pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <rect
            x="50"
            y="50"
            width="300"
            height="300"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            transform={`rotate(${frame * 0.1} 200 200)`}
          />
          <circle
            cx={1800}
            cy={900}
            r={200 + Math.sin(frame * 0.01) * 10}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 25 }).map((_, i) => {
            const px = seededRandom(i * 13) * 1920;
            const py = (seededRandom(i * 17) * 1080 + frame * 0.5) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={1 + seededRandom(i) * 2}
                fill={i % 2 === 0 ? '#e9d700' : '#F5F5F5'}
                opacity={0.12}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(35) }}>
          <g transform={`translate(480, ${880 + Math.sin(frame * 0.05) * 2})`}>
            <circle cx="0" cy="-130" r="50" fill="#e9d700" opacity="0.1" />
            <circle
              cx="0"
              cy="-130"
              r="26"
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 4} 0 -130)`}
            />
            <ellipse cx="-9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <ellipse cx="9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="0" y1="-104" x2="0" y2="-42" stroke="#9E9E9E" strokeWidth="3" />
            <line x1="0" y1="-82" x2="-38" y2="-58" stroke="#9E9E9E" strokeWidth="3" />
            <line x1="0" y1="-82" x2="38" y2="-58" stroke="#9E9E9E" strokeWidth="3" />
            <line x1="0" y1="-42" x2="-20" y2="0" stroke="#9E9E9E" strokeWidth="3" />
            <line x1="0" y1="-42" x2="20" y2="0" stroke="#9E9E9E" strokeWidth="3" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig2" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.9" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig2)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_Ch7_FogVsClarity;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene3_Ch7_ThreeQuestions',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_Ch7_ThreeQuestions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const questions = [
    { q: 'आपके पास क्या है?', icon: 'M', color: '#e9d700', delay: 20 },
    { q: 'आप क्या खर्च करते हैं?', icon: '↗', color: '#ff0000', delay: 50 },
    { q: 'आय अभी भी क्या है?', icon: '⟳', color: '#FFC107', delay: 80 },
  ];

  // Orbit animation for icons
  const orbitAngle = frame * 0.8;

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      {/* === BACKGROUND === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="bg3" cx="50%" cy="50%" r="60%">
              <stop offset="0%" stopColor="#0D1A0D" stopOpacity="1" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="1" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg3)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Triangle constellation === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {/* Large triangle */}
          <polygon
            points="960,100 200,900 1720,900"
            fill="none"
            stroke="#e9d700"
            strokeWidth="1.5"
            strokeDasharray="20,10"
            strokeDashoffset={frame * 0.5}
          />
          {/* Inner triangle */}
          <polygon
            points="960,320 430,780 1490,780"
            fill="none"
            stroke="#FFC107"
            strokeWidth="1"
          />
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — Dots grid === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 15 }).map((_, row) =>
            Array.from({ length: 25 }).map((_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 80 + 40}
                cy={row * 72 + 36}
                r="2"
                fill="#F5F5F5"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 30 }).map((_, i) => {
            const px = seededRandom(i * 7) * 1920;
            const py = (seededRandom(i * 11) * 1080 + frame * 0.4) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i * 3) * 2.5}
                fill={i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#FFC107' : '#F5F5F5'}
                opacity={0.15}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === HEADER === */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontSize: 34,
          fontFamily: 'Inter',
          fontWeight: 700,
          color: '#e9d700',
          letterSpacing: 8,
          opacity: sp(5),
        }}
      >
        3 सवाल जो आपको पता होने चाहिए
      </div>

      {/* === THREE QUESTION CARDS === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 60,
          paddingTop: 60,
        }}
      >
        {questions.map((item, i) => {
          const cardSp = spring({
            frame: Math.max(0, frame - item.delay),
            fps,
            config: { damping: 10, mass: 0.5, stiffness: 100 },
          });
          // Breathing effect
          const breathe = 1 + Math.sin(frame * 0.06 + i * 2) * 0.01;

          return (
            <div
              key={i}
              style={{
                width: 480,
                background: '#2D2D2D',
                borderRadius: 24,
                padding: '48px 40px',
                border: `2px solid ${item.color}`,
                transform: `translateY(${(1 - cardSp) * 60}px) scale(${cardSp * breathe})`,
                opacity: cardSp,
                boxShadow: `0 0 40px ${item.color}22`,
                position: 'relative',
                overflow: 'hidden',
              }}
            >
              {/* Card number */}
              <div
                style={{
                  position: 'absolute',
                  top: 20,
                  right: 28,
                  fontSize: 80,
                  fontFamily: 'JetBrains Mono',
                  fontWeight: 900,
                  color: item.color,
                  opacity: 0.12,
                  lineHeight: 1,
                }}
              >
                {i + 1}
              </div>

              {/* Icon Circle */}
              <div
                style={{
                  width: 80,
                  height: 80,
                  borderRadius: '50%',
                  backgroundColor: `${item.color}22`,
                  border: `2px solid ${item.color}`,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  marginBottom: 28,
                  fontSize: 36,
                  color: item.color,
                  fontFamily: 'JetBrains Mono',
                  fontWeight: 900,
                }}
              >
                {i === 0 && (
                  <svg width="36" height="36" viewBox="0 0 36 36" fill="none">
                    <rect x="4" y="8" width="28" height="4" rx="2" fill={item.color} />
                    <rect x="4" y="16" width="20" height="4" rx="2" fill={item.color} />
                    <rect x="4" y="24" width="24" height="4" rx="2" fill={item.color} />
                  </svg>
                )}
                {i === 1 && (
                  <svg width="36" height="36" viewBox="0 0 36 36" fill="none">
                    <path d="M8 28 L28 8 M28 8 L14 8 M28 8 L28 22" stroke={item.color} strokeWidth="3" strokeLinecap="round" />
                  </svg>
                )}
                {i === 2 && (
                  <svg width="36" height="36" viewBox="0 0 36 36" fill="none">
                    <path d="M18 6 A12 12 0 1 1 6 18" stroke={item.color} strokeWidth="3" strokeLinecap="round" fill="none" />
                    <path d="M6 18 L2 14 M6 18 L10 14" stroke={item.color} strokeWidth="3" strokeLinecap="round" />
                  </svg>
                )}
              </div>

              {/* Question text */}
              <div
                style={{
                  fontSize: 46,
                  fontFamily: 'Noto Sans Devanagari',
                  fontWeight: 700,
                  color: '#F5F5F5',
                  lineHeight: 1.3,
                }}
              >
                {item.q}
              </div>

              {/* Bottom accent line */}
              <div
                style={{
                  marginTop: 24,
                  height: 3,
                  background: `linear-gradient(90deg, ${item.color}, transparent)`,
                  borderRadius: 2,
                  width: `${spring({
                    frame: Math.max(0, frame - item.delay - 20),
                    fps,
                    config: { damping: 15, stiffness: 80 },
                  }) * 100}%`,
                }}
              />
            </div>
          );
        })}
      </AbsoluteFill>

      {/* === ORBITING ELEMENT === */}
      <AbsoluteFill style={{ pointerEvents: 'none', opacity: 0.15 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[0, 120, 240].map((offset, i) => {
            const angle = ((orbitAngle + offset) * Math.PI) / 180;
            const cx = 960 + Math.cos(angle) * 480;
            const cy = 540 + Math.sin(angle) * 80;
            return (
              <circle key={i} cx={cx} cy={cy} r="6" fill="#e9d700" opacity="0.5" />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(100) }}>
          <g transform={`translate(140, ${860 + Math.sin(frame * 0.05) * 2})`}>
            <circle cx="0" cy="-130" r="48" fill="#e9d700" opacity="0.1" />
            <circle
              cx="0"
              cy="-130"
              r="26"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 0 -130)`}
            />
            <ellipse cx="-9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <ellipse cx="9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="0" y1="-104" x2="0" y2="-42" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arm pointing right at cards */}
            <line x1="0" y1="-80" x2="45" y2="-65" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-80" x2="-30" y2="-60" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-42" x2="-20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-42" x2="20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig3" cx="50%" cy="50%" r="70%">
              <stop offset="60%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.85" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig3)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_Ch7_ThreeQuestions;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene4_Ch7_FactsDashboard',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_Ch7_FactsDashboard: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Counter animations
  const counterProgress = interpolate(frame, [30, 100], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: (t) => t * (2 - t),
  });

  const metrics = [
    { label: 'कुल बचत', value: 120, unit: 'L', color: '#e9d700', barMax: 100 },
    { label: 'सालाना खर्च', value: 8.4, unit: 'L', color: '#ff0000', barMax: 20 },
    { label: 'पेंशन आय', value: 3.6, unit: 'L/yr', color: '#FFC107', barMax: 10 },
    { label: 'निवल सम्पत्ति', value: 1.2, unit: 'Cr', color: '#F5F5F5', barMax: 2 },
  ];

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      {/* === BACKGROUND — Dark blue finance theme === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <linearGradient id="bg4" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor="#050F1A" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg4)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Dashboard hexagon grid === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 5 }).map((_, i) =>
            Array.from({ length: 8 }).map((_, j) => {
              const hx = j * 240 + (i % 2) * 120;
              const hy = i * 180;
              const r = 80;
              const points = Array.from({ length: 6 })
                .map((_, k) => {
                  const a = (k * 60 - 30) * (Math.PI / 180);
                  return `${hx + r * Math.cos(a)},${hy + r * Math.sin(a)}`;
                })
                .join(' ');
              return (
                <polygon
                  key={`${i}-${j}`}
                  points={points}
                  fill="none"
                  stroke="#e9d700"
                  strokeWidth="0.5"
                />
              );
            })
          )}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 40 }).map((_, i) => {
            const px = seededRandom(i * 9) * 1920;
            const py = (seededRandom(i * 13) * 1080 + frame * 0.3) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i) * 2}
                fill="#e9d700"
                opacity={0.08 + seededRandom(i * 5) * 0.1}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === HEADLINE === */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 100,
          fontSize: 52,
          fontFamily: 'Inter',
          fontWeight: 900,
          color: '#e9d700',
          letterSpacing: 4,
          opacity: sp(5),
        }}
      >
        FINANCIAL CLARITY DASHBOARD
      </div>
      <div
        style={{
          position: 'absolute',
          top: 128,
          left: 100,
          fontSize: 42,
          fontFamily: 'Noto Sans Devanagari',
          color: '#9E9E9E',
          opacity: sp(15),
        }}
      >
        तथ्यों को वैसे देखें जैसे वो हैं
      </div>

      {/* === METRICS GRID === */}
      <div
        style={{
          position: 'absolute',
          top: 220,
          left: 100,
          right: 100,
          display: 'grid',
          gridTemplateColumns: '1fr 1fr',
          gap: 32,
        }}
      >
        {metrics.map((m, i) => {
          const mSp = spring({
            frame: Math.max(0, frame - (25 + i * 18)),
            fps,
            config: { damping: 11, mass: 0.45, stiffness: 105 },
          });
          const barWidth = interpolate(
            frame,
            [40 + i * 18, 110 + i * 18],
            [0, (m.value / m.barMax) * 100],
            { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
          );
          const displayVal = (m.value * counterProgress).toFixed(
            m.value < 10 ? 1 : 0
          );

          return (
            <div
              key={i}
              style={{
                background: '#2D2D2D',
                borderRadius: 20,
                padding: '36px 40px',
                borderLeft: `5px solid ${m.color}`,
                transform: `translateY(${(1 - mSp) * 40}px)`,
                opacity: mSp,
                boxShadow: `0 8px 40px ${m.color}11`,
              }}
            >
              <div
                style={{
                  fontSize: 30,
                  fontFamily: 'Noto Sans Devanagari',
                  color: '#9E9E9E',
                  marginBottom: 12,
                }}
              >
                {m.label}
              </div>
              <div
                style={{
                  fontSize: 90,
                  fontFamily: 'JetBrains Mono',
                  fontWeight: 900,
                  color: m.color,
                  lineHeight: 1,
                  marginBottom: 16,
                }}
              >
                ₹{displayVal}
                <span style={{ fontSize: 40 }}>{m.unit}</span>
              </div>
              {/* Bar */}
              <div
                style={{
                  height: 8,
                  backgroundColor: '#1C1C1C',
                  borderRadius: 4,
                  overflow: 'hidden',
                }}
              >
                <div
                  style={{
                    height: '100%',
                    width: `${Math.min(barWidth, 100)}%`,
                    background: `linear-gradient(90deg, ${m.color}88, ${m.color})`,
                    borderRadius: 4,
                  }}
                />
              </div>
            </div>
          );
        })}
      </div>

      {/* === RIGHT SIDE — Summary insight === */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          bottom: 100,
          width: 500,
          textAlign: 'right',
          opacity: sp(100),
          transform: `translateX(${(1 - sp(100)) * 30}px)`,
        }}
      >
        <div
          style={{
            fontSize: 38,
            fontFamily: 'Noto Sans Devanagari',
            color: '#F5F5F5',
            lineHeight: 1.5,
          }}
        >
          ये अमूर्त सवाल नहीं हैं
          <br />
          <span style={{ color: '#e9d700', fontWeight: 700 }}>
            ये मानसिक शांति की नींव है
          </span>
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(80) }}>
          <g transform={`translate(1820, ${900 + Math.sin(frame * 0.05) * 3})`}>
            <circle cx="0" cy="-130" r="50" fill="#e9d700" opacity="0.08" />
            <circle
              cx="0"
              cy="-130"
              r="26"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 0 -130)`}
            />
            <ellipse cx="-9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <ellipse cx="9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="0" y1="-104" x2="0" y2="-42" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-82" x2="-38" y2="-65" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-82" x2="30" y2="-60" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-42" x2="-20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-42" x2="20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig4" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.88" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig4)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_Ch7_FactsDashboard;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene5_Ch7_ThreeLayers',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_Ch7_ThreeLayers: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Stacked pyramid layers
  const layers = [
    {
      num: '1',
      title: 'मूल जीवन खर्च',
      sub: 'खाना, घर, दवाई, बिजली',
      color: '#F5F5F5',
      width: 1100,
      delay: 15,
    },
    {
      num: '2',
      title: 'सुरक्षा कुशन',
      sub: 'स्वास्थ्य आपात + अप्रत्याशित खर्च',
      color: '#FFC107',
      width: 780,
      delay: 40,
    },
    {
      num: '3',
      title: 'जीने के लिए पैसा',
      sub: 'अनुभव · आराम · प्रियजन',
      color: '#e9d700',
      width: 500,
      delay: 70,
    },
  ];

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      {/* === BACKGROUND === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <linearGradient id="bg5" x1="0%" y1="100%" x2="0%" y2="0%">
              <stop offset="0%" stopColor="#080808" />
              <stop offset="100%" stopColor="#1A1A2E" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg5)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Horizontal lines (pyramid feel) === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 30 }).map((_, i) => (
            <line
              key={i}
              x1="0"
              y1={i * 36 + Math.sin(frame * 0.01 + i * 0.3) * 3}
              x2="1920"
              y2={i * 36 + Math.sin(frame * 0.01 + i * 0.3) * 3}
              stroke="#F5F5F5"
              strokeWidth="0.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 28 }).map((_, i) => {
            const px = seededRandom(i * 11) * 1920;
            const py = (seededRandom(i * 17) * 1080 + frame * 0.35) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i) * 2.5}
                fill={i % 2 === 0 ? '#e9d700' : '#F5F5F5'}
                opacity={0.1}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === HEADLINE === */}
      <div
        style={{
          position: 'absolute',
          top: 55,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontSize: 44,
          fontFamily: 'Inter',
          fontWeight: 900,
          color: '#e9d700',
          letterSpacing: 6,
          opacity: sp(5),
        }}
      >
        पैसे की 3 परतें — PYRAMID APPROACH
      </div>

      {/* === PYRAMID LAYERS === */}
      <div
        style={{
          position: 'absolute',
          top: 160,
          left: 0,
          right: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 16,
        }}
      >
        {/* Reversed for pyramid — widest at bottom */}
        {[...layers].reverse().map((layer, ri) => {
          const i = layers.length - 1 - ri;
          const layerSp = spring({
            frame: Math.max(0, frame - layer.delay),
            fps,
            config: { damping: 11, mass: 0.5, stiffness: 95 },
          });
          // Width animates in
          const animW = layerSp * layer.width;

          return (
            <div key={i} style={{ position: 'relative', width: animW, overflow: 'hidden' }}>
              <div
                style={{
                  width: '100%',
                  background: `linear-gradient(90deg, ${layer.color}11, ${layer.color}33, ${layer.color}11)`,
                  border: `2px solid ${layer.color}`,
                  borderRadius: 12,
                  padding: '28px 48px',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 32,
                  opacity: layerSp,
                }}
              >
                {/* Layer number */}
                <div
                  style={{
                    fontSize: 80,
                    fontFamily: 'JetBrains Mono',
                    fontWeight: 900,
                    color: layer.color,
                    lineHeight: 1,
                    minWidth: 60,
                  }}
                >
                  {layer.num}
                </div>

                <div style={{ flex: 1 }}>
                  <div
                    style={{
                      fontSize: 54,
                      fontFamily: 'Noto Sans Devanagari',
                      fontWeight: 700,
                      color: '#F5F5F5',
                    }}
                  >
                    {layer.title}
                  </div>
                  <div
                    style={{
                      fontSize: 34,
                      fontFamily: 'Noto Sans Devanagari',
                      color: '#9E9E9E',
                      marginTop: 6,
                    }}
                  >
                    {layer.sub}
                  </div>
                </div>

                {/* Right badge */}
                {i === 2 && (
                  <div
                    style={{
                      background: '#e9d700',
                      borderRadius: 8,
                      padding: '8px 20px',
                      fontSize: 28,
                      fontFamily: 'Inter',
                      fontWeight: 800,
                      color: '#1C1C1C',
                      whiteSpace: 'nowrap',
                    }}
                  >
                    यही असली खेल है
                  </div>
                )}
              </div>
            </div>
          );
        })}
      </div>

      {/* === 4% RULE NOTE === */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(105),
          transform: `translateY(${(1 - sp(105)) * 20}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            background: '#2D2D2D',
            border: '1px solid #e9d70044',
            borderRadius: 12,
            padding: '14px 40px',
            fontSize: 36,
            fontFamily: 'Noto Sans Devanagari',
            color: '#9E9E9E',
          }}
        >
          4% नियम — शुरुआत का बिंदु है, पवित्र कानून नहीं
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(50) }}>
          <g transform={`translate(1820, ${920 + Math.sin(frame * 0.05) * 2})`}>
            <circle cx="0" cy="-130" r="50" fill="#e9d700" opacity="0.1" />
            <circle
              cx="0"
              cy="-130"
              r="26"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3} 0 -130)`}
            />
            <ellipse cx="-9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <ellipse cx="9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="0" y1="-104" x2="0" y2="-42" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-82" x2="-35" y2="-60" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-82" x2="30" y2="-58" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-42" x2="-20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-42" x2="20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig5" cx="50%" cy="50%" r="70%">
              <stop offset="60%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.9" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig5)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_Ch7_ThreeLayers;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene6_Ch7_SpendNow',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_Ch7_SpendNow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Warm golden scene — positive energy
  const pulse = 1 + Math.sin(frame * 0.08) * 0.015;

  const cards = [
    {
      icon: 'TRAVEL',
      title: 'वो यात्रा',
      body: 'जो आप 20 साल से टाल रहे हैं',
      delay: 20,
      pos: { x: 120, y: 200 },
    },
    {
      icon: 'BED',
      title: 'बेहतर बिस्तर',
      body: '10 साल की अच्छी नींद',
      delay: 40,
      pos: { x: 120, y: 480 },
    },
    {
      icon: 'CHAIR',
      title: 'अच्छी कुर्सी',
      body: 'कम दर्द, ज़्यादा धैर्य',
      delay: 60,
      pos: { x: 120, y: 740 },
    },
  ];

  const rightCards = [
    {
      icon: 'FAMILY',
      title: 'परिवार को दो',
      body: 'जब वो 35 पर हों, 65 पर नहीं',
      delay: 35,
    },
    {
      icon: 'HOME',
      title: 'घर सुधारो',
      body: 'सुरक्षित, आरामदायक, रहने योग्य',
      delay: 55,
    },
    {
      icon: 'HEALTH',
      title: 'स्वास्थ्य पर',
      body: 'अभी निवेश करो, बाद में नहीं',
      delay: 75,
    },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${pulse})`,
      }}
    >
      {/* === BACKGROUND — Warm amber glow === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="bg6" cx="50%" cy="50%" r="65%">
              <stop offset="0%" stopColor="#1A1000" stopOpacity="1" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="1" />
            </radialGradient>
            <radialGradient id="glow6" cx="50%" cy="50%" r="40%">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.08" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg6)" />
          <rect width="1920" height="1080" fill="url(#glow6)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Soft circles === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[200, 350, 500].map((r, i) => (
            <circle
              key={i}
              cx={960 + Math.sin(frame * 0.007 + i) * 20}
              cy={540 + Math.cos(frame * 0.009 + i) * 15}
              r={r + Math.sin(frame * 0.005 + i) * 5}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
          <rect
            x="50"
            y="50"
            width="150"
            height="150"
            fill="none"
            stroke="#FFC107"
            strokeWidth="1"
            transform={`rotate(${frame * 0.12} 125 125)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES — Golden dust === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 50 }).map((_, i) => {
            const px = seededRandom(i * 7) * 1920;
            const py = (seededRandom(i * 11) * 1080 + frame * (0.2 + seededRandom(i) * 0.5)) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i * 3) * 2}
                fill="#e9d700"
                opacity={0.08 + seededRandom(i * 5) * 0.15}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === CENTRAL HEADLINE === */}
      <div
        style={{
          position: 'absolute',
          top: 0,
          left: 0,
          right: 0,
          bottom: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <div
          style={{
            fontSize: 44,
            fontFamily: 'Inter',
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 8,
            textTransform: 'uppercase',
            opacity: sp(10),
            marginBottom: 20,
          }}
        >
          अभी खर्च करो — सही तरीके से
        </div>
        <div
          style={{
            fontSize: 140,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            background: 'linear-gradient(135deg, #e9d700, #FFC107, #e9d700)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent',
            textAlign: 'center',
            lineHeight: 1.1,
            transform: `scale(${sp(20)})`,
            opacity: sp(20),
          }}
        >
          अनुभव
        </div>
        <div
          style={{
            fontSize: 70,
            fontFamily: 'Noto Sans Devanagari',
            color: '#F5F5F5',
            opacity: sp(35),
            transform: `translateY(${(1 - sp(35)) * 20}px)`,
            textAlign: 'center',
          }}
        >
          आराम · परिवार
        </div>

        {/* Divider */}
        <div
          style={{
            marginTop: 30,
            width: `${sp(50) * 500}px`,
            height: 2,
            background: 'linear-gradient(90deg, transparent, #e9d700, transparent)',
          }}
        />

        <div
          style={{
            fontSize: 44,
            fontFamily: 'Noto Sans Devanagari',
            color: '#9E9E9E',
            marginTop: 24,
            opacity: sp(65),
            textAlign: 'center',
            maxWidth: 900,
            lineHeight: 1.5,
          }}
        >
          ये फिज़ूलखर्ची नहीं है
          <br />
          <span style={{ color: '#e9d700' }}>ये पैसा अपना काम कर रहा है</span>
        </div>
      </div>

      {/* === LEFT ICONS (SVG) === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {/* Travel airplane icon */}
          <g
            opacity={sp(20)}
            transform={`translate(80, 180) scale(${sp(20)})`}
          >
            <circle cx="30" cy="30" r="28" fill="#e9d70022" stroke="#e9d700" strokeWidth="2" />
            <path d="M15 30 L30 18 L45 30 L30 35 Z" fill="#e9d700" />
            <line x1="22" y1="30" x2="38" y2="30" stroke="#e9d700" strokeWidth="2" />
          </g>
          {/* Bed icon */}
          <g
            opacity={sp(40)}
            transform={`translate(80, 390) scale(${sp(40)})`}
          >
            <circle cx="30" cy="30" r="28" fill="#FFC10722" stroke="#FFC107" strokeWidth="2" />
            <rect x="12" y="22" width="36" height="16" rx="3" fill="none" stroke="#FFC107" strokeWidth="2" />
            <rect x="18" y="26" width="12" height="8" rx="2" fill="#FFC107" opacity="0.5" />
          </g>
          {/* Family icon */}
          <g
            opacity={sp(60)}
            transform={`translate(80, 600) scale(${sp(60)})`}
          >
            <circle cx="30" cy="30" r="28" fill="#F5F5F522" stroke="#F5F5F5" strokeWidth="2" />
            <circle cx="22" cy="24" r="6" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            <circle cx="38" cy="24" r="6" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            <path d="M12 44 Q22 36 30 40 Q38 36 48 44" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === STICK FIGURE — Happy pose === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(45) }}>
          <g transform={`translate(1780, ${840 + Math.sin(frame * 0.07) * 4})`}>
            {/* Glow */}
            <circle cx="0" cy="-130" r="70" fill="#e9d700" opacity="0.1" />
            <circle
              cx="0"
              cy="-130"
              r="28"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 4} 0 -130)`}
            />
            {/* Smile */}
            <path
              d="M-8 -125 Q0 -118 8 -125"
              fill="none"
              stroke="#e9d700"
              strokeWidth="2"
              strokeLinecap="round"
            />
            <ellipse cx="-9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <ellipse cx="9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="0" y1="-102" x2="0" y2="-38" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms up — celebration */}
            <line x1="0" y1="-75" x2="-45" y2="-105" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-75" x2="45" y2="-105" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-38" x2="-22" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-38" x2="22" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            {/* Star sparkles */}
            <circle cx="-50" cy="-115" r="4" fill="#e9d700" opacity={0.6 + Math.sin(frame * 0.15) * 0.4} />
            <circle cx="50" cy="-115" r="4" fill="#e9d700" opacity={0.6 + Math.sin(frame * 0.15 + 2) * 0.4} />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig6" cx="50%" cy="50%" r="65%">
              <stop offset="50%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.88" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig6)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_Ch7_SpendNow;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene7_Ch7_GivingAlive',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_Ch7_GivingAlive: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Money flow animation — from parent figure to child figure
  const flowProgress = interpolate(frame, [30, 100], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: (t) => t * (2 - t),
  });

  // Ripple waves from giving
  const ripple1 = interpolate(frame, [60, 120], [0, 200], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const ripple1Op = interpolate(frame, [60, 90, 120], [0.8, 0.4, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const ripple2 = interpolate(frame, [75, 140], [0, 200], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const ripple2Op = interpolate(frame, [75, 105, 140], [0.8, 0.4, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Impact text
  const impactItems = [
    { text: 'बेहतर इलाके में घर', delay: 80 },
    { text: 'पोते-पोती की पढ़ाई', delay: 95 },
    { text: 'जब जरूरत हो — तब मदद', delay: 110 },
  ];

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      {/* === BACKGROUND — Deep purple/legacy feel === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="bg7" cx="40%" cy="50%" r="70%">
              <stop offset="0%" stopColor="#0D0A1A" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg7)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Connection arcs === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[100, 200, 300, 400].map((r, i) => (
            <circle
              key={i}
              cx={700}
              cy={540}
              r={r + Math.sin(frame * 0.01 + i) * 5}
              fill="none"
              stroke="#FFC107"
              strokeWidth="0.5"
            />
          ))}
          {/* Diagonal shape */}
          <polygon
            points="1600,100 1900,100 1900,400 1600,400"
            fill="none"
            stroke="#e9d700"
            strokeWidth="0.5"
            transform={`rotate(${frame * 0.05} 1750 250)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 30 }).map((_, i) => {
            const px = seededRandom(i * 9) * 1920;
            const py = (seededRandom(i * 15) * 1080 + frame * 0.4) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i) * 2}
                fill={i % 2 === 0 ? '#FFC107' : '#e9d700'}
                opacity={0.1}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === TOP LABEL === */}
      <div
        style={{
          position: 'absolute',
          top: 55,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontSize: 38,
          fontFamily: 'Inter',
          fontWeight: 800,
          color: '#FFC107',
          letterSpacing: 6,
          opacity: sp(5),
        }}
      >
        जीवित रहते हुए देना — GIVING WHILE ALIVE
      </div>

      {/* === PARENT STICK FIGURE (LEFT) === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(15) }}>
          {/* Parent — Golden glow, glasses */}
          <g transform={`translate(420, 540)`}>
            <circle cx="0" cy="-130" r="65" fill="#e9d700" opacity="0.12" />
            <circle cx="0" cy="-130" r="30" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <ellipse cx="-11" cy="-130" rx="10" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <ellipse cx="11" cy="-130" rx="10" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="0" y1="-100" x2="0" y2="-30" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Arm extending — giving gesture */}
            <line
              x1="0"
              y1="-70"
              x2={60 + flowProgress * 80}
              y2="-50"
              stroke="#F5F5F5"
              strokeWidth="3"
            />
            <line x1="0" y1="-70" x2="-40" y2="-50" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-30" x2="-24" y2="20" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-30" x2="24" y2="20" stroke="#F5F5F5" strokeWidth="3" />
            {/* Label */}
            <text x="0" y="50" textAnchor="middle" fill="#9E9E9E" fontSize="28" fontFamily="Noto Sans Devanagari">
              माता-पिता (65+)
            </text>
          </g>

          {/* Ripples from parent */}
          <circle cx="420" cy="540" r={ripple1} fill="none" stroke="#e9d700" strokeWidth="2" opacity={ripple1Op} />
          <circle cx="420" cy="540" r={ripple2} fill="none" stroke="#FFC107" strokeWidth="2" opacity={ripple2Op} />

          {/* Money flow particles along arc */}
          {Array.from({ length: 5 }).map((_, i) => {
            const t = (flowProgress - i * 0.15 + 1) % 1;
            const mx = 420 + t * (1100 - 420);
            const my = 540 + Math.sin(t * Math.PI) * (-120);
            return t > 0 && t < 1 ? (
              <g key={i}>
                <circle cx={mx} cy={my} r="10" fill="#e9d700" opacity={0.7 - t * 0.3} />
                <text x={mx} y={my + 4} textAnchor="middle" fill="#1C1C1C" fontSize="10" fontWeight="bold">
                  ₹
                </text>
              </g>
            ) : null;
          })}

          {/* Child figure (RIGHT) */}
          <g transform={`translate(1100, 580)`} opacity={sp(30)}>
            <circle cx="0" cy="-110" r="50" fill="#FFC107" opacity="0.08" />
            <circle cx="0" cy="-110" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            {/* Smile */}
            <path d="M-7 -106 Q0 -100 7 -106" fill="none" stroke="#FFC107" strokeWidth="2" strokeLinecap="round" />
            <line x1="0" y1="-88" x2="0" y2="-35" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-65" x2="-35" y2="-50" stroke="#F5F5F5" strokeWidth="3" />
            {/* Receiving arm */}
            <line x1="0" y1="-65" x2="30" y2="-50" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-35" x2="-18" y2="10" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-35" x2="18" y2="10" stroke="#F5F5F5" strokeWidth="3" />
            <text x="0" y="35" textAnchor="middle" fill="#9E9E9E" fontSize="24" fontFamily="Noto Sans Devanagari">
              बेटा-बेटी (35)
            </text>
          </g>
        </svg>
      </AbsoluteFill>

      {/* === IMPACT ITEMS — Right side === */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          top: 200,
          width: 580,
        }}
      >
        <div
          style={{
            fontSize: 36,
            fontFamily: 'Inter',
            color: '#9E9E9E',
            letterSpacing: 4,
            marginBottom: 30,
            opacity: sp(60),
          }}
        >
          इसका असर
        </div>
        {impactItems.map((item, i) => {
          const iSp = spring({
            frame: Math.max(0, frame - item.delay),
            fps,
            config: { damping: 12, mass: 0.4, stiffness: 110 },
          });
          return (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 20,
                marginBottom: 24,
                opacity: iSp,
                transform: `translateX(${(1 - iSp) * 40}px)`,
              }}
            >
              <div
                style={{
                  width: 10,
                  height: 10,
                  borderRadius: '50%',
                  backgroundColor: '#e9d700',
                  flexShrink: 0,
                  boxShadow: '0 0 12px #e9d700',
                }}
              />
              <div
                style={{
                  fontSize: 48,
                  fontFamily: 'Noto Sans Devanagari',
                  fontWeight: 700,
                  color: '#F5F5F5',
                }}
              >
                {item.text}
              </div>
            </div>
          );
        })}
      </div>

      {/* === KEY INSIGHT === */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(120),
          transform: `translateY(${(1 - sp(120)) * 15}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            background: 'linear-gradient(90deg, #e9d70033, #e9d70011)',
            border: '1px solid #e9d70066',
            borderRadius: 12,
            padding: '16px 48px',
            fontSize: 44,
            fontFamily: 'Noto Sans Devanagari',
            color: '#e9d700',
            fontWeight: 700,
          }}
        >
          वही रुपया — सही समय पर — अलग दुनिया बना देता है
        </div>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig7" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.88" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig7)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_Ch7_GivingAlive;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene8_Ch7_Legacy',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_Ch7_Legacy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Two-column contrast: OLD DEFINITION vs NEW DEFINITION
  const divProgress = interpolate(frame, [15, 60], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: (t) => t * t * (3 - 2 * t),
  });

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      {/* === BACKGROUND === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="bg8" cx="50%" cy="50%" r="75%">
              <stop offset="0%" stopColor="#0A0A0A" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </radialGradient>
            <radialGradient id="glow8" cx="50%" cy="50%" r="30%">
              <stop offset="0%" stopColor="#FFC107" stopOpacity="0.05" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg8)" />
          <rect width="1920" height="1080" fill="url(#glow8)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Fading circles, legacy feel === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[80, 180, 280, 380, 480].map((r, i) => (
            <circle
              key={i}
              cx={960}
              cy={540}
              r={r + Math.sin(frame * 0.007 + i * 0.8) * 6}
              fill="none"
              stroke="#FFC107"
              strokeWidth="1"
              strokeDasharray={`${10 + i * 5} ${15 + i * 5}`}
              strokeDashoffset={frame * 0.3 * (i % 2 === 0 ? 1 : -1)}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 40 }).map((_, i) => {
            const px = seededRandom(i * 7) * 1920;
            const py = (seededRandom(i * 13) * 1080 + frame * 0.25) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i * 3) * 2.5}
                fill={i % 3 === 0 ? '#FFC107' : i % 3 === 1 ? '#e9d700' : '#F5F5F5'}
                opacity={0.08}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === OLD DEFINITION (LEFT — CROSSED OUT) === */}
      <div
        style={{
          position: 'absolute',
          left: 60,
          top: 120,
          width: 800,
          opacity: divProgress * 0.5,
        }}
      >
        <div
          style={{
            fontSize: 32,
            fontFamily: 'Inter',
            color: '#9E9E9E',
            letterSpacing: 6,
            marginBottom: 20,
          }}
        >
          पुरानी सोच
        </div>
        <div
          style={{
            fontSize: 72,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 700,
            color: '#9E9E9E',
            lineHeight: 1.3,
            position: 'relative',
          }}
        >
          विरासत = मरने के बाद
          <br />
          छोड़ा हुआ पैसा
          {/* Strike through */}
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: `${divProgress * 100}%`,
              height: 4,
              backgroundColor: '#ff0000',
              transform: 'translateY(-50%)',
            }}
          />
        </div>
      </div>

      {/* === NEW DEFINITION (RIGHT — HERO) === */}
      <div
        style={{
          position: 'absolute',
          right: 60,
          top: 80,
          width: 900,
          opacity: sp(45),
          transform: `translateX(${(1 - sp(45)) * 50}px)`,
        }}
      >
        <div
          style={{
            fontSize: 32,
            fontFamily: 'Inter',
            color: '#FFC107',
            letterSpacing: 6,
            marginBottom: 20,
          }}
        >
          असली मतलब
        </div>
        <div
          style={{
            fontSize: 80,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.2,
          }}
        >
          विरासत वो भी है
        </div>
        <div
          style={{
            fontSize: 80,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            background: 'linear-gradient(90deg, #FFC107, #e9d700)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent',
            lineHeight: 1.2,
          }}
        >
          जो आप संभव बनाते हैं
        </div>
        <div
          style={{
            fontSize: 54,
            fontFamily: 'Noto Sans Devanagari',
            color: '#9E9E9E',
            marginTop: 16,
            lineHeight: 1.4,
          }}
        >
          जब आप अभी भी यहाँ हैं —
          <br />
          <span style={{ color: '#FFC107' }}>इसे देखने के लिए</span>
        </div>
      </div>

      {/* === CENTRAL DIVIDER === */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: 100,
          width: 2,
          height: 700,
          background: 'linear-gradient(180deg, transparent, #9E9E9E44, transparent)',
          transform: `scaleY(${divProgress})`,
          transformOrigin: 'top',
        }}
      />

      {/* === BOTTOM IMPACT LINE === */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(90),
        }}
      >
        <div
          style={{
            fontSize: 52,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            color: '#e9d700',
          }}
        >
          पैसा अपना सबसे अच्छा काम तब करता है
        </div>
        <div
          style={{
            fontSize: 44,
            fontFamily: 'Noto Sans Devanagari',
            color: '#F5F5F5',
            marginTop: 8,
            opacity: 0.8,
          }}
        >
          जब वो सही पल से मिलता है
        </div>
      </div>

      {/* === STICK FIGURE — Contemplative === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(25) }}>
          <g transform={`translate(960, ${820 + Math.sin(frame * 0.04) * 2})`}>
            <circle cx="0" cy="-130" r="60" fill="#FFC107" opacity="0.08" />
            <circle
              cx="0"
              cy="-130"
              r="28"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.035) * 3} 0 -130)`}
            />
            <ellipse cx="-10" cy="-130" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <ellipse cx="10" cy="-130" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="0" y1="-102" x2="0" y2="-38" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms wide — embracing */}
            <line x1="0" y1="-75" x2="-60" y2="-55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-75" x2="60" y2="-55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-38" x2="-22" y2="8" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-38" x2="22" y2="8" stroke="#F5F5F5" strokeWidth="3" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig8" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.9" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig8)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_Ch7_Legacy;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene9_Ch7_MoneyJobsChange',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_Ch7_MoneyJobsChange: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Timeline progress line
  const timelineW = interpolate(frame, [20, 100], [0, 1500], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: (t) => t * (2 - t),
  });

  const phases = [
    {
      age: '20s-40s',
      label: 'बढ़ाओ',
      sub: 'बचाओ · निवेश करो · कंपाउंड करो',
      color: '#e9d700',
      x: 280,
      delay: 30,
      icon: '↑',
    },
    {
      age: '40s-60s',
      label: 'संतुलन बनाओ',
      sub: 'सुरक्षित करो · बढ़ाते रहो',
      color: '#FFC107',
      x: 960,
      delay: 60,
      icon: '⟺',
    },
    {
      age: '60s+',
      label: 'जीओ',
      sub: 'अनुभव · आराम · परिवार',
      color: '#F5F5F5',
      x: 1640,
      delay: 90,
      icon: '♦',
    },
  ];

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      {/* === BACKGROUND — Gradient timeline === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <linearGradient id="bg9" x1="0%" y1="0%" x2="100%" y2="0%">
              <stop offset="0%" stopColor="#0A1500" />
              <stop offset="50%" stopColor="#1C1C1C" />
              <stop offset="100%" stopColor="#1A1000" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg9)" />
        </svg>
      </AbsoluteFill>

      {/* === SHAPE LAYERS — Timeline markers === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 12 }).map((_, i) => (
            <rect
              key={i}
              x={i * 160 + 40}
              y={300 + seededRandom(i * 7) * 400}
              width={60 + seededRandom(i) * 60}
              height={2}
              fill="#F5F5F5"
            />
          ))}
          <polygon
            points="100,100 200,50 300,100 200,150"
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
            transform={`rotate(${frame * 0.1} 200 100)`}
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 35 }).map((_, i) => {
            const px = seededRandom(i * 11) * 1920;
            const py = (seededRandom(i * 17) * 1080 + frame * 0.3) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i * 3) * 2}
                fill={i % 2 === 0 ? '#e9d700' : '#F5F5F5'}
                opacity={0.08}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === HEADLINE === */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          fontSize: 48,
          fontFamily: 'Noto Sans Devanagari',
          fontWeight: 900,
          color: '#F5F5F5',
          opacity: sp(8),
        }}
      >
        पैसा जीवनकाल में{' '}
        <span style={{ color: '#e9d700' }}>नौकरियाँ बदलता है</span>
      </div>

      {/* === TIMELINE LINE === */}
      <div
        style={{
          position: 'absolute',
          top: 530,
          left: 210,
          height: 6,
          width: timelineW,
          background: 'linear-gradient(90deg, #e9d700, #FFC107, #F5F5F5)',
          borderRadius: 3,
          boxShadow: '0 0 20px #e9d70066',
        }}
      />

      {/* === PHASE NODES === */}
      {phases.map((phase, i) => {
        const nodeSp = spring({
          frame: Math.max(0, frame - phase.delay),
          fps,
          config: { damping: 10, mass: 0.45, stiffness: 100 },
        });
        const breathe = 1 + Math.sin(frame * 0.07 + i * 2) * 0.03;

        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: phase.x - 160,
              top: 180,
              width: 320,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              opacity: nodeSp,
              transform: `translateY(${(1 - nodeSp) * 30}px) scale(${breathe})`,
            }}
          >
            {/* Age badge */}
            <div
              style={{
                fontSize: 28,
                fontFamily: 'JetBrains Mono',
                fontWeight: 700,
                color: phase.color,
                letterSpacing: 3,
                marginBottom: 16,
              }}
            >
              {phase.age}
            </div>

            {/* Circle node */}
            <div
              style={{
                width: 100,
                height: 100,
                borderRadius: '50%',
                border: `3px solid ${phase.color}`,
                backgroundColor: `${phase.color}15`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontSize: 44,
                color: phase.color,
                boxShadow: `0 0 30px ${phase.color}33`,
                marginBottom: 20,
                fontFamily: 'JetBrains Mono',
              }}
            >
              {phase.icon}
            </div>

            {/* Main label */}
            <div
              style={{
                fontSize: 72,
                fontFamily: 'Noto Sans Devanagari',
                fontWeight: 900,
                color: phase.color,
                lineHeight: 1,
                marginBottom: 12,
                textAlign: 'center',
              }}
            >
              {phase.label}
            </div>

            {/* Sub text */}
            <div
              style={{
                fontSize: 30,
                fontFamily: 'Noto Sans Devanagari',
                color: '#9E9E9E',
                textAlign: 'center',
                lineHeight: 1.4,
              }}
            >
              {phase.sub}
            </div>

            {/* Connector dot on timeline */}
            <div
              style={{
                marginTop: 32,
                width: 20,
                height: 20,
                borderRadius: '50%',
                backgroundColor: phase.color,
                boxShadow: `0 0 20px ${phase.color}`,
              }}
            />
          </div>
        );
      })}

      {/* === BOTTOM TAGLINE === */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(100),
        }}
      >
        <div
          style={{
            fontSize: 44,
            fontFamily: 'Noto Sans Devanagari',
            color: '#9E9E9E',
            lineHeight: 1.5,
          }}
        >
          अनुशासन{' '}
          <span style={{ color: '#e9d700' }}>बनाता</span> है →{' '}
          <span style={{ color: '#FFC107' }}>सुरक्षित</span> होता है →{' '}
          <span style={{ color: '#F5F5F5' }}>जीता</span> है
        </div>
      </div>

      {/* === STICK FIGURE — Walking along timeline === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(20) }}>
          <g
            transform={`translate(${210 + Math.min(timelineW, 1500) * 0.5}, ${
              480 + Math.sin(frame * 0.12) * 4
            })`}
          >
            <circle cx="0" cy="-80" r="35" fill="#e9d700" opacity="0.1" />
            <circle cx="0" cy="-80" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
            <ellipse cx="-6" cy="-80" rx="6" ry="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <ellipse cx="6" cy="-80" rx="6" ry="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="0" y1="-62" x2="0" y2="-20" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="0" y1="-45" x2="-28" y2="-32" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="0" y1="-45" x2="28" y2="-32" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Walking legs */}
            <line
              x1="0"
              y1="-20"
              x2={-14 + Math.sin(frame * 0.2) * 8}
              y2="10"
              stroke="#F5F5F5"
              strokeWidth="2.5"
            />
            <line
              x1="0"
              y1="-20"
              x2={14 - Math.sin(frame * 0.2) * 8}
              y2="10"
              stroke="#F5F5F5"
              strokeWidth="2.5"
            />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig9" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.88" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig9)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_Ch7_MoneyJobsChange;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene10_Ch7_FinalStatement',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_Ch7_FinalStatement: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // EPIC final scene — full screen statement
  // Slow dramatic zoom
  const zoom = interpolate(frame, [0, durationInFrames], [1.0, 1.08], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Word by word reveal
  const words = [
    { text: 'सार्थक', color: '#e9d700', size: 240, delay: 5 },
    { text: 'ज़िंदगी', color: '#F5F5F5', size: 180, delay: 30 },
  ];

  // Bottom text stagger
  const lines = [
    { text: 'पैसा रखने के लिए नहीं —', delay: 70 },
    { text: 'जीने के लिए है', delay: 85, gold: true },
  ];

  // Gold burst
  const burstOp = interpolate(frame, [0, 20, 50], [0, 0.3, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `scale(${zoom})`,
      }}
    >
      {/* === BACKGROUND — Maximum impact === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="bg10" cx="50%" cy="45%" r="65%">
              <stop offset="0%" stopColor="#1A1200" />
              <stop offset="60%" stopColor="#0D0D0D" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg10)" />
        </svg>
      </AbsoluteFill>

      {/* === GOLD BURST at entrance === */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 45%, #e9d700, transparent 60%)',
          opacity: burstOp,
          pointerEvents: 'none',
        }}
      />

      {/* === SHAPE LAYERS — Radiating lines === */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 24 }).map((_, i) => {
            const angle = (i / 24) * Math.PI * 2;
            const length = 600 + Math.sin(frame * 0.01 + i * 0.5) * 30;
            return (
              <line
                key={i}
                x1={960}
                y1={480}
                x2={960 + Math.cos(angle) * length}
                y2={480 + Math.sin(angle) * length}
                stroke="#e9d700"
                strokeWidth="0.8"
                opacity={0.4}
              />
            );
          })}
          {/* Central ring */}
          <circle
            cx="960"
            cy="480"
            r={150 + Math.sin(frame * 0.05) * 5}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1"
          />
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES — Dense golden dust === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {Array.from({ length: 60 }).map((_, i) => {
            const px = seededRandom(i * 7) * 1920;
            const py = (seededRandom(i * 11) * 1080 + frame * (0.2 + seededRandom(i) * 0.3)) % 1080;
            return (
              <circle
                key={i}
                cx={px}
                cy={py}
                r={seededRandom(i * 3) * 3}
                fill="#e9d700"
                opacity={0.06 + seededRandom(i * 5) * 0.12}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === MOTION TEXTURE — Concentric rings === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[200, 350, 500, 650, 800].map((r, i) => (
            <circle
              key={i}
              cx={960}
              cy={540}
              r={r + ((frame * 0.5 + i * 40) % 160)}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
              opacity={1 - ((frame * 0.5 + i * 40) % 160) / 160}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === MAIN HERO TEXT === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          paddingBottom: 80,
        }}
      >
        {/* Chapter tag */}
        <div
          style={{
            fontSize: 30,
            fontFamily: 'Inter',
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 10,
            textTransform: 'uppercase',
            opacity: sp(5),
            marginBottom: 32,
          }}
        >
          Chapter 7 • Final Lesson
        </div>

        {/* सार्थक — massive */}
        <div
          style={{
            fontSize: 260,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            background: 'linear-gradient(135deg, #e9d700 0%, #FFC107 50%, #e9d700 100%)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent',
            lineHeight: 0.9,
            transform: `scale(${sp(10)})`,
            opacity: sp(10),
            filter: `drop-shadow(0 0 40px #e9d70066)`,
          }}
        >
          सार्थक
        </div>

        {/* ज़िंदगी */}
        <div
          style={{
            fontSize: 160,
            fontFamily: 'Noto Sans Devanagari',
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
            transform: `scale(${sp(28)})`,
            opacity: sp(28),
            marginBottom: 40,
          }}
        >
          ज़िंदगी
        </div>

        {/* Divider */}
        <div
          style={{
            width: `${sp(50) * 700}px`,
            height: 3,
            background: 'linear-gradient(90deg, transparent, #e9d700, transparent)',
            marginBottom: 32,
          }}
        />

        {/* Bottom lines */}
        {lines.map((line, i) => (
          <div
            key={i}
            style={{
              fontSize: 60,
              fontFamily: 'Noto Sans Devanagari',
              fontWeight: line.gold ? 900 : 400,
              color: line.gold ? '#e9d700' : '#9E9E9E',
              opacity: sp(line.delay),
              transform: `translateY(${(1 - sp(line.delay)) * 20}px)`,
              lineHeight: 1.5,
              textAlign: 'center',
            }}
          >
            {line.text}
          </div>
        ))}
      </AbsoluteFill>

      {/* === STICK FIGURE — Grand finale pose === */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: sp(60) }}>
          {/* Left figure */}
          <g transform={`translate(180, ${920 + Math.sin(frame * 0.05) * 2})`}>
            <circle cx="0" cy="-130" r="55" fill="#e9d700" opacity="0.12" />
            <circle cx="0" cy="-130" r="26" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <ellipse cx="-9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <ellipse cx="9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            {/* Smile */}
            <path d="M-7 -125 Q0 -118 7 -125" fill="none" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" />
            <line x1="0" y1="-104" x2="0" y2="-40" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arm up — victory */}
            <line x1="0" y1="-75" x2="-45" y2="-110" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-75" x2="35" y2="-60" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-40" x2="-20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-40" x2="20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            {/* Sparkle */}
            <circle cx="-48" cy="-120" r="5" fill="#e9d700" opacity={0.5 + Math.sin(frame * 0.18) * 0.5} />
          </g>

          {/* Right figure */}
          <g transform={`translate(1740, ${920 + Math.sin(frame * 0.05 + 1) * 2})`}>
            <circle cx="0" cy="-130" r="55" fill="#e9d700" opacity="0.12" />
            <circle cx="0" cy="-130" r="26" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <ellipse cx="-9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <ellipse cx="9" cy="-130" rx="8" ry="5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <path d="M-7 -125 Q0 -118 7 -125" fill="none" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" />
            <line x1="0" y1="-104" x2="0" y2="-40" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-75" x2="45" y2="-110" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-75" x2="-35" y2="-60" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-40" x2="-20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="0" y1="-40" x2="20" y2="0" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="48" cy="-120" r="5" fill="#e9d700" opacity={0.5 + Math.sin(frame * 0.18 + 1) * 0.5} />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === ProBuffet HQ BRANDING — Bottom center === */}
      <div
        style={{
          position: 'absolute',
          bottom: 40,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(110) * 0.5,
        }}
      >
        <div
          style={{
            fontSize: 28,
            fontFamily: 'Inter',
            fontWeight: 700,
            color: '#e9d700',
            letterSpacing: 6,
          }}
        >
          ProBuffet HQ
        </div>
      </div>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <defs>
            <radialGradient id="vig10" cx="50%" cy="50%" r="65%">
              <stop offset="45%" stopColor="#1C1C1C" stopOpacity="0" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.92" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig10)" />
        </svg>
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_Ch7_FinalStatement;
```

