```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_AlerteChoc24h',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_AlerteChoc24h: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera zoom effect
  const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], {
    extrapolateRight: 'clamp',
  });

  // Gradient animation
  const gradAngle = interpolate(frame, [0, durationInFrames], [135, 165], {
    extrapolateRight: 'clamp',
  });

  // Alarm pulse
  const alarmPulse = Math.sin(frame * 0.15) * 0.3 + 0.7;

  // Scissors animation
  const scissorAngle = interpolate(frame, [60, 90, 120, 150], [0, -15, 0, -15], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Screen shake on impact
  const shakeX = frame > 30 && frame < 50 ? Math.sin(frame * 2.5) * (50 - frame) * 0.4 : 0;
  const shakeY = frame > 30 && frame < 50 ? Math.cos(frame * 3) * (50 - frame) * 0.3 : 0;

  // "24H" counter animation
  const counterVal = interpolate(frame, [40, 100], [0, 24], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Timeline bar progress
  const timelineWidth = interpolate(frame, [120, 300], [0, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Date reveal
  const dateReveal = sp(20);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `scale(${cameraZoom}) translate(${shakeX}px, ${shakeY}px)`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #FF6600 0%, #CC4400 35%, #FF6600 65%, #E55500 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {/* Large drifting circle */}
          <circle
            cx={300 + Math.sin(frame * 0.008) * 60}
            cy={200 + Math.cos(frame * 0.006) * 40}
            r={180 + Math.sin(frame * 0.01) * 20}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={2}
            opacity={0.06}
          />
          {/* Drifting rectangle */}
          <rect
            x={1400 + Math.cos(frame * 0.007) * 50}
            y={600 + Math.sin(frame * 0.009) * 30}
            width={250}
            height={250}
            rx={20}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={2}
            opacity={0.05}
            transform={`rotate(${frame * 0.15}, ${1525 + Math.cos(frame * 0.007) * 50}, ${725 + Math.sin(frame * 0.009) * 30})`}
          />
          {/* Triangle shape */}
          <polygon
            points={`${960 + Math.sin(frame * 0.005) * 80},${100} ${1100 + Math.sin(frame * 0.005) * 80},${350} ${820 + Math.sin(frame * 0.005) * 80},${350}`}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.04}
          />
          {/* Diamond */}
          <polygon
            points={`${200},${700 + Math.cos(frame * 0.008) * 30} ${350},${850 + Math.cos(frame * 0.008) * 30} ${200},${1000 + Math.cos(frame * 0.008) * 30} ${50},${850 + Math.cos(frame * 0.008) * 30}`}
            fill="none"
            stroke="#FF6600"
            strokeWidth={2}
            opacity={0.07}
          />
        </svg>

        {/* Motion texture - diagonal lines grid */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.04 }}>
          {Array.from({ length: 20 }).map((_, i) => (
            <line
              key={`grid-${i}`}
              x1={i * 120 - 200 + frame * 0.3}
              y1={0}
              x2={i * 120 - 200 + frame * 0.3 - 400}
              y2={1080}
              stroke="#F5F5F5"
              strokeWidth={1}
            />
          ))}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 30 }).map((_, i) => {
          const px = seededRandom(i * 7 + 1) * 1920;
          const py = seededRandom(i * 13 + 3) * 1080;
          const speed = seededRandom(i * 5 + 2) * 0.5 + 0.2;
          const size = seededRandom(i * 11 + 4) * 4 + 2;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: (py + frame * speed * 1.5) % 1120 - 40,
                width: size,
                height: size,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
                opacity: seededRandom(i * 9 + 6) * 0.25 + 0.05,
              }}
            />
          );
        })}

        {/* === MAIN CONTENT === */}

        {/* Date badge - top left */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 80,
          transform: `translateY(${interpolate(dateReveal, [0, 1], [-60, 0])}px)`,
          opacity: dateReveal,
          display: 'flex',
          alignItems: 'center',
          gap: 14,
        }}>
          <div style={{
            width: 12,
            height: 12,
            borderRadius: '50%',
            backgroundColor: '#ff0000',
            boxShadow: `0 0 ${12 + alarmPulse * 10}px #ff0000`,
            opacity: alarmPulse,
          }} />
          <span style={{
            fontFamily: 'Space Mono, monospace',
            fontSize: 28,
            color: '#F5F5F5',
            fontWeight: 700,
            letterSpacing: 3,
            opacity: 0.9,
          }}>
            27 JUIN 2026
          </span>
        </div>

        {/* Central alarm clock SVG icon */}
        <div style={{
          position: 'absolute',
          top: 140,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(30)})`,
          opacity: sp(30),
        }}>
          <svg width={200} height={200} viewBox="0 0 100 100">
            {/* Alarm glow */}
            <circle cx={50} cy={55} r={42} fill="#FF6600" opacity={alarmPulse * 0.3} />
            {/* Clock body */}
            <circle cx={50} cy={55} r={35} fill="none" stroke="#F5F5F5" strokeWidth={3} />
            <circle cx={50} cy={55} r={32} fill="none" stroke="#F5F5F5" strokeWidth={1} opacity={0.3} />
            {/* Clock bells */}
            <circle cx={25} cy={22} r={10} fill="none" stroke="#F5F5F5" strokeWidth={2.5} />
            <circle cx={75} cy={22} r={10} fill="none" stroke="#F5F5F5" strokeWidth={2.5} />
            {/* Bell connector */}
            <line x1={32} y1={28} x2={50} y2={22} stroke="#F5F5F5" strokeWidth={2} />
            <line x1={68} y1={28} x2={50} y2={22} stroke="#F5F5F5" strokeWidth={2} />
            {/* Hammer */}
            <line x1={45} y1={12} x2={55} y2={12} stroke="#8FD14F" strokeWidth={3} strokeLinecap="round" />
            {/* Clock hands */}
            <line x1={50} y1={55} x2={50} y2={32} stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" />
            <line
              x1={50}
              y1={55}
              x2={50 + Math.cos((frame * 0.05) - Math.PI / 2) * 20}
              y2={55 + Math.sin((frame * 0.05) - Math.PI / 2) * 20}
              stroke="#8FD14F"
              strokeWidth={2}
              strokeLinecap="round"
            />
            {/* Center dot */}
            <circle cx={50} cy={55} r={3} fill="#8FD14F" />
            {/* Hour markers */}
            {Array.from({ length: 12 }).map((_, i) => {
              const angle = (i * 30 - 90) * (Math.PI / 180);
              const x1m = 50 + Math.cos(angle) * 28;
              const y1m = 55 + Math.sin(angle) * 28;
              const x2m = 50 + Math.cos(angle) * 32;
              const y2m = 55 + Math.sin(angle) * 32;
              return (
                <line key={`m-${i}`} x1={x1m} y1={y1m} x2={x2m} y2={y2m} stroke="#F5F5F5" strokeWidth={2} />
              );
            })}
            {/* Legs */}
            <line x1={30} y1={85} x2={22} y2={95} stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" />
            <line x1={70} y1={85} x2={78} y2={95} stroke="#F5F5F5" strokeWidth={3} strokeLinecap="round" />
          </svg>
        </div>

        {/* "24H" massive counter */}
        <div style={{
          position: 'absolute',
          top: 350,
          width: '100%',
          textAlign: 'center',
          transform: `scale(${sp(50)})`,
          opacity: sp(50),
        }}>
          <span style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 200,
            fontWeight: 900,
            color: '#F5F5F5',
            textShadow: `0 0 80px rgba(255,102,0,0.6), 0 4px 20px rgba(0,0,0,0.5)`,
            letterSpacing: -5,
          }}>
            {Math.floor(counterVal)}
            <span style={{ fontSize: 100, color: '#8FD14F', marginLeft: 8 }}>H</span>
          </span>
        </div>

        {/* Scissors SVG cutting animation */}
        <div style={{
          position: 'absolute',
          top: 530,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(70)})`,
          opacity: sp(70),
        }}>
          <svg width={160} height={80} viewBox="0 0 160 80">
            {/* Dotted cut line */}
            <line x1={0} y1={40} x2={160} y2={40} stroke="#F5F5F5" strokeWidth={2} strokeDasharray="8,6" opacity={0.5} />
            {/* Scissors blade 1 */}
            <g transform={`rotate(${scissorAngle}, 80, 40)`}>
              <ellipse cx={60} cy={30} rx={25} ry={8} fill="none" stroke="#8FD14F" strokeWidth={2.5} />
              <line x1={80} y1={35} x2={120} y2={40} stroke="#8FD14F" strokeWidth={3} strokeLinecap="round" />
            </g>
            {/* Scissors blade 2 */}
            <g transform={`rotate(${-scissorAngle}, 80, 40)`}>
              <ellipse cx={60} cy={50} rx={25} ry={8} fill="none" stroke="#8FD14F" strokeWidth={2.5} />
              <line x1={80} y1={45} x2={120} y2={40} stroke="#8FD14F" strokeWidth={3} strokeLinecap="round" />
            </g>
            {/* Pivot */}
            <circle cx={80} cy={40} r={4} fill="#F5F5F5" />
          </svg>
        </div>

        {/* Main text: "FRANCE TRAVAIL" */}
        <div style={{
          position: 'absolute',
          top: 630,
          width: '100%',
          textAlign: 'center',
          transform: `translateY(${interpolate(sp(90), [0, 1], [80, 0])}px)`,
          opacity: sp(90),
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 68,
            color: '#F5F5F5',
            letterSpacing: 8,
            textTransform: 'uppercase',
          }}>
            FRANCE TRAVAIL
          </span>
        </div>

        {/* Subtitle: "PEUT COUPER TES ALLOCATIONS" */}
        <div style={{
          position: 'absolute',
          top: 720,
          width: '100%',
          textAlign: 'center',
          transform: `translateY(${interpolate(sp(110), [0, 1], [60, 0])}px)`,
          opacity: sp(110),
        }}>
          <span style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 38,
            color: '#8FD14F',
            fontWeight: 700,
            letterSpacing: 4,
          }}>
            PEUT COUPER TES ALLOCATIONS
          </span>
        </div>

        {/* Timeline bar at bottom */}
        <div style={{
          position: 'absolute',
          bottom: 130,
          left: 160,
          right: 160,
          height: 6,
          backgroundColor: 'rgba(245,245,245,0.15)',
          borderRadius: 3,
        }}>
          <div style={{
            width: `${timelineWidth}%`,
            height: '100%',
            backgroundColor: '#ff0000',
            borderRadius: 3,
            boxShadow: '0 0 15px rgba(255,0,0,0.5)',
          }} />
          {/* Timeline marker */}
          <div style={{
            position: 'absolute',
            left: `${timelineWidth}%`,
            top: -8,
            width: 22,
            height: 22,
            borderRadius: '50%',
            backgroundColor: '#ff0000',
            border: '3px solid #F5F5F5',
            transform: 'translateX(-50%)',
            boxShadow: `0 0 ${10 + alarmPulse * 8}px rgba(255,0,0,0.6)`,
          }} />
        </div>

        {/* Timeline labels */}
        <div style={{
          position: 'absolute',
          bottom: 95,
          left: 160,
          opacity: sp(150) * 0.7,
        }}>
          <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 20, color: '#F5F5F5' }}>
            AVANT
          </span>
        </div>
        <div style={{
          position: 'absolute',
          bottom: 95,
          right: 160,
          opacity: sp(150) * 0.7,
        }}>
          <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 20, color: '#ff0000' }}>
            COUPURE
          </span>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_AlerteChoc24h;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_SansAvertissement',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_SansAvertissement: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera slow pan right
  const cameraPanX = interpolate(frame, [0, durationInFrames], [0, -30], {
    extrapolateRight: 'clamp',
  });

  const gradShift = interpolate(frame, [0, durationInFrames], [140, 180], {
    extrapolateRight: 'clamp',
  });

  // Envelope opening animation
  const envelopeOpen = interpolate(frame, [80, 140], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // X marks appearing staggered
  const xMark1 = sp(100);
  const xMark2 = sp(140);
  const xMark3 = sp(180);

  // Strike-through animation
  const strikeWidth1 = interpolate(frame, [110, 150], [0, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const strikeWidth2 = interpolate(frame, [150, 190], [0, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const strikeWidth3 = interpolate(frame, [190, 230], [0, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Warning flash
  const warnFlash = frame > 60 && frame < 80 ? Math.sin(frame * 0.8) * 0.15 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `translateX(${cameraPanX}px)`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '120%',
          height: '100%',
          left: '-10%',
          background: `linear-gradient(${gradShift}deg, #E55500 0%, #FF6600 40%, #CC4400 70%, #FF6600 100%)`,
        }} />

        {/* Flash overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          backgroundColor: '#F5F5F5',
          opacity: warnFlash,
        }} />

        {/* Animated shape layers - hexagons */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {/* Hexagon 1 */}
          <polygon
            points={`${1600 + Math.sin(frame * 0.006) * 40},${150} ${1700 + Math.sin(frame * 0.006) * 40},${200} ${1700 + Math.sin(frame * 0.006) * 40},${310} ${1600 + Math.sin(frame * 0.006) * 40},${360} ${1500 + Math.sin(frame * 0.006) * 40},${310} ${1500 + Math.sin(frame * 0.006) * 40},${200}`}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.05}
          />
          {/* Large circle bottom left */}
          <circle
            cx={150 + Math.cos(frame * 0.007) * 50}
            cy={850 + Math.sin(frame * 0.005) * 35}
            r={220}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={2}
            opacity={0.04}
          />
          {/* Rotating square */}
          <rect
            x={800}
            y={-50}
            width={200}
            height={200}
            rx={10}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1}
            opacity={0.05}
            transform={`rotate(${frame * 0.2}, 900, 50)`}
          />
        </svg>

        {/* Motion texture - concentric circles */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}>
          {Array.from({ length: 8 }).map((_, i) => (
            <circle
              key={`cc-${i}`}
              cx={960}
              cy={540}
              r={100 + i * 80 + Math.sin(frame * 0.02) * 10}
              fill="none"
              stroke="#F5F5F5"
              strokeWidth={0.8}
            />
          ))}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 25 }).map((_, i) => {
          const px = seededRandom(i * 11 + 7) * 1920;
          const py = seededRandom(i * 17 + 3) * 1080;
          const speed = seededRandom(i * 3 + 9) * 0.6 + 0.15;
          const size = seededRandom(i * 7 + 5) * 5 + 1.5;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: (px + frame * speed * 0.8) % 2000 - 40,
                top: py + Math.sin(frame * 0.03 + i) * 15,
                width: size,
                height: size,
                borderRadius: i % 4 === 0 ? 0 : '50%',
                backgroundColor: i % 2 === 0 ? '#F5F5F5' : '#8FD14F',
                opacity: seededRandom(i * 13 + 2) * 0.2 + 0.04,
                transform: i % 4 === 0 ? `rotate(45deg)` : undefined,
              }}
            />
          );
        })}

        {/* === MAIN CONTENT === */}

        {/* Large envelope SVG icon - center top */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(15)})`,
          opacity: sp(15),
        }}>
          <svg width={180} height={140} viewBox="0 0 180 140">
            {/* Envelope body */}
            <rect x={10} y={40} width={160} height={95} rx={8} fill="none" stroke="#F5F5F5" strokeWidth={3} />
            {/* Envelope flap - opens */}
            <polygon
              points={`10,40 90,${40 - envelopeOpen * 50} 170,40`}
              fill="none"
              stroke="#F5F5F5"
              strokeWidth={3}
              strokeLinejoin="round"
            />
            {/* Letter inside */}
            <rect
              x={35}
              y={50 - envelopeOpen * 35}
              width={110}
              height={70}
              rx={4}
              fill="none"
              stroke="#8FD14F"
              strokeWidth={2}
              opacity={envelopeOpen}
            />
            {/* Lines on letter */}
            <line x1={50} y1={65 - envelopeOpen * 35} x2={130} y2={65 - envelopeOpen * 35} stroke="#8FD14F" strokeWidth={2} opacity={envelopeOpen * 0.6} />
            <line x1={50} y1={80 - envelopeOpen * 35} x2={115} y2={80 - envelopeOpen * 35} stroke="#8FD14F" strokeWidth={2} opacity={envelopeOpen * 0.6} />
            {/* Big X on letter */}
            <line x1={55} y1={55 - envelopeOpen * 35} x2={125} y2={105 - envelopeOpen * 35} stroke="#ff0000" strokeWidth={4} opacity={envelopeOpen} strokeLinecap="round" />
            <line x1={125} y1={55 - envelopeOpen * 35} x2={55} y2={105 - envelopeOpen * 35} stroke="#ff0000" strokeWidth={4} opacity={envelopeOpen} strokeLinecap="round" />
          </svg>
        </div>

        {/* Three items with X marks and strikethroughs */}
        {/* Item 1: Préavis */}
        <div style={{
          position: 'absolute',
          top: 310,
          left: 200,
          display: 'flex',
          alignItems: 'center',
          gap: 30,
          transform: `translateX(${interpolate(sp(100), [0, 1], [-120, 0])}px)`,
          opacity: xMark1,
        }}>
          {/* X icon */}
          <svg width={56} height={56} viewBox="0 0 56 56">
            <circle cx={28} cy={28} r={26} fill="rgba(255,0,0,0.2)" stroke="#ff0000" strokeWidth={3} />
            <line x1={16} y1={16} x2={40} y2={40} stroke="#ff0000" strokeWidth={4} strokeLinecap="round" />
            <line x1={40} y1={16} x2={16} y2={40} stroke="#ff0000" strokeWidth={4} strokeLinecap="round" />
          </svg>
          <div style={{ position: 'relative' }}>
            <span style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 48,
              color: '#F5F5F5',
              fontWeight: 700,
              opacity: 0.5,
            }}>
              PRÉAVIS D'UN MOIS
            </span>
            <div style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: `${strikeWidth1}%`,
              height: 5,
              backgroundColor: '#ff0000',
              transform: 'translateY(-50%)',
            }} />
          </div>
        </div>

        {/* Item 2: Lettre recommandée */}
        <div style={{
          position: 'absolute',
          top: 440,
          left: 200,
          display: 'flex',
          alignItems: 'center',
          gap: 30,
          transform: `translateX(${interpolate(sp(140), [0, 1], [-120, 0])}px)`,
          opacity: xMark2,
        }}>
          <svg width={56} height={56} viewBox="0 0 56 56">
            <circle cx={28} cy={28} r={26} fill="rgba(255,0,0,0.2)" stroke="#ff0000" strokeWidth={3} />
            <line x1={16} y1={16} x2={40} y2={40} stroke="#ff0000" strokeWidth={4} strokeLinecap="round" />
            <line x1={40} y1={16} x2={16} y2={40} stroke="#ff0000" strokeWidth={4} strokeLinecap="round" />
          </svg>
          <div style={{ position: 'relative' }}>
            <span style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 48,
              color: '#F5F5F5',
              fontWeight: 700,
              opacity: 0.5,
            }}>
              LETTRE RECOMMANDÉE
            </span>
            <div style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: `${strikeWidth2}%`,
              height: 5,
              backgroundColor: '#ff0000',
              transform: 'translateY(-50%)',
            }} />
          </div>
        </div>

        {/* Item 3: Délai confortable */}
        <div style={{
          position: 'absolute',
          top: 570,
          left: 200,
          display: 'flex',
          alignItems: 'center',
          gap: 30,
          transform: `translateX(${interpolate(sp(180), [0, 1], [-120, 0])}px)`,
          opacity: xMark3,
        }}>
          <svg width={56} height={56} viewBox="0 0 56 56">
            <circle cx={28} cy={28} r={26} fill="rgba(255,0,0,0.2)" stroke="#ff0000" strokeWidth={3} />
            <line x1={16} y1={16} x2={40} y2={40} stroke="#ff0000" strokeWidth={4} strokeLinecap="round" />
            <line x1={40} y1={16} x2={16} y2={40} stroke="#ff0000" strokeWidth={4} strokeLinecap="round" />
          </svg>
          <div style={{ position: 'relative' }}>
            <span style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 48,
              color: '#F5F5F5',
              fontWeight: 700,
              opacity: 0.5,
            }}>
              DÉLAI CONFORTABLE
            </span>
            <div style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: `${strikeWidth3}%`,
              height: 5,
              backgroundColor: '#ff0000',
              transform: 'translateY(-50%)',
            }} />
          </div>
        </div>

        {/* Bottom badge: "RIEN DE TOUT ÇA" */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(250)})`,
          opacity: sp(250),
          padding: '22px 60px',
          border: '4px solid #8FD14F',
          borderRadius: 12,
          backgroundColor: 'rgba(0,0,0,0.3)',
        }}>
          <span style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 58,
            fontWeight: 800,
            color: '#8FD14F',
            letterSpacing: 4,
          }}>
            RIEN DE TOUT ÇA.
          </span>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_SansAvertissement;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_SuspicionIndices',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_SuspicionIndices: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera tilt
  const cameraTilt = interpolate(frame, [0, durationInFrames], [0.5, -0.5], {
    extrapolateRight: 'clamp',
  });

  const gradAngle = interpolate(frame, [0, durationInFrames], [160, 200], {
    extrapolateRight: 'clamp',
  });

  // Eye blink
  const eyeOpen = frame % 180 > 165 && frame % 180 < 175 ? interpolate(frame % 180, [165, 170, 175], [1, 0.1, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) : 1;

  // Eye scan movement
  const eyeLookX = Math.sin(frame * 0.03) * 8;
  const eyeLookY = Math.cos(frame * 0.025) * 4;

  // Money vanish
  const moneyOpacity = interpolate(frame, [400, 550], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const moneyScale = interpolate(frame, [400, 550], [1, 0.3], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Scan line moving
  const scanY = interpolate(frame, [0, durationInFrames], [0, 1080], {
    extrapolateRight: 'clamp',
  });

  // Screen shake
  const shakeIntensity = frame > 420 && frame < 460 ? (460 - frame) * 0.3 : 0;
  const shakeX = Math.sin(frame * 3) * shakeIntensity;
  const shakeY = Math.cos(frame * 4) * shakeIntensity * 0.6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `translateY(${cameraTilt}px) translate(${shakeX}px, ${shakeY}px)`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #CC4400 0%, #FF6600 30%, #E55500 60%, #FF6600 100%)`,
        }} />

        {/* Animated shape layers - pentagons and lines */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {/* Pentagon */}
          <polygon
            points={`${400 + Math.sin(frame * 0.005) * 30},${80} ${520 + Math.sin(frame * 0.005) * 30},${130} ${490 + Math.sin(frame * 0.005) * 30},${260} ${310 + Math.sin(frame * 0.005) * 30},${260} ${280 + Math.sin(frame * 0.005) * 30},${130}`}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.05}
            transform={`rotate(${frame * 0.08}, 400, 170)`}
          />
          {/* Large arc */}
          <path
            d={`M ${1300 + Math.cos(frame * 0.006) * 40} 800 A 300 300 0 0 1 ${1700 + Math.cos(frame * 0.006) * 40} 600`}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={2}
            opacity={0.06}
          />
          {/* Small rotating diamond */}
          <rect
            x={1700}
            y={150}
            width={100}
            height={100}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1}
            opacity={0.04}
            transform={`rotate(${45 + frame * 0.1}, 1750, 200)`}
          />
        </svg>

        {/* Motion texture - scanning horizontal lines */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.035 }}>
          {Array.from({ length: 40 }).map((_, i) => (
            <line
              key={`hl-${i}`}
              x1={0}
              y1={i * 30 + (Math.sin(frame * 0.01 + i * 0.5) * 5)}
              x2={1920}
              y2={i * 30 + (Math.sin(frame * 0.01 + i * 0.5) * 5)}
              stroke="#F5F5F5"
              strokeWidth={0.6}
            />
          ))}
        </svg>

        {/* Scanning beam */}
        <div style={{
          position: 'absolute',
          top: scanY,
          left: 0,
          width: '100%',
          height: 3,
          backgroundColor: '#8FD14F',
          opacity: 0.35,
          boxShadow: '0 0 30px #8FD14F, 0 0 60px rgba(143,209,79,0.3)',
        }} />

        {/* Floating particles */}
        {Array.from({ length: 28 }).map((_, i) => {
          const px = seededRandom(i * 9 + 2) * 1920;
          const py = seededRandom(i * 14 + 5) * 1080;
          const speed = seededRandom(i * 6 + 8) * 0.4 + 0.2;
          const size = seededRandom(i * 8 + 12) * 4 + 1.5;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: (py - frame * speed * 1.2 + 1200) % 1200 - 60,
                width: size,
                height: size,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
                opacity: seededRandom(i * 4 + 7) * 0.2 + 0.03,
              }}
            />
          );
        })}

        {/* === MAIN CONTENT === */}

        {/* Large surveillance eye SVG */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(10)})`,
          opacity: sp(10),
        }}>
          <svg width={320} height={180} viewBox="0 0 320 180">
            {/* Eye outer */}
            <path
              d={`M 20 90 Q 160 ${10 * eyeOpen} 300 90 Q 160 ${170 + (1 - eyeOpen) * 80} 20 90 Z`}
              fill="none"
              stroke="#F5F5F5"
              strokeWidth={4}
            />
            {/* Iris */}
            <circle cx={160 + eyeLookX} cy={90 + eyeLookY} r={38 * eyeOpen} fill="none" stroke="#8FD14F" strokeWidth={3} />
            {/* Pupil */}
            <circle cx={160 + eyeLookX} cy={90 + eyeLookY} r={18 * eyeOpen} fill="#F5F5F5" />
            {/* Pupil inner */}
            <circle cx={160 + eyeLookX} cy={90 + eyeLookY} r={8 * eyeOpen} fill="#1C1C1C" />
            {/* Reflection */}
            <circle cx={168 + eyeLookX} cy={82 + eyeLookY} r={5 * eyeOpen} fill="#F5F5F5" opacity={0.7} />
            {/* Scan lines radiating from eye */}
            {Array.from({ length: 6 }).map((_, i) => {
              const angle = ((i - 2.5) * 15) * (Math.PI / 180);
              const len = 60 + Math.sin(frame * 0.05 + i) * 15;
              return (
                <line
                  key={`ray-${i}`}
                  x1={160 + Math.cos(angle) * 50}
                  y1={160}
                  x2={160 + Math.cos(angle) * (50 + len)}
                  y2={160 + Math.sin(Math.PI / 4) * len}
                  stroke="#8FD14F"
                  strokeWidth={1.5}
                  opacity={0.3}
                  strokeDasharray="4,4"
                />
              );
            })}
          </svg>
        </div>

        {/* "SUSPICION" label */}
        <div style={{
          position: 'absolute',
          top: 300,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(40)})`,
          opacity: sp(40),
        }}>
          <span style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 110,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 12,
            textShadow: '0 0 40px rgba(245,245,245,0.3)',
          }}>
            SUSPICION
          </span>
        </div>

        {/* Arrow pointing down */}
        <div style={{
          position: 'absolute',
          top: 430,
          left: '50%',
          transform: `translateX(-50%) translateY(${Math.sin(frame * 0.06) * 8}px)`,
          opacity: sp(80),
        }}>
          <svg width={60} height={50} viewBox="0 0 60 50">
            <path d="M 10 10 L 30 40 L 50 10" fill="none" stroke="#8FD14F" strokeWidth={5} strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* Money/Virement vanishing */}
        <div style={{
          position: 'absolute',
          top: 500,
          left: '50%',
          transform: `translateX(-50%) scale(${moneyScale})`,
          opacity: moneyOpacity * sp(100),
        }}>
          {/* Bank transfer receipt SVG */}
          <svg width={400} height={220} viewBox="0 0 400 220">
            {/* Receipt body */}
            <rect x={10} y={10} width={380} height={200} rx={12} fill="rgba(0,0,0,0.3)" stroke="#F5F5F5" strokeWidth={2} />
            {/* Header */}
            <rect x={10} y={10} width={380} height={50} rx={12} fill="rgba(143,209,79,0.15)" />
            <text x={200} y={42} textAnchor="middle" fontFamily="Space Mono, monospace" fontSize={20} fill="#8FD14F" fontWeight={700}>VIREMENT ALLOCATIONS</text>
            {/* Amount */}
            <text x={200} y={110} textAnchor="middle" fontFamily="Clash Display, sans-serif" fontSize={52} fill="#F5F5F5" fontWeight={800}>1 040,00 €</text>
            {/* Status */}
            <rect x={120} y={140} width={160} height={40} rx={20} fill="rgba(255,0,0,0.25)" stroke="#ff0000" strokeWidth={2} />
            <text x={200} y={166} textAnchor="middle" fontFamily="Montserrat, sans-serif" fontSize={18} fill="#ff0000" fontWeight={700}>SUSPENDU</text>
          </svg>
        </div>

        {/* Vanishing particles from money */}
        {frame > 400 && Array.from({ length: 15 }).map((_, i) => {
          const progress = interpolate(frame, [400, 550], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          const angle = seededRandom(i * 7) * Math.PI * 2;
          const dist = progress * 200 * (seededRandom(i * 11) + 0.5);
          return (
            <div
              key={`vp-${i}`}
              style={{
                position: 'absolute',
                left: 960 + Math.cos(angle) * dist,
                top: 600 + Math.sin(angle) * dist,
                width: 8,
                height: 8,
                borderRadius: '50%',
                backgroundColor: i % 2 === 0 ? '#8FD14F' : '#F5F5F5',
                opacity: (1 - progress) * 0.7,
              }}
            />
          );
        })}

        {/* "DISPARAÎT" text bottom */}
        <div style={{
          position: 'absolute',
          bottom: 160,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(450)})`,
          opacity: sp(450),
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 72,
            color: '#ff0000',
            letterSpacing: 6,
            textShadow: '0 0 30px rgba(255,0,0,0.4)',
          }}>
            DISPARAÎT
          </span>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_SuspicionIndices;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_FausseSecurite',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_FausseSecurite: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera orbit
  const cameraScale = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateRight: 'clamp' });

  const gradAngle = interpolate(frame, [0, durationInFrames], [120, 160], { extrapolateRight: 'clamp' });

  // Shield crack animation
  const crackProgress = interpolate(frame, [300, 500], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Shield scale breathing
  const shieldBreath = 1 + Math.sin(frame * 0.04) * 0.02;

  // Two sides comparison reveal
  const leftReveal = sp(30);
  const rightReveal = sp(200);

  // Divider animation
  const dividerHeight = interpolate(sp(60), [0, 1], [0, 600]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `scale(${cameraScale})`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #FF6600 0%, #D94E00 40%, #FF6600 70%, #E55500 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <ellipse
            cx={1650 + Math.sin(frame * 0.007) * 50}
            cy={250 + Math.cos(frame * 0.005) * 30}
            rx={200}
            ry={130}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.04}
          />
          <rect
            x={100 + Math.cos(frame * 0.008) * 30}
            y={700 + Math.sin(frame * 0.006) * 25}
            width={180}
            height={180}
            rx={15}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={1.5}
            opacity={0.05}
            transform={`rotate(${frame * 0.12}, ${190 + Math.cos(frame * 0.008) * 30}, ${790 + Math.sin(frame * 0.006) * 25})`}
          />
          <circle
            cx={960 + Math.cos(frame * 0.004) * 70}
            cy={900 + Math.sin(frame * 0.007) * 40}
            r={150}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1}
            opacity={0.03}
          />
        </svg>

        {/* Motion texture - dot grid */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.04 }}>
          {Array.from({ length: 12 }).map((_, row) =>
            Array.from({ length: 20 }).map((_, col) => (
              <circle
                key={`dot-${row}-${col}`}
                cx={col * 105 + 40 + Math.sin(frame * 0.01 + row) * 3}
                cy={row * 100 + 40 + Math.cos(frame * 0.01 + col) * 3}
                r={2}
                fill="#F5F5F5"
              />
            ))
          )}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 22 }).map((_, i) => {
          const px = seededRandom(i * 8 + 15) * 1920;
          const py = seededRandom(i * 12 + 9) * 1080;
          const speed = seededRandom(i * 4 + 6) * 0.5 + 0.1;
          const size = seededRandom(i * 10 + 3) * 4 + 2;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px + Math.sin(frame * speed * 0.03) * 30,
                top: (py + frame * speed) % 1160 - 40,
                width: size,
                height: size,
                borderRadius: '50%',
                backgroundColor: i % 4 === 0 ? '#8FD14F' : '#F5F5F5',
                opacity: seededRandom(i * 6 + 1) * 0.18 + 0.04,
              }}
            />
          );
        })}

        {/* === MAIN CONTENT: TWO COLUMN COMPARISON === */}

        {/* Left column: "SALARIÉ" - false security */}
        <div style={{
          position: 'absolute',
          top: 120,
          left: 100,
          width: 800,
          opacity: leftReveal,
          transform: `translateX(${interpolate(leftReveal, [0, 1], [-80, 0])}px)`,
        }}>
          {/* Shield icon */}
          <div style={{ marginBottom: 25, transform: `scale(${shieldBreath})` }}>
            <svg width={100} height={110} viewBox="0 0 100 110">
              <path
                d="M 50 5 L 90 25 L 90 55 Q 90 85 50 105 Q 10 85 10 55 L 10 25 Z"
                fill="rgba(143,209,79,0.15)"
                stroke="#8FD14F"
                strokeWidth={3}
              />
              {/* Check mark */}
              <path
                d="M 30 55 L 45 70 L 72 38"
                fill="none"
                stroke="#8FD14F"
                strokeWidth={5}
                strokeLinecap="round"
                strokeLinejoin="round"
              />
              {/* Cracks */}
              {crackProgress > 0 && (
                <>
                  <line x1={50} y1={20} x2={45 + crackProgress * 10} y2={45 + crackProgress * 10} stroke="#ff0000" strokeWidth={2.5} opacity={crackProgress} strokeLinecap="round" />
                  <line x1={45 + crackProgress * 10} y1={45 + crackProgress * 10} x2={35} y2={60 + crackProgress * 15} stroke="#ff0000" strokeWidth={2} opacity={crackProgress * 0.8} strokeLinecap="round" />
                  <line x1={45 + crackProgress * 10} y1={45 + crackProgress * 10} x2={60} y2={55 + crackProgress * 12} stroke="#ff0000" strokeWidth={2} opacity={crackProgress * 0.7} strokeLinecap="round" />
                </>
              )}
            </svg>
          </div>

          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 58,
            color: '#F5F5F5',
            letterSpacing: 5,
            display: 'block',
            marginBottom: 20,
          }}>
            SALARIÉ
          </span>

          <span style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 34,
            color: '#F5F5F5',
            fontWeight: 600,
            opacity: 0.7,
            lineHeight: 1.6,
            display: 'block',
          }}>
            "Ça ne me concerne pas"
          </span>

          {/* Comfort meter - false safety */}
          <div style={{
            marginTop: 40,
            width: 500,
            height: 24,
            backgroundColor: 'rgba(0,0,0,0.3)',
            borderRadius: 12,
            overflow: 'hidden',
          }}>
            <div style={{
              width: `${interpolate(sp(80), [0, 1], [0, 85])}%`,
              height: '100%',
              background: 'linear-gradient(90deg, #8FD14F, #6ab030)',
              borderRadius: 12,
            }} />
          </div>
          <span style={{
            fontFamily: 'Space Mono, monospace',
            fontSize: 20,
            color: '#8FD14F',
            marginTop: 8,
            display: 'block',
            opacity: sp(100),
          }}>
            SENTIMENT DE SÉCURITÉ: 85%
          </span>
        </div>

        {/* Center divider */}
        <div style={{
          position: 'absolute',
          left: 958,
          top: 240,
          width: 4,
          height: dividerHeight,
          background: 'linear-gradient(180deg, transparent, #F5F5F5, transparent)',
          opacity: 0.3,
        }} />

        {/* Right column: "RÉALITÉ" */}
        <div style={{
          position: 'absolute',
          top: 120,
          right: 100,
          width: 800,
          textAlign: 'right',
          opacity: rightReveal,
          transform: `translateX(${interpolate(rightReveal, [0, 1], [80, 0])}px)`,
        }}>
          {/* Danger icon */}
          <div style={{ display: 'flex', justifyContent: 'flex-end', marginBottom: 25 }}>
            <svg width={100} height={100} viewBox="0 0 100 100">
              <polygon
                points="50,8 95,88 5,88"
                fill="rgba(255,0,0,0.15)"
                stroke="#ff0000"
                strokeWidth={3}
                strokeLinejoin="round"
              />
              <line x1={50} y1={35} x2={50} y2={60} stroke="#ff0000" strokeWidth={5} strokeLinecap="round" />
              <circle cx={50} cy={73} r={4} fill="#ff0000" />
            </svg>
          </div>

          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 58,
            color: '#ff0000',
            letterSpacing: 5,
            display: 'block',
            marginBottom: 20,
          }}>
            RÉALITÉ
          </span>

          <span style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 34,
            color: '#F5F5F5',
            fontWeight: 600,
            opacity: 0.7,
            lineHeight: 1.6,
            display: 'block',
          }}>
            "Personne n'est à l'abri"
          </span>

          {/* Real danger meter */}
          <div style={{
            marginTop: 40,
            width: 500,
            height: 24,
            backgroundColor: 'rgba(0,0,0,0.3)',
            borderRadius: 12,
            overflow: 'hidden',
            marginLeft: 'auto',
          }}>
            <div style={{
              width: `${interpolate(sp(250), [0, 1], [0, 92])}%`,
              height: '100%',
              background: 'linear-gradient(90deg, #FFC107, #ff0000)',
              borderRadius: 12,
            }} />
          </div>
          <span style={{
            fontFamily: 'Space Mono, monospace',
            fontSize: 20,
            color: '#ff0000',
            marginTop: 8,
            display: 'block',
            opacity: sp(270),
          }}>
            RISQUE RÉEL: 92%
          </span>
        </div>

        {/* Bottom badge */}
        <div style={{
          position: 'absolute',
          bottom: 130,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(400)})`,
          opacity: sp(400),
          padding: '18px 50px',
          backgroundColor: 'rgba(0,0,0,0.4)',
          border: '3px solid #ff0000',
          borderRadius: 10,
        }}>
          <span style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 48,
            fontWeight: 800,
            color: '#F5F5F5',
          }}>
            C'EST UNE{' '}
            <span style={{ color: '#ff0000' }}>ERREUR</span>
          </span>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_FausseSecurite;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_RepasserLes8',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_RepasserLes8: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraZoom = interpolate(frame, [0, durationInFrames], [1.02, 1], { extrapolateRight: 'clamp' });
  const gradAngle = interpolate(frame, [0, durationInFrames], [150, 190], { extrapolateRight: 'clamp' });

  // Bar graph animation - unemployment rate climbing
  const barData = [
    { year: '2021', value: 8.0, color: '#8FD14F' },
    { year: '2022', value: 7.3, color: '#8FD14F' },
    { year: '2023', value: 7.1, color: '#8FD14F' },
    { year: '2024', value: 7.4, color: '#FFC107' },
    { year: '2025', value: 7.9, color: '#FFC107' },
    { year: '2026', value: 8.1, color: '#ff0000' },
  ];

  // Danger line at 8%
  const dangerLineOpacity = sp(180);

  // Pulse for the 8.1 number
  const pulseBig = 1 + Math.sin(frame * 0.1) * 0.03;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `scale(${cameraZoom})`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #CC4400 0%, #FF6600 35%, #E55500 65%, #FF6600 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <circle
            cx={200 + Math.sin(frame * 0.006) * 60}
            cy={180 + Math.cos(frame * 0.008) * 35}
            r={160}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.04}
          />
          <polygon
            points={`${1600},${800 + Math.sin(frame * 0.005) * 25} ${1750},${900 + Math.sin(frame * 0.005) * 25} ${1600},${1000 + Math.sin(frame * 0.005) * 25} ${1450},${900 + Math.sin(frame * 0.005) * 25}`}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={1.5}
            opacity={0.05}
            transform={`rotate(${frame * 0.1}, 1600, 900)`}
          />
        </svg>

        {/* Motion texture - crosshatch */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.025 }}>
          {Array.from({ length: 15 }).map((_, i) => (
            <React.Fragment key={`ch-${i}`}>
              <line x1={i * 140} y1={0} x2={i * 140 + 600} y2={1080} stroke="#F5F5F5" strokeWidth={0.7} />
              <line x1={i * 140 + 600} y1={0} x2={i * 140} y2={1080} stroke="#F5F5F5" strokeWidth={0.7} />
            </React.Fragment>
          ))}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 24 }).map((_, i) => {
          const px = seededRandom(i * 6 + 20) * 1920;
          const py = seededRandom(i * 10 + 14) * 1080;
          const speed = seededRandom(i * 3 + 11) * 0.4 + 0.15;
          const size = seededRandom(i * 9 + 7) * 4 + 2;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: (py + frame * speed) % 1160 - 40,
                width: size,
                height: size,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
                opacity: seededRandom(i * 5 + 8) * 0.18 + 0.04,
              }}
            />
          );
        })}

        {/* === MAIN CONTENT: BAR CHART === */}

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 50,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(10)})`,
          opacity: sp(10),
          textAlign: 'center',
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 56,
            color: '#F5F5F5',
            letterSpacing: 4,
          }}>
            TAUX DE CHÔMAGE EN FRANCE
          </span>
        </div>

        {/* Bar chart area */}
        <div style={{
          position: 'absolute',
          top: 160,
          left: 200,
          right: 200,
          bottom: 180,
        }}>
          {/* Y-axis labels */}
          {[6, 7, 8, 9].map((val, idx) => (
            <div key={`y-${idx}`} style={{
              position: 'absolute',
              left: -60,
              bottom: ((val - 5.5) / 4.5) * 100 + '%',
              transform: 'translateY(50%)',
              fontFamily: 'Space Mono, monospace',
              fontSize: 22,
              color: '#F5F5F5',
              opacity: 0.5,
            }}>
              {val}%
            </div>
          ))}

          {/* Horizontal grid lines */}
          {[6, 7, 8, 9].map((val, idx) => (
            <div key={`gl-${idx}`} style={{
              position: 'absolute',
              left: 0,
              right: 0,
              bottom: `${((val - 5.5) / 4.5) * 100}%`,
              height: 1,
              backgroundColor: '#F5F5F5',
              opacity: val === 8 ? 0 : 0.08,
            }} />
          ))}

          {/* 8% DANGER LINE */}
          <div style={{
            position: 'absolute',
            left: 0,
            right: 0,
            bottom: `${((8 - 5.5) / 4.5) * 100}%`,
            height: 3,
            backgroundColor: '#ff0000',
            opacity: dangerLineOpacity * 0.8,
            boxShadow: '0 0 15px rgba(255,0,0,0.5)',
            zIndex: 10,
          }}>
            <div style={{
              position: 'absolute',
              right: 0,
              top: -30,
              padding: '4px 16px',
              backgroundColor: 'rgba(255,0,0,0.3)',
              borderRadius: 6,
              border: '1px solid #ff0000',
            }}>
              <span style={{
                fontFamily: 'Space Mono, monospace',
                fontSize: 18,
                color: '#ff0000',
                fontWeight: 700,
              }}>
                SEUIL CRITIQUE 8%
              </span>
            </div>
          </div>

          {/* Bars */}
          {barData.map((bar, idx) => {
            const barHeight = ((bar.value - 5.5) / 4.5) * 100;
            const barDelay = 30 + idx * 25;
            const barSpring = sp(barDelay);
            const barW = 160;
            const totalBars = barData.length;
            const spacing = (1520 - totalBars * barW) / (totalBars + 1);
            const barLeft = spacing + idx * (barW + spacing);

            return (
              <div key={`bar-${idx}`} style={{
                position: 'absolute',
                left: barLeft,
                bottom: 0,
                width: barW,
                height: `${barHeight * barSpring}%`,
                background: idx === 5
                  ? `linear-gradient(180deg, ${bar.color}, rgba(255,0,0,0.6))`
                  : `linear-gradient(180deg, ${bar.color}, rgba(143,209,79,0.3))`,
                borderRadius: '10px 10px 0 0',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'flex-start',
                paddingTop: 14,
                boxShadow: idx === 5 ? '0 0 30px rgba(255,0,0,0.4)' : 'none',
              }}>
                {/* Value on bar */}
                <span style={{
                  fontFamily: 'Clash Display, sans-serif',
                  fontSize: idx === 5 ? 38 : 30,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  transform: idx === 5 ? `scale(${pulseBig})` : undefined,
                  textShadow: idx === 5 ? '0 0 15px rgba(255,0,0,0.6)' : undefined,
                }}>
                  {bar.value}%
                </span>
              </div>
            );
          })}

          {/* X-axis labels (years) */}
          {barData.map((bar, idx) => {
            const barW = 160;
            const totalBars = barData.length;
            const spacing = (1520 - totalBars * barW) / (totalBars + 1);
            const barLeft = spacing + idx * (barW + spacing);
            return (
              <div key={`xlabel-${idx}`} style={{
                position: 'absolute',
                left: barLeft,
                bottom: -45,
                width: barW,
                textAlign: 'center',
                opacity: sp(40 + idx * 25),
              }}>
                <span style={{
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 24,
                  color: idx === 5 ? '#ff0000' : '#F5F5F5',
                  fontWeight: idx === 5 ? 700 : 400,
                }}>
                  {bar.year}
                </span>
              </div>
            );
          })}
        </div>

        {/* Bottom question */}
        <div style={{
          position: 'absolute',
          bottom: 55,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(300)})`,
          opacity: sp(300),
        }}>
          <span style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 36,
            color: '#8FD14F',
            fontWeight: 700,
            fontStyle: 'italic',
          }}>
            Es-tu vraiment à l'abri ?
          </span>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_RepasserLes8;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_Stat81Pourcent',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_Stat81Pourcent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const gradAngle = interpolate(frame, [0, durationInFrames], [170, 210], { extrapolateRight: 'clamp' });

  // Donut chart animation
  const donutProgress = interpolate(frame, [40, 200], [0, 8.1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Counter for 8.1
  const counterVal = interpolate(frame, [40, 200], [0, 8.1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Arrow up for +0.7
  const arrowBounce = sp(250);

  // Pulse for emphasis
  const emphPulse = 1 + Math.sin(frame * 0.08) * 0.025;

  // Donut chart math
  const radius = 160;
  const circumference = 2 * Math.PI * radius;
  const donutPercent = donutProgress / 100;
  const strokeDash = circumference * donutPercent;
  const strokeGap = circumference - strokeDash;

  // Population icons animation
  const iconCount = 10;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #E55500 0%, #FF6600 30%, #D94E00 60%, #FF6600 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <rect
            x={1500 + Math.sin(frame * 0.006) * 40}
            y={100 + Math.cos(frame * 0.008) * 25}
            width={300}
            height={300}
            rx={25}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.04}
            transform={`rotate(${frame * 0.08}, ${1650}, ${250})`}
          />
          <circle
            cx={150 + Math.cos(frame * 0.005) * 35}
            cy={850 + Math.sin(frame * 0.007) * 30}
            r={200}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={1.5}
            opacity={0.05}
          />
        </svg>

        {/* Motion texture - radial lines from center left */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.025 }}>
          {Array.from({ length: 24 }).map((_, i) => {
            const angle = (i * 15) * (Math.PI / 180);
            return (
              <line
                key={`rl-${i}`}
                x1={400}
                y1={540}
                x2={400 + Math.cos(angle) * 900}
                y2={540 + Math.sin(angle) * 900}
                stroke="#F5F5F5"
                strokeWidth={0.6}
              />
            );
          })}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const px = seededRandom(i * 7 + 22) * 1920;
          const py = seededRandom(i * 11 + 16) * 1080;
          const speed = seededRandom(i * 5 + 13) * 0.35 + 0.15;
          const size = seededRandom(i * 8 + 9) * 4 + 2;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: (py + frame * speed) % 1160 - 40,
                width: size,
                height: size,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
                opacity: seededRandom(i * 4 + 10) * 0.18 + 0.04,
              }}
            />
          );
        })}

        {/* === MAIN CONTENT === */}

        {/* Left side: Donut chart */}
        <div style={{
          position: 'absolute',
          top: 120,
          left: 120,
          width: 500,
          height: 500,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}>
          <svg width={400} height={400} viewBox="0 0 400 400" style={{ transform: 'rotate(-90deg)' }}>
            {/* Background ring */}
            <circle cx={200} cy={200} r={radius} fill="none" stroke="rgba(245,245,245,0.1)" strokeWidth={35} />
            {/* Progress ring */}
            <circle
              cx={200}
              cy={200}
              r={radius}
              fill="none"
              stroke="#ff0000"
              strokeWidth={35}
              strokeDasharray={`${strokeDash} ${strokeGap}`}
              strokeLinecap="round"
              style={{ filter: 'drop-shadow(0 0 15px rgba(255,0,0,0.5))' }}
            />
            {/* Secondary ring - remaining */}
            <circle
              cx={200}
              cy={200}
              r={radius}
              fill="none"
              stroke="#8FD14F"
              strokeWidth={35}
              strokeDasharray={`${circumference - strokeDash} ${strokeDash}`}
              strokeDashoffset={-strokeDash}
              strokeLinecap="round"
              opacity={0.3}
            />
          </svg>

          {/* Center percentage text */}
          <div style={{
            position: 'absolute',
            textAlign: 'center',
            transform: `scale(${emphPulse})`,
          }}>
            <span style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 120,
              fontWeight: 900,
              color: '#F5F5F5',
              textShadow: '0 0 40px rgba(255,0,0,0.4)',
            }}>
              {counterVal.toFixed(1)}
            </span>
            <span style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 60,
              fontWeight: 700,
              color: '#8FD14F',
            }}>
              %
            </span>
          </div>
        </div>

        {/* Right side: Stats */}
        <div style={{
          position: 'absolute',
          top: 140,
          right: 120,
          width: 800,
        }}>
          {/* T1 2026 label */}
          <div style={{
            opacity: sp(20),
            transform: `translateY(${interpolate(sp(20), [0, 1], [30, 0])}px)`,
            marginBottom: 30,
          }}>
            <span style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 30,
              color: '#8FD14F',
              letterSpacing: 4,
              fontWeight: 700,
            }}>
              T1 2026
            </span>
          </div>

          {/* Main title */}
          <div style={{
            opacity: sp(40),
            transform: `translateY(${interpolate(sp(40), [0, 1], [30, 0])}px)`,
            marginBottom: 40,
          }}>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 52,
              color: '#F5F5F5',
              letterSpacing: 3,
              lineHeight: 1.3,
              display: 'block',
            }}>
              POPULATION ACTIVE
            </span>
            <span style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 30,
              color: '#F5F5F5',
              fontWeight: 500,
              opacity: 0.7,
            }}>
              au sens du Bureau International du Travail
            </span>
          </div>

          {/* +0.7 point badge */}
          <div style={{
            opacity: arrowBounce,
            transform: `translateY(${interpolate(arrowBounce, [0, 1], [40, 0])}px) scale(${arrowBounce})`,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            marginBottom: 50,
            padding: '16px 30px',
            backgroundColor: 'rgba(255,0,0,0.15)',
            border: '2px solid #ff0000',
            borderRadius: 12,
            width: 'fit-content',
          }}>
            {/* Up arrow SVG */}
            <svg width={40} height={40} viewBox="0 0 40 40">
              <path d="M 20 5 L 35 20 L 27 20 L 27 35 L 13 35 L 13 20 L 5 20 Z" fill="#ff0000" />
            </svg>
            <span style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 48,
              fontWeight: 800,
              color: '#ff0000',
            }}>
              +0,7 POINT
            </span>
            <span style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 26,
              color: '#F5F5F5',
              fontWeight: 600,
              opacity: 0.7,
            }}>
              sur un an
            </span>
          </div>

          {/* Population icons row */}
          <div style={{
            display: 'flex',
            gap: 18,
            alignItems: 'flex-end',
            opacity: sp(300),
          }}>
            {Array.from({ length: iconCount }).map((_, i) => {
              const isAffected = i === 0;
              const iconDelay = 300 + i * 12;
              const iconScale = sp(iconDelay);
              return (
                <div key={`icon-${i}`} style={{
                  transform: `scale(${iconScale})`,
                  opacity: iconScale,
                }}>
                  <svg width={50} height={70} viewBox="0 0 50 70">
                    {/* Head */}
                    <circle cx={25} cy={15} r={10} fill={isAffected ? '#ff0000' : 'rgba(245,245,245,0.5)'} />
                    {/* Body */}
                    <line x1={25} y1={25} x2={25} y2={48} stroke={isAffected ? '#ff0000' : 'rgba(245,245,245,0.5)'} strokeWidth={3} strokeLinecap="round" />
                    {/* Arms */}
                    <line x1={12} y1={35} x2={38} y2={35} stroke={isAffected ? '#ff0000' : 'rgba(245,245,245,0.5)'} strokeWidth={3} strokeLinecap="round" />
                    {/* Legs */}
                    <line x1={25} y1={48} x2={14} y2={65} stroke={isAffected ? '#ff0000' : 'rgba(245,245,245,0.5)'} strokeWidth={3} strokeLinecap="round" />
                    <line x1={25} y1={48} x2={36} y2={65} stroke={isAffected ? '#ff0000' : 'rgba(245,245,245,0.5)'} strokeWidth={3} strokeLinecap="round" />
                  </svg>
                </div>
              );
            })}
          </div>

          {/* Label under icons */}
          <div style={{ marginTop: 12, opacity: sp(450) }}>
            <span style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 22,
              color: '#F5F5F5',
              fontWeight: 600,
              opacity: 0.6,
            }}>
              1 actif sur 12 est sans emploi
            </span>
          </div>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_Stat81Pourcent;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_JamaisVuDepuis2021',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_JamaisVuDepuis2021: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const gradAngle = interpolate(frame, [0, durationInFrames], [130, 170], { extrapolateRight: 'clamp' });

  // Line chart data points (quarterly from 2021 to 2026)
  const lineData = [
    { q: 'T1-21', val: 8.0 },
    { q: 'T3-21', val: 7.8 },
    { q: 'T1-22', val: 7.3 },
    { q: 'T3-22', val: 7.2 },
    { q: 'T1-23', val: 7.1 },
    { q: 'T3-23', val: 7.3 },
    { q: 'T1-24', val: 7.4 },
    { q: 'T3-24', val: 7.5 },
    { q: 'T1-25', val: 7.9 },
    { q: 'T3-25', val: 7.8 },
    { q: 'T1-26', val: 8.1 },
  ];

  const chartLeft = 200;
  const chartRight = 1720;
  const chartTop = 220;
  const chartBottom = 750;
  const chartW = chartRight - chartLeft;
  const chartH = chartBottom - chartTop;
  const minVal = 6.5;
  const maxVal = 9.0;

  const getX = (idx: number) => chartLeft + (idx / (lineData.length - 1)) * chartW;
  const getY = (val: number) => chartBottom - ((val - minVal) / (maxVal - minVal)) * chartH;

  // Line draw progress
  const lineProgress = interpolate(frame, [30, 350], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // 8% danger zone highlight
  const dangerY = getY(8.0);

  // Impact shake when crossing 8%
  const crossFrame = 310;
  const shakeAmt = frame > crossFrame && frame < crossFrame + 30 ? (crossFrame + 30 - frame) * 0.25 : 0;
  const shX = Math.sin(frame * 3) * shakeAmt;
  const shY = Math.cos(frame * 4) * shakeAmt * 0.5;

  // Build path string
  const visiblePoints = Math.floor(lineProgress * (lineData.length - 1)) + 1;
  let pathD = '';
  for (let i = 0; i < Math.min(visiblePoints, lineData.length); i++) {
    const x = getX(i);
    const y = getY(lineData[i].val);
    pathD += i === 0 ? `M ${x} ${y}` : ` L ${x} ${y}`;
  }
  // Partial segment
  const partialIdx = lineProgress * (lineData.length - 1);
  const fracPart = partialIdx - Math.floor(partialIdx);
  if (visiblePoints < lineData.length && fracPart > 0) {
    const prevIdx = Math.floor(partialIdx);
    const nextIdx = Math.min(prevIdx + 1, lineData.length - 1);
    const px = getX(prevIdx) + (getX(nextIdx) - getX(prevIdx)) * fracPart;
    const py = getY(lineData[prevIdx].val) + (getY(lineData[nextIdx].val) - getY(lineData[prevIdx].val)) * fracPart;
    pathD += ` L ${px} ${py}`;
  }

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `translate(${shX}px, ${shY}px)`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #FF6600 0%, #CC4400 40%, #FF6600 80%, #E55500 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <ellipse
            cx={1750 + Math.sin(frame * 0.005) * 30}
            cy={900 + Math.cos(frame * 0.007) * 20}
            rx={250}
            ry={150}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.04}
          />
          <circle
            cx={120 + Math.cos(frame * 0.006) * 25}
            cy={200 + Math.sin(frame * 0.008) * 20}
            r={130}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={1}
            opacity={0.04}
          />
        </svg>

        {/* Motion texture - fine vertical lines */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.02 }}>
          {Array.from({ length: 50 }).map((_, i) => (
            <line key={`vl-${i}`} x1={i * 40 + 10} y1={0} x2={i * 40 + 10} y2={1080} stroke="#F5F5F5" strokeWidth={0.5} />
          ))}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 18 }).map((_, i) => {
          const px = seededRandom(i * 5 + 30) * 1920;
          const py = seededRandom(i * 9 + 18) * 1080;
          const speed = seededRandom(i * 7 + 4) * 0.3 + 0.1;
          const size = seededRandom(i * 3 + 15) * 4 + 2;
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: px,
              top: (py + frame * speed) % 1160 - 40,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
              opacity: seededRandom(i * 6 + 12) * 0.15 + 0.04,
            }} />
          );
        })}

        {/* === MAIN CONTENT: LINE CHART === */}

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 50,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(5)})`,
          opacity: sp(5),
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 52,
            color: '#F5F5F5',
            letterSpacing: 4,
          }}>
            ÉVOLUTION DU CHÔMAGE 2021-2026
          </span>
        </div>

        {/* Subtitle */}
        <div style={{
          position: 'absolute',
          top: 120,
          left: '50%',
          transform: `translateX(-50%)`,
          opacity: sp(15),
        }}>
          <span style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 26,
            color: '#8FD14F',
            fontWeight: 600,
          }}>
            Du jamais vu depuis 5 ans
          </span>
        </div>

        {/* Chart SVG */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          {/* 8% danger zone fill */}
          <rect
            x={chartLeft}
            y={chartTop}
            width={chartW}
            height={dangerY - chartTop}
            fill="rgba(255,0,0,0.06)"
            opacity={sp(100)}
          />

          {/* 8% line */}
          <line
            x1={chartLeft}
            y1={dangerY}
            x2={chartRight}
            y2={dangerY}
            stroke="#ff0000"
            strokeWidth={2.5}
            strokeDasharray="12,8"
            opacity={sp(80) * 0.7}
          />
          <text
            x={chartRight + 10}
            y={dangerY + 6}
            fontFamily="Space Mono, monospace"
            fontSize={20}
            fill="#ff0000"
            fontWeight={700}
            opacity={sp(80)}
          >
            8%
          </text>

          {/* Y-axis grid */}
          {[7.0, 7.5, 8.0, 8.5].map((v, i) => (
            <React.Fragment key={`yg-${i}`}>
              <line x1={chartLeft} y1={getY(v)} x2={chartRight} y2={getY(v)} stroke="#F5F5F5" strokeWidth={0.5} opacity={0.1} />
              <text x={chartLeft - 55} y={getY(v) + 5} fontFamily="Space Mono, monospace" fontSize={18} fill="#F5F5F5" opacity={0.5}>{v}%</text>
            </React.Fragment>
          ))}

          {/* Line path */}
          <path
            d={pathD}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={4}
            strokeLinecap="round"
            strokeLinejoin="round"
            style={{ filter: 'drop-shadow(0 0 8px rgba(143,209,79,0.4))' }}
          />

          {/* Data points */}
          {lineData.map((pt, i) => {
            const pointVisible = lineProgress * (lineData.length - 1) >= i;
            if (!pointVisible) return null;
            const isLast = i === lineData.length - 1;
            const aboveDanger = pt.val >= 8.0;
            return (
              <React.Fragment key={`pt-${i}`}>
                <circle
                  cx={getX(i)}
                  cy={getY(pt.val)}
                  r={isLast ? 10 : 6}
                  fill={aboveDanger ? '#ff0000' : '#8FD14F'}
                  stroke="#F5F5F5"
                  strokeWidth={2}
                />
                {isLast && (
                  <circle
                    cx={getX(i)}
                    cy={getY(pt.val)}
                    r={20 + Math.sin(frame * 0.1) * 5}
                    fill="none"
                    stroke="#ff0000"
                    strokeWidth={2}
                    opacity={0.4 + Math.sin(frame * 0.1) * 0.2}
                  />
                )}
              </React.Fragment>
            );
          })}

          {/* X-axis labels */}
          {lineData.map((pt, i) => {
            const pointVisible = lineProgress * (lineData.length - 1) >= i;
            if (!pointVisible) return null;
            const isLast = i === lineData.length - 1;
            return (
              <text
                key={`xl-${i}`}
                x={getX(i)}
                y={chartBottom + 40}
                textAnchor="middle"
                fontFamily="Space Mono, monospace"
                fontSize={i % 2 === 0 ? 18 : 0}
                fill={isLast ? '#ff0000' : '#F5F5F5'}
                fontWeight={isLast ? 700 : 400}
                opacity={isLast ? 1 : 0.6}
              >
                {pt.q}
              </text>
            );
          })}
        </svg>

        {/* 2021 and 2026 comparison boxes */}
        <div style={{
          position: 'absolute',
          bottom: 110,
          left: 200,
          display: 'flex',
          gap: 60,
          opacity: sp(400),
        }}>
          <div style={{
            padding: '14px 30px',
            backgroundColor: 'rgba(0,0,0,0.35)',
            borderRadius: 10,
            border: '2px solid rgba(245,245,245,0.2)',
          }}>
            <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 20, color: '#F5F5F5', opacity: 0.6 }}>T1 2021: </span>
            <span style={{ fontFamily: 'Clash Display, sans-serif', fontSize: 34, color: '#F5F5F5', fontWeight: 800 }}>8,0%</span>
          </div>
          <div style={{
            display: 'flex',
            alignItems: 'center',
          }}>
            <svg width={60} height={30} viewBox="0 0 60 30">
              <path d="M 5 15 L 45 15 M 35 5 L 50 15 L 35 25" fill="none" stroke="#8FD14F" strokeWidth={3} strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>
          <div style={{
            padding: '14px 30px',
            backgroundColor: 'rgba(255,0,0,0.15)',
            borderRadius: 10,
            border: '2px solid #ff0000',
          }}>
            <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 20, color: '#F5F5F5', opacity: 0.6 }}>T1 2026: </span>
            <span style={{ fontFamily: 'Clash Display, sans-serif', fontSize: 34, color: '#ff0000', fontWeight: 800 }}>8,1%</span>
          </div>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_JamaisVuDepuis2021;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_AmeliorationEffacee',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_AmeliorationEffacee: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const gradAngle = interpolate(frame, [0, durationInFrames], [200, 240], { extrapolateRight: 'clamp' });

  // Progress bar that fills GREEN then erases back RED
  const greenFill = interpolate(frame, [30, 180], [0, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const redErase = interpolate(frame, [250, 500], [0, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Eraser SVG position
  const eraserX = interpolate(frame, [250, 500], [100, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // 5 trimester blocks
  const trimesters = ['T1', 'T2', 'T3', 'T4', 'T5'];

  // Shake when erasing
  const eraseShake = frame > 250 && frame < 500 ? Math.sin(frame * 4) * 2 : 0;

  // Counter: "3 ans" vs "5 trimestres"
  const yearCount = interpolate(frame, [60, 160], [0, 3], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const trimCount = interpolate(frame, [280, 450], [0, 5], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `translateX(${eraseShake}px)`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #D94E00 0%, #FF6600 35%, #CC4400 65%, #FF6600 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <rect
            x={80 + Math.sin(frame * 0.006) * 30}
            y={120 + Math.cos(frame * 0.008) * 20}
            width={220}
            height={220}
            rx={20}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.05}
            transform={`rotate(${frame * 0.1}, ${190}, ${230})`}
          />
          <circle
            cx={1700 + Math.cos(frame * 0.005) * 40}
            cy={800 + Math.sin(frame * 0.007) * 25}
            r={180}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={1.5}
            opacity={0.04}
          />
          <polygon
            points={`${960 + Math.sin(frame * 0.004) * 50},${950} ${1060 + Math.sin(frame * 0.004) * 50},${1050} ${860 + Math.sin(frame * 0.004) * 50},${1050}`}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1}
            opacity={0.03}
          />
        </svg>

        {/* Motion texture - wave lines */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}>
          {Array.from({ length: 6 }).map((_, i) => {
            let d = `M 0 ${400 + i * 60}`;
            for (let x = 0; x <= 1920; x += 40) {
              d += ` Q ${x + 20} ${400 + i * 60 + Math.sin((x + frame * 2) * 0.01) * 20}, ${x + 40} ${400 + i * 60}`;
            }
            return <path key={`w-${i}`} d={d} fill="none" stroke="#F5F5F5" strokeWidth={0.7} />;
          })}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 22 }).map((_, i) => {
          const px = seededRandom(i * 8 + 35) * 1920;
          const py = seededRandom(i * 12 + 20) * 1080;
          const speed = seededRandom(i * 4 + 17) * 0.35 + 0.1;
          const size = seededRandom(i * 6 + 11) * 4 + 2;
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: px,
              top: (py + frame * speed) % 1160 - 40,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
              opacity: seededRandom(i * 9 + 3) * 0.16 + 0.04,
            }} />
          );
        })}

        {/* === MAIN CONTENT === */}

        {/* Top section: "3 ANS D'AMÉLIORATION" */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(10)})`,
          opacity: sp(10),
          textAlign: 'center',
        }}>
          <div style={{ display: 'flex', alignItems: 'baseline', justifyContent: 'center', gap: 20 }}>
            <span style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 140,
              fontWeight: 900,
              color: '#8FD14F',
            }}>
              {Math.floor(yearCount)}
            </span>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 64,
              color: '#F5F5F5',
              letterSpacing: 4,
            }}>
              ANS
            </span>
          </div>
          <span style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 34,
            color: '#F5F5F5',
            fontWeight: 600,
            opacity: 0.7,
          }}>
            d'amélioration post-Covid
          </span>
        </div>

        {/* Progress bar - build and erase */}
        <div style={{
          position: 'absolute',
          top: 380,
          left: 200,
          right: 200,
        }}>
          {/* Bar track */}
          <div style={{
            width: '100%',
            height: 50,
            backgroundColor: 'rgba(0,0,0,0.3)',
            borderRadius: 25,
            overflow: 'hidden',
            position: 'relative',
          }}>
            {/* Green fill */}
            <div style={{
              position: 'absolute',
              left: 0,
              top: 0,
              width: `${greenFill}%`,
              height: '100%',
              background: 'linear-gradient(90deg, #8FD14F, #6ab030)',
              borderRadius: 25,
            }} />
            {/* Red erase overlay */}
            <div style={{
              position: 'absolute',
              right: 0,
              top: 0,
              width: `${redErase}%`,
              height: '100%',
              background: 'linear-gradient(270deg, #ff0000, rgba(255,0,0,0.7))',
              borderRadius: 25,
            }} />
          </div>

          {/* Eraser icon on the erasing edge */}
          {frame > 250 && (
            <div style={{
              position: 'absolute',
              left: `${eraserX}%`,
              top: -25,
              transform: 'translateX(-50%)',
            }}>
              <svg width={50} height={100} viewBox="0 0 50 100">
                {/* Eraser body */}
                <rect x={5} y={10} width={40} height={60} rx={5} fill="#F5F5F5" />
                <rect x={5} y={50} width={40} height={20} rx={3} fill="#ff0000" opacity={0.8} />
                {/* Eraser top */}
                <rect x={10} y={5} width={30} height={12} rx={3} fill="rgba(245,245,245,0.6)" />
                {/* Debris particles */}
                {Array.from({ length: 4 }).map((_, i) => (
                  <circle
                    key={`deb-${i}`}
                    cx={25 + Math.sin(frame * 0.3 + i * 2) * 15}
                    cy={80 + i * 5 + Math.cos(frame * 0.2 + i) * 5}
                    r={3}
                    fill="#ff0000"
                    opacity={0.5}
                  />
                ))}
              </svg>
            </div>
          )}

          {/* Labels under bar */}
          <div style={{
            display: 'flex',
            justifyContent: 'space-between',
            marginTop: 16,
          }}>
            <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 22, color: '#8FD14F', opacity: 0.7 }}>
              PROGRÈS
            </span>
            <span style={{ fontFamily: 'Space Mono, monospace', fontSize: 22, color: '#ff0000', opacity: sp(280) }}>
              EFFACÉ
            </span>
          </div>
        </div>

        {/* 5 Trimester blocks */}
        <div style={{
          position: 'absolute',
          top: 540,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 30,
        }}>
          {trimesters.map((t, i) => {
            const blockDelay = 280 + i * 30;
            const blockSpring = sp(blockDelay);
            const isFilled = trimCount > i;
            return (
              <div key={`t-${i}`} style={{
                width: 200,
                height: 120,
                backgroundColor: isFilled ? 'rgba(255,0,0,0.25)' : 'rgba(0,0,0,0.25)',
                border: `3px solid ${isFilled ? '#ff0000' : 'rgba(245,245,245,0.15)'}`,
                borderRadius: 14,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                transform: `scale(${blockSpring})`,
                opacity: blockSpring,
              }}>
                <span style={{
                  fontFamily: 'Clash Display, sans-serif',
                  fontSize: 44,
                  fontWeight: 800,
                  color: isFilled ? '#ff0000' : '#F5F5F5',
                }}>
                  {t}
                </span>
                <span style={{
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 18,
                  color: '#F5F5F5',
                  opacity: 0.5,
                }}>
                  TRIMESTRE
                </span>
              </div>
            );
          })}
        </div>

        {/* Bottom text: "EFFACÉ EN 5 TRIMESTRES" */}
        <div style={{
          position: 'absolute',
          bottom: 120,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(500)})`,
          opacity: sp(500),
          display: 'flex',
          alignItems: 'baseline',
          gap: 20,
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 68,
            color: '#F5F5F5',
            letterSpacing: 3,
          }}>
            EFFACÉ EN
          </span>
          <span style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 100,
            fontWeight: 900,
            color: '#ff0000',
            textShadow: '0 0 30px rgba(255,0,0,0.4)',
          }}>
            5
          </span>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 68,
            color: '#F5F5F5',
            letterSpacing: 3,
          }}>
            TRIMESTRES
          </span>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_AmeliorationEffacee;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_LucasIntro',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_LucasIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const gradAngle = interpolate(frame, [0, durationInFrames], [145, 185], { extrapolateRight: 'clamp' });

  // Logo pulse
  const logoPulse = 1 + Math.sin(frame * 0.06) * 0.02;

  // Mic wave animation
  const wavePhase = frame * 0.08;

  // Text reveal stagger
  const nameReveal = sp(40);
  const tagReveal = sp(80);
  const msgReveal = sp(160);

  // Orbiting ring around logo
  const orbitAngle = frame * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #FF6600 0%, #E55500 30%, #FF6600 60%, #CC4400 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <circle
            cx={300 + Math.sin(frame * 0.007) * 40}
            cy={800 + Math.cos(frame * 0.005) * 30}
            r={200}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.04}
          />
          <rect
            x={1400 + Math.cos(frame * 0.006) * 35}
            y={200 + Math.sin(frame * 0.008) * 25}
            width={280}
            height={280}
            rx={22}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={1.5}
            opacity={0.04}
            transform={`rotate(${frame * 0.1}, ${1540}, ${340})`}
          />
          <polygon
            points={`${150},${200 + Math.sin(frame * 0.004) * 20} ${250},${300 + Math.sin(frame * 0.004) * 20} ${50},${300 + Math.sin(frame * 0.004) * 20}`}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1}
            opacity={0.03}
          />
        </svg>

        {/* Motion texture - orbital circles */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}>
          {Array.from({ length: 5 }).map((_, i) => (
            <circle
              key={`orb-${i}`}
              cx={960}
              cy={400}
              r={120 + i * 60}
              fill="none"
              stroke="#F5F5F5"
              strokeWidth={0.6}
              strokeDasharray="6,12"
              transform={`rotate(${frame * (0.05 + i * 0.02)}, 960, 400)`}
            />
          ))}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const px = seededRandom(i * 6 + 40) * 1920;
          const py = seededRandom(i * 10 + 25) * 1080;
          const speed = seededRandom(i * 4 + 19) * 0.3 + 0.1;
          const size = seededRandom(i * 8 + 14) * 4 + 2;
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: px,
              top: (py + frame * speed) % 1160 - 40,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
              opacity: seededRandom(i * 5 + 16) * 0.16 + 0.04,
            }} />
          );
        })}

        {/* === MAIN CONTENT: BRAND INTRO === */}

        {/* Central logo area */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(5) * logoPulse})`,
          opacity: sp(5),
        }}>
          {/* Logo circle with MF initials */}
          <div style={{
            width: 200,
            height: 200,
            borderRadius: '50%',
            border: '5px solid #8FD14F',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            backgroundColor: 'rgba(0,0,0,0.3)',
            position: 'relative',
            margin: '0 auto',
          }}>
            <span style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 76,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 4,
            }}>
              MF
            </span>
            {/* Orbiting dot */}
            <div style={{
              position: 'absolute',
              width: 14,
              height: 14,
              borderRadius: '50%',
              backgroundColor: '#8FD14F',
              left: 100 + Math.cos(orbitAngle) * 110 - 7,
              top: 100 + Math.sin(orbitAngle) * 110 - 7,
              boxShadow: '0 0 12px #8FD14F',
            }} />
          </div>
        </div>

        {/* "MATURES FINANCE" brand name */}
        <div style={{
          position: 'absolute',
          top: 340,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(nameReveal, [0, 1], [40, 0])}px)`,
          opacity: nameReveal,
          textAlign: 'center',
        }}>
          <span style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 80,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 10,
          }}>
            MATURES FINANCE
          </span>
        </div>

        {/* "Par Lucas" */}
        <div style={{
          position: 'absolute',
          top: 440,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(tagReveal, [0, 1], [30, 0])}px)`,
          opacity: tagReveal,
          textAlign: 'center',
          display: 'flex',
          alignItems: 'center',
          gap: 15,
        }}>
          {/* Microphone icon */}
          <svg width={32} height={32} viewBox="0 0 32 32">
            <rect x={12} y={3} width={8} height={16} rx={4} fill="none" stroke="#8FD14F" strokeWidth={2} />
            <path d="M 6 16 Q 6 26 16 26 Q 26 26 26 16" fill="none" stroke="#8FD14F" strokeWidth={2} />
            <line x1={16} y1={26} x2={16} y2={30} stroke="#8FD14F" strokeWidth={2} />
            <line x1={10} y1={30} x2={22} y2={30} stroke="#8FD14F" strokeWidth={2} strokeLinecap="round" />
          </svg>
          <span style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 36,
            color: '#8FD14F',
            fontWeight: 600,
          }}>
            Par Lucas
          </span>
        </div>

        {/* Separator line */}
        <div style={{
          position: 'absolute',
          top: 510,
          left: '50%',
          transform: 'translateX(-50%)',
          width: interpolate(sp(120), [0, 1], [0, 600]),
          height: 2,
          background: 'linear-gradient(90deg, transparent, #8FD14F, transparent)',
          opacity: 0.5,
        }} />

        {/* Audio waveform visualization */}
        <div style={{
          position: 'absolute',
          top: 540,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 5,
          alignItems: 'center',
          opacity: sp(140),
        }}>
          {Array.from({ length: 40 }).map((_, i) => {
            const h = 15 + Math.abs(Math.sin(wavePhase + i * 0.4)) * 35 + seededRandom(i * 3) * 10;
            return (
              <div key={`wav-${i}`} style={{
                width: 6,
                height: h,
                backgroundColor: '#8FD14F',
                borderRadius: 3,
                opacity: 0.5 + Math.sin(wavePhase + i * 0.3) * 0.3,
              }} />
            );
          })}
        </div>

        {/* Message box: "CE N'EST PAS DU CATASTROPHISME" */}
        <div style={{
          position: 'absolute',
          top: 640,
          left: '50%',
          transform: `translateX(-50%) scale(${msgReveal})`,
          opacity: msgReveal,
          textAlign: 'center',
          padding: '30px 60px',
          backgroundColor: 'rgba(0,0,0,0.35)',
          borderRadius: 16,
          border: '2px solid rgba(143,209,79,0.3)',
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 52,
            color: '#F5F5F5',
            letterSpacing: 3,
            lineHeight: 1.4,
            display: 'block',
          }}>
            CE N'EST{' '}
            <span style={{ color: '#ff0000', textDecoration: 'line-through', textDecorationThickness: 4 }}>PAS</span>
            {' '}DU
          </span>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 52,
            color: '#F5F5F5',
            letterSpacing: 3,
          }}>
            CATASTROPHISME
          </span>
        </div>

        {/* Three pill badges at bottom */}
        <div style={{
          position: 'absolute',
          bottom: 110,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 25,
        }}>
          {['FAITS', 'CHIFFRES', 'SOLUTIONS'].map((label, i) => (
            <div key={`pill-${i}`} style={{
              padding: '12px 35px',
              borderRadius: 30,
              border: '2px solid #8FD14F',
              backgroundColor: 'rgba(143,209,79,0.1)',
              transform: `scale(${sp(300 + i * 30)})`,
              opacity: sp(300 + i * 30),
            }}>
              <span style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 22,
                fontWeight: 700,
                color: '#8FD14F',
                letterSpacing: 3,
              }}>
                {label}
              </span>
            </div>
          ))}
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_LucasIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_EtatDesLieux',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_EtatDesLieux: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const gradAngle = interpolate(frame, [0, durationInFrames], [160, 200], { extrapolateRight: 'clamp' });
  const cameraZoom = interpolate(frame, [0, durationInFrames], [1, 1.03], { extrapolateRight: 'clamp' });

  // Three pillars: Marché du Travail, Nouvelles Lois, Stratégies
  const pillarData = [
    {
      icon: 'briefcase',
      title: 'MARCHÉ DU\nTRAVAIL',
      stat: '8,1%',
      sub: 'chômage T1 2026',
      color: '#ff0000',
    },
    {
      icon: 'document',
      title: 'NOUVELLES\nLOIS',
      stat: '25 JUIN',
      sub: 'loi anti-fraude 2026',
      color: '#FFC107',
    },
    {
      icon: 'shield',
      title: 'STRATÉGIES\nCONCRÈTES',
      stat: '3 ÉTAPES',
      sub: 'pour se protéger',
      color: '#8FD14F',
    },
  ];

  // Connection line animation
  const connLine = interpolate(frame, [200, 400], [0, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      <div style={{
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: `scale(${cameraZoom})`,
      }}>
        {/* Dynamic gradient background */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `linear-gradient(${gradAngle}deg, #E55500 0%, #FF6600 35%, #D94E00 65%, #FF6600 100%)`,
        }} />

        {/* Animated shape layers */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%' }}>
          <circle
            cx={200 + Math.sin(frame * 0.005) * 50}
            cy={150 + Math.cos(frame * 0.007) * 30}
            r={180}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1.5}
            opacity={0.04}
          />
          <rect
            x={1550 + Math.cos(frame * 0.006) * 30}
            y={750 + Math.sin(frame * 0.008) * 20}
            width={240}
            height={240}
            rx={18}
            fill="none"
            stroke="#8FD14F"
            strokeWidth={1.5}
            opacity={0.05}
            transform={`rotate(${frame * 0.09}, ${1670}, ${870})`}
          />
          <polygon
            points={`${960 + Math.sin(frame * 0.004) * 40},${850} ${1060 + Math.sin(frame * 0.004) * 40},${950} ${860 + Math.sin(frame * 0.004) * 40},${950}`}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={1}
            opacity={0.03}
          />
        </svg>

        {/* Motion texture - hexagonal grid */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.025 }}>
          {Array.from({ length: 8 }).map((_, row) =>
            Array.from({ length: 10 }).map((_, col) => {
              const cx = col * 220 + (row % 2 === 0 ? 0 : 110) + 50;
              const cy = row * 140 + 70;
              const r = 50;
              const pts = Array.from({ length: 6 }).map((__, i) => {
                const a = (i * 60 - 30) * (Math.PI / 180);
                return `${cx + Math.cos(a) * r},${cy + Math.sin(a) * r}`;
              }).join(' ');
              return <polygon key={`hex-${row}-${col}`} points={pts} fill="none" stroke="#F5F5F5" strokeWidth={0.5} />;
            })
          )}
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const px = seededRandom(i * 7 + 45) * 1920;
          const py = seededRandom(i * 11 + 28) * 1080;
          const speed = seededRandom(i * 5 + 21) * 0.3 + 0.1;
          const size = seededRandom(i * 9 + 17) * 4 + 2;
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: px,
              top: (py + frame * speed) % 1160 - 40,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#8FD14F' : '#F5F5F5',
              opacity: seededRandom(i * 4 + 19) * 0.16 + 0.04,
            }} />
          );
        })}

        {/* === MAIN CONTENT === */}

        {/* Title */}
        <div style={{
          position: 'absolute',
          top: 55,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(5)})`,
          opacity: sp(5),
          textAlign: 'center',
        }}>
          <span style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 6,
          }}>
            ÉTAT DES LIEUX
          </span>
        </div>

        {/* Subtitle */}
        <div style={{
          position: 'absolute',
          top: 145,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: sp(20),
        }}>
          <span style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 28,
            color: '#8FD14F',
            fontWeight: 600,
            letterSpacing: 3,
          }}>
            CE QUE CETTE VIDÉO VA COUVRIR
          </span>
        </div>

        {/* Connection line between pillars */}
        <div style={{
          position: 'absolute',
          top: 430,
          left: 310,
          width: 1300,
          height: 4,
          backgroundColor: 'rgba(245,245,245,0.08)',
          borderRadius: 2,
        }}>
          <div style={{
            width: `${connLine}%`,
            height: '100%',
            background: 'linear-gradient(90deg, #ff0000, #FFC107, #8FD14F)',
            borderRadius: 2,
          }} />
        </div>

        {/* Three pillars */}
        {pillarData.map((pillar, i) => {
          const pillarDelay = 40 + i * 50;
          const pillarSpring = sp(pillarDelay);
          const pillarX = 160 + i * 580;

          return (
            <div key={`pillar-${i}`} style={{
              position: 'absolute',
              top: 210,
              left: pillarX,
              width: 480,
              textAlign: 'center',
              transform: `translateY(${interpolate(pillarSpring, [0, 1], [60, 0])}px) scale(${pillarSpring})`,
              opacity: pillarSpring,
            }}>
              {/* Icon */}
              <div style={{
                width: 100,
                height: 100,
                borderRadius: '50%',
                border: `3px solid ${pillar.color}`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                margin: '0 auto 25px',
                backgroundColor: 'rgba(0,0,0,0.3)',
              }}>
                <svg width={48} height={48} viewBox="0 0 48 48">
                  {pillar.icon === 'briefcase' && (
                    <>
                      <rect x={6} y={16} width={36} height={26} rx={4} fill="none" stroke={pillar.color} strokeWidth={3} />
                      <path d="M 16 16 L 16 10 Q 16 6 20 6 L 28 6 Q 32 6 32 10 L 32 16" fill="none" stroke={pillar.color} strokeWidth={3} />
                      <line x1={6} y1={28} x2={42} y2={28} stroke={pillar.color} strokeWidth={2} />
                      <circle cx={24} cy={28} r={3} fill={pillar.color} />
                    </>
                  )}
                  {pillar.icon === 'document' && (
                    <>
                      <rect x={8} y={4} width={28} height={40} rx={3} fill="none" stroke={pillar.color} strokeWidth={3} />
                      <line x1={14} y1={14} x2={30} y2={14} stroke={pillar.color} strokeWidth={2} />
                      <line x1={14} y1={22} x2={28} y2={22} stroke={pillar.color} strokeWidth={2} />
                      <line x1={14} y1={30} x2={24} y2={30} stroke={pillar.color} strokeWidth={2} />
                      <path d="M 36 14 L 36 38 Q 36 44 30 44 L 12 44" fill="none" stroke={pillar.color} strokeWidth={2} opacity={0.4} />
                      {/* Seal/stamp */}
                      <circle cx={30} cy={36} r={5} fill="none" stroke={pillar.color} strokeWidth={1.5} />
                    </>
                  )}
                  {pillar.icon === 'shield' && (
                    <>
                      <path d="M 24 4 L 42 14 L 42 28 Q 42 40 24 46 Q 6 40 6 28 L 6 14 Z" fill="none" stroke={pillar.color} strokeWidth={3} />
                      <path d="M 16 24 L 22 30 L 34 18" fill="none" stroke={pillar.color} strokeWidth={3} strokeLinecap="round" strokeLinejoin="round" />
                    </>
                  )}
                </svg>
              </div>

              {/* Title */}
              <span style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 36,
                color: '#F5F5F5',
                letterSpacing: 3,
                lineHeight: 1.3,
                display: 'block',
                whiteSpace: 'pre-line',
                marginBottom: 20,
              }}>
                {pillar.title}
              </span>

              {/* Stat box */}
              <div style={{
                padding: '16px 30px',
                backgroundColor: 'rgba(0,0,0,0.35)',
                borderRadius: 12,
                border: `2px solid ${pillar.color}`,
                display: 'inline-block',
                marginBottom: 14,
              }}>
                <span style={{
                  fontFamily: 'Clash Display, sans-serif',
                  fontSize: 48,
                  fontWeight: 800,
                  color: pillar.color,
                }}>
                  {pillar.stat}
                </span>
              </div>

              {/* Sub text */}
              <div>
                <span style={{
                  fontFamily: 'Montserrat, sans-serif',
                  fontSize: 22,
                  color: '#F5F5F5',
                  fontWeight: 500,
                  opacity: 0.6,
                }}>
                  {pillar.sub}
                </span>
              </div>

              {/* Connector dot on line */}
              <div style={{
                position: 'absolute',
                top: 218,
                left: '50%',
                transform: 'translateX(-50%)',
                width: 18,
                height: 18,
                borderRadius: '50%',
                backgroundColor: pillar.color,
                border: '3px solid #F5F5F5',
                boxShadow: `0 0 12px ${pillar.color}`,
              }} />
            </div>
          );
        })}

        {/* Bottom CTA line */}
        <div style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) scale(${sp(350)})`,
          opacity: sp(350),
          display: 'flex',
          alignItems: 'center',
          gap: 18,
          padding: '18px 45px',
          backgroundColor: 'rgba(0,0,0,0.35)',
          borderRadius: 14,
          border: '2px solid rgba(143,209,79,0.3)',
        }}>
          {/* Arrow icon */}
          <svg width={36} height={36} viewBox="0 0 36 36">
            <circle cx={18} cy={18} r={16} fill="none" stroke="#8FD14F" strokeWidth={2} />
            <path d="M 14 10 L 24 18 L 14 26" fill="none" stroke="#8FD14F" strokeWidth={3} strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 30,
            color: '#F5F5F5',
            fontWeight: 700,
          }}>
            Analyse complète + stratégies concrètes
          </span>
        </div>

        {/* Vignette overlay */}
        <div style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }} />

        {/* Watermark */}
        <div style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}>
          Matures Finance
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_EtatDesLieux;
```

