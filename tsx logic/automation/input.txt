```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene1_RealExample',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene1_RealExample: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], {
    extrapolateRight: 'clamp',
  });
  const camX = interpolate(frame, [0, durationInFrames], [0, -15], {
    extrapolateRight: 'clamp',
  });

  // Gradient animation
  const gradAngle = interpolate(frame, [0, durationInFrames], [135, 160]);

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => ({
    x: seededRandom(i * 7 + 1) * 1920,
    y: seededRandom(i * 7 + 2) * 1080,
    size: seededRandom(i * 7 + 3) * 4 + 2,
    speed: seededRandom(i * 7 + 4) * 0.6 + 0.2,
    opacity: seededRandom(i * 7 + 5) * 0.3 + 0.1,
  }));

  // Envelope open animation
  const envelopeOpen = sp(15);
  const letterRise = sp(30);
  const profileSlide = sp(45);
  const detailsStagger = [55, 70, 85, 100];

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translateX(${camX}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Dynamic gradient background */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0D1B2A 0%, #1B2838 35%, #1C2D3F 60%, #0A1628 100%)`,
          }}
        />

        {/* Animated shape layers */}
        {/* Large hexagon shape drifting */}
        <div
          style={{
            position: 'absolute',
            top: '10%',
            right: '5%',
            width: 400,
            height: 400,
            opacity: 0.04,
            transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        >
          <svg viewBox="0 0 400 400" fill="none">
            <polygon
              points="200,20 370,110 370,290 200,380 30,290 30,110"
              stroke="#4FC3F7"
              strokeWidth="2"
              fill="none"
            />
          </svg>
        </div>

        {/* Rectangle shape */}
        <div
          style={{
            position: 'absolute',
            bottom: '15%',
            left: '8%',
            width: 300,
            height: 200,
            border: '1.5px solid #FFC107',
            opacity: 0.05,
            borderRadius: 12,
            transform: `rotate(${-frame * 0.1 + 15}deg) translateY(${Math.sin(frame * 0.025) * 20}px)`,
          }}
        />

        {/* Circle shape */}
        <div
          style={{
            position: 'absolute',
            top: '55%',
            right: '20%',
            width: 250,
            height: 250,
            border: '1.5px solid #F5F5F5',
            borderRadius: '50%',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
          }}
        />

        {/* Motion texture - diagonal lines */}
        <svg
          style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}
        >
          {Array.from({ length: 12 }, (_, i) => (
            <line
              key={i}
              x1={i * 180 + frame * 0.3}
              y1={0}
              x2={i * 180 - 200 + frame * 0.3}
              y2={1080}
              stroke="#4FC3F7"
              strokeWidth="1"
            />
          ))}
        </svg>

        {/* Floating particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y + frame * p.speed * 2) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#4FC3F7' : i % 3 === 1 ? '#FFC107' : '#F5F5F5',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Main Content */}
        {/* Email/Mail icon entrance */}
        <div
          style={{
            position: 'absolute',
            top: '8%',
            left: '50%',
            transform: `translateX(-50%) scale(${envelopeOpen}) rotateX(${interpolate(envelopeOpen, [0, 1], [90, 0])}deg)`,
            opacity: envelopeOpen,
          }}
        >
          <svg width="120" height="100" viewBox="0 0 120 100">
            {/* Envelope body */}
            <rect x="5" y="30" width="110" height="65" rx="8" fill="#2D2D2D" stroke="#4FC3F7" strokeWidth="2" />
            {/* Envelope flap */}
            <path
              d="M5 35 L60 70 L115 35"
              fill="none"
              stroke="#4FC3F7"
              strokeWidth="2"
            />
            {/* Letter peeking out */}
            <rect
              x="20"
              y={30 - letterRise * 25}
              width="80"
              height="50"
              rx="4"
              fill="#F5F5F5"
              opacity={letterRise * 0.9}
            />
            <line x1="30" y1={40 - letterRise * 25} x2="90" y2={40 - letterRise * 25} stroke="#9E9E9E" strokeWidth="2" opacity={letterRise} />
            <line x1="30" y1={50 - letterRise * 25} x2="75" y2={50 - letterRise * 25} stroke="#9E9E9E" strokeWidth="1.5" opacity={letterRise} />
          </svg>
        </div>

        {/* "Real Example" title */}
        <div
          style={{
            position: 'absolute',
            top: '22%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(20), [0, 1], [40, 0])}px)`,
            opacity: sp(20),
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 72,
            color: '#F5F5F5',
            textAlign: 'center',
            letterSpacing: 2,
          }}
        >
          REAL <span style={{ color: '#FFC107' }}>EXAMPLE</span>
        </div>

        {/* Profile Card */}
        <div
          style={{
            position: 'absolute',
            top: '38%',
            left: '50%',
            transform: `translateX(-50%) translateX(${interpolate(profileSlide, [0, 1], [-100, 0])}px)`,
            opacity: profileSlide,
            width: 900,
            background: 'linear-gradient(145deg, #2D2D2D 0%, #1C1C1C 100%)',
            borderRadius: 24,
            border: '2px solid #4FC3F7',
            padding: '40px 50px',
            boxShadow: '0 20px 60px rgba(79, 195, 247, 0.15)',
          }}
        >
          {/* Profile header */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginBottom: 30 }}>
            {/* Avatar circle */}
            <div
              style={{
                width: 80,
                height: 80,
                borderRadius: '50%',
                background: 'linear-gradient(135deg, #4FC3F7, #0288D1)',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontSize: 36,
                fontFamily: "'Inter', sans-serif",
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              V
            </div>
            <div>
              <div
                style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: 32,
                  fontWeight: 700,
                  color: '#F5F5F5',
                }}
              >
                ProBuffet HQ Viewer
              </div>
              <div
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontSize: 22,
                  color: '#9E9E9E',
                  marginTop: 4,
                }}
              >
                via Email
              </div>
            </div>
          </div>

          {/* Detail rows */}
          {[
            { label: 'उम्र', value: '28 साल', icon: '👤', color: '#4FC3F7' },
            { label: 'शहर', value: 'पुणे — IT Job', icon: '🏢', color: '#66BB6A' },
            { label: 'Salary', value: '₹45,000/month', icon: '💰', color: '#FFC107' },
          ].map((item, idx) => (
            <div
              key={idx}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 20,
                padding: '14px 0',
                borderBottom: idx < 2 ? '1px solid rgba(245,245,245,0.08)' : 'none',
                opacity: sp(detailsStagger[idx]),
                transform: `translateX(${interpolate(sp(detailsStagger[idx]), [0, 1], [60, 0])}px)`,
              }}
            >
              <div
                style={{
                  width: 50,
                  height: 50,
                  borderRadius: 12,
                  backgroundColor: `${item.color}15`,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontSize: 24,
                }}
              >
                <svg width="28" height="28" viewBox="0 0 28 28">
                  <circle cx="14" cy="14" r="10" fill={item.color} opacity="0.3" />
                  <circle cx="14" cy="14" r="5" fill={item.color} />
                </svg>
              </div>
              <div
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontSize: 28,
                  color: '#9E9E9E',
                  minWidth: 100,
                }}
              >
                {item.label}
              </div>
              <div
                style={{
                  fontFamily: "'Inter', sans-serif",
                  fontSize: 34,
                  fontWeight: 700,
                  color: item.color,
                }}
              >
                {item.value}
              </div>
            </div>
          ))}
        </div>

        {/* Stick figure with curiosity pose */}
        <div
          style={{
            position: 'absolute',
            bottom: '5%',
            right: '8%',
            opacity: sp(60),
            transform: `translateY(${interpolate(sp(60), [0, 1], [50, 0])}px)`,
          }}
        >
          {/* Golden glow */}
          <div
            style={{
              position: 'absolute',
              top: -20,
              left: -20,
              width: 140,
              height: 200,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(255,193,7,0.12) 0%, transparent 70%)',
              filter: 'blur(15px)',
            }}
          />
          <svg width="100" height="160" viewBox="0 0 100 160">
            {/* Head */}
            <circle
              cx="50"
              cy="28"
              r="18"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 50, 28)`}
            />
            {/* Golden glasses */}
            <circle cx="42" cy="26" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <circle cx="58" cy="26" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="48" y1="26" x2="52" y2="26" stroke="#FFC107" strokeWidth="1.5" />
            {/* Body */}
            <line x1="50" y1="46" x2="50" y2="95" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms - one raised thinking */}
            <line x1="50" y1="60" x2="25" y2="80" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="50" y1="60" x2="75" y2="55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="75" y1="55" x2="78" y2="38" stroke="#F5F5F5" strokeWidth="3" />
            {/* Legs */}
            <line
              x1="50"
              y1="95"
              x2="30"
              y2={130 + Math.sin(frame * 0.05) * 2}
              stroke="#F5F5F5"
              strokeWidth="3"
            />
            <line
              x1="50"
              y1="95"
              x2="70"
              y2={130 + Math.sin(frame * 0.05 + 1) * 2}
              stroke="#F5F5F5"
              strokeWidth="3"
            />
            {/* Thinking dots */}
            {[0, 1, 2].map((d) => (
              <circle
                key={d}
                cx={82 + d * 8}
                cy={30 - d * 10}
                r={3 + d}
                fill="#FFC107"
                opacity={interpolate(
                  frame % 60,
                  [d * 12, d * 12 + 20, d * 12 + 40],
                  [0, 0.6, 0],
                  { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
                )}
              />
            ))}
          </svg>
        </div>

        {/* Vignette overlay */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(frame, [0, 30], [0, 0.3], {
              extrapolateRight: 'clamp',
            }),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene1_RealExample;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene2_KabCrorepati',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene2_KabCrorepati: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow pan right
  const camX = interpolate(frame, [0, durationInFrames], [10, -20]);
  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  const gradShift = interpolate(frame, [0, durationInFrames], [200, 250]);

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i * 11 + 1) * 1920,
    y: seededRandom(i * 11 + 2) * 1080,
    size: seededRandom(i * 11 + 3) * 5 + 1.5,
    speed: seededRandom(i * 11 + 4) * 0.5 + 0.3,
    opacity: seededRandom(i * 11 + 5) * 0.25 + 0.08,
  }));

  // SIP card entrance
  const sipCardIn = sp(10);
  // Question mark bounce
  const questionIn = sp(50, { damping: 8, stiffness: 160 });
  // Big question text
  const bigTextIn = sp(70);
  // Nifty 50 badge
  const niftyBadge = sp(30);

  // Counting animation for ₹10,000
  const countVal = Math.round(
    interpolate(frame, [15, 50], [0, 10000], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
      easing: Easing.out(Easing.cubic),
    })
  );

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translateX(${camX}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Dynamic gradient background - warm dark tones */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradShift}deg, #1A0A2E 0%, #16213E 40%, #0F3460 70%, #1A1A2E 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Triangle */}
        <div
          style={{
            position: 'absolute',
            top: '5%',
            left: '60%',
            opacity: 0.05,
            transform: `rotate(${frame * 0.2}deg)`,
          }}
        >
          <svg width="350" height="350" viewBox="0 0 350 350">
            <polygon
              points="175,20 340,320 10,320"
              fill="none"
              stroke="#E040FB"
              strokeWidth="2"
            />
          </svg>
        </div>

        {/* Circle */}
        <div
          style={{
            position: 'absolute',
            bottom: '20%',
            right: '10%',
            width: 280,
            height: 280,
            border: '1.5px solid #FFC107',
            borderRadius: '50%',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06}) rotate(${frame * 0.1}deg)`,
          }}
        />

        {/* Small diamond */}
        <div
          style={{
            position: 'absolute',
            top: '60%',
            left: '5%',
            opacity: 0.06,
            transform: `rotate(${45 + frame * 0.12}deg) translateY(${Math.sin(frame * 0.03) * 15}px)`,
          }}
        >
          <svg width="120" height="120" viewBox="0 0 120 120">
            <rect x="10" y="10" width="100" height="100" fill="none" stroke="#4FC3F7" strokeWidth="1.5" />
          </svg>
        </div>

        {/* Motion texture - concentric circles */}
        <svg
          style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.025 }}
        >
          {Array.from({ length: 6 }, (_, i) => (
            <circle
              key={i}
              cx="960"
              cy="540"
              r={150 + i * 120 + Math.sin(frame * 0.02) * 10}
              fill="none"
              stroke="#E040FB"
              strokeWidth="0.8"
            />
          ))}
        </svg>

        {/* Floating particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (p.x + frame * p.speed) % 1920,
              top: p.y,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 4 === 0 ? '#E040FB' : i % 4 === 1 ? '#4FC3F7' : i % 4 === 2 ? '#FFC107' : '#F5F5F5',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Main Content Area */}
        {/* Left side - SIP Info Card */}
        <div
          style={{
            position: 'absolute',
            top: '15%',
            left: '6%',
            width: 700,
            opacity: sipCardIn,
            transform: `translateY(${interpolate(sipCardIn, [0, 1], [60, 0])}px)`,
          }}
        >
          {/* SIP Amount display */}
          <div
            style={{
              background: 'linear-gradient(135deg, rgba(79,195,247,0.12) 0%, rgba(45,45,45,0.9) 100%)',
              borderRadius: 20,
              border: '1.5px solid rgba(79,195,247,0.3)',
              padding: '35px 45px',
              marginBottom: 25,
            }}
          >
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 30,
                color: '#9E9E9E',
                marginBottom: 10,
              }}
            >
              Monthly SIP
            </div>
            <div
              style={{
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: 100,
                fontWeight: 800,
                color: '#4FC3F7',
                lineHeight: 1,
              }}
            >
              ₹{countVal.toLocaleString('en-IN')}
            </div>
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 26,
                color: '#9E9E9E',
                marginTop: 12,
              }}
            >
              हर महीने
            </div>
          </div>

          {/* Nifty 50 Badge */}
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 15,
              background: 'rgba(255,193,7,0.1)',
              border: '1.5px solid rgba(255,193,7,0.3)',
              borderRadius: 14,
              padding: '15px 30px',
              opacity: niftyBadge,
              transform: `scale(${niftyBadge})`,
            }}
          >
            <svg width="32" height="32" viewBox="0 0 32 32">
              <rect x="2" y="2" width="28" height="28" rx="6" fill="#FFC107" opacity="0.2" />
              <path d="M8 22 L14 12 L20 16 L26 8" stroke="#FFC107" strokeWidth="2.5" fill="none" strokeLinecap="round" />
            </svg>
            <span
              style={{
                fontFamily: "'Inter', sans-serif",
                fontSize: 28,
                fontWeight: 700,
                color: '#FFC107',
              }}
            >
              Nifty 50 Index Fund
            </span>
          </div>
        </div>

        {/* Right side - Big Question */}
        <div
          style={{
            position: 'absolute',
            top: '18%',
            right: '5%',
            width: 550,
            textAlign: 'center',
          }}
        >
          {/* Giant question mark */}
          <div
            style={{
              opacity: questionIn,
              transform: `scale(${interpolate(questionIn, [0, 1], [0.3, 1])}) rotate(${interpolate(questionIn, [0, 1], [-20, 0])}deg)`,
            }}
          >
            <svg width="200" height="260" viewBox="0 0 200 260">
              <defs>
                <linearGradient id="qGrad" x1="0" y1="0" x2="1" y2="1">
                  <stop offset="0%" stopColor="#FFC107" />
                  <stop offset="100%" stopColor="#FF6F00" />
                </linearGradient>
              </defs>
              <text
                x="100"
                y="200"
                textAnchor="middle"
                fill="url(#qGrad)"
                fontSize="240"
                fontFamily="Inter"
                fontWeight="900"
              >
                ?
              </text>
            </svg>
          </div>

          {/* Question text */}
          <div
            style={{
              marginTop: 10,
              opacity: bigTextIn,
              transform: `translateY(${interpolate(bigTextIn, [0, 1], [30, 0])}px)`,
            }}
          >
            <div
              style={{
                fontFamily: "'Yantra One', sans-serif",
                fontSize: 56,
                color: '#F5F5F5',
                lineHeight: 1.3,
              }}
            >
              मैं <span style={{ color: '#FFC107' }}>कब</span>
            </div>
            <div
              style={{
                fontFamily: "'Yantra One', sans-serif",
                fontSize: 68,
                color: '#FFC107',
                lineHeight: 1.2,
                textShadow: '0 0 40px rgba(255,193,7,0.3)',
              }}
            >
              करोड़पती
            </div>
            <div
              style={{
                fontFamily: "'Yantra One', sans-serif",
                fontSize: 56,
                color: '#F5F5F5',
                lineHeight: 1.3,
              }}
            >
              बनूँगा?
            </div>
          </div>
        </div>

        {/* Chat bubble from viewer */}
        <div
          style={{
            position: 'absolute',
            bottom: '12%',
            left: '50%',
            transform: `translateX(-50%) scale(${sp(90)})`,
            opacity: sp(90),
            background: 'rgba(224,64,251,0.1)',
            border: '1.5px solid rgba(224,64,251,0.3)',
            borderRadius: 20,
            padding: '20px 40px',
          }}
        >
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 34,
              color: '#F5F5F5',
              fontWeight: 600,
            }}
          >
            "भाई, मैं कब <span style={{ color: '#E040FB' }}>करोड़पती</span> बनूँगा?"
          </div>
        </div>

        {/* Stick figure - pointing at question */}
        <div
          style={{
            position: 'absolute',
            bottom: '28%',
            left: '42%',
            opacity: sp(55),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="80" height="130" viewBox="0 0 80 130">
            <circle cx="40" cy="22" r="14" fill="none" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 40, 22)`} />
            <circle cx="34" cy="20" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="46" cy="20" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="40" y1="22" x2="42" y2="20" stroke="#FFC107" strokeWidth="1" />
            <line x1="40" y1="36" x2="40" y2="78" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="40" y1="50" x2="18" y2="65" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="40" y1="50" x2="68" y2="42" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="40" y1="78" x2="25" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="40" y1="78" x2="55" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene2_KabCrorepati;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene3_CalculationReveal',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene3_CalculationReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera tilt up
  const camY = interpolate(frame, [0, durationInFrames], [15, -10]);
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  const gradAngle = interpolate(frame, [0, durationInFrames], [180, 220]);

  const particles = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i * 13 + 1) * 1920,
    y: seededRandom(i * 13 + 2) * 1080,
    size: seededRandom(i * 13 + 3) * 4 + 2,
    speed: seededRandom(i * 13 + 4) * 0.4 + 0.2,
    opacity: seededRandom(i * 13 + 5) * 0.2 + 0.08,
  }));

  // Calculator animation
  const calcIn = sp(8);
  // Input rows stagger
  const row1 = sp(20);
  const row2 = sp(35);
  const row3 = sp(50);
  // Result reveal
  const resultIn = sp(75, { damping: 10, stiffness: 140 });
  // Age highlight
  const ageIn = sp(95, { damping: 8, stiffness: 160 });

  // Animated years counter
  const yearsCount = Math.round(
    interpolate(frame, [80, 120], [0, 21], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
      easing: Easing.out(Easing.cubic),
    })
  );

  // Screen shake on result
  const shakeX = frame > 75 && frame < 85 ? Math.sin(frame * 3) * 4 : 0;
  const shakeY = frame > 75 && frame < 85 ? Math.cos(frame * 4) * 3 : 0;

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translateY(${camY}px) translate(${shakeX}px, ${shakeY}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Dynamic gradient - deep green/dark */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0D1F0D 0%, #1C2B1C 30%, #1A2E1A 60%, #0A170A 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Pentagon */}
        <div
          style={{
            position: 'absolute',
            top: '8%',
            left: '3%',
            opacity: 0.05,
            transform: `rotate(${frame * 0.18}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        >
          <svg width="300" height="300" viewBox="0 0 300 300">
            <polygon
              points="150,15 285,105 233,270 67,270 15,105"
              fill="none"
              stroke="#66BB6A"
              strokeWidth="1.5"
            />
          </svg>
        </div>

        {/* Rect */}
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            right: '15%',
            width: 200,
            height: 350,
            border: '1px solid #FFC107',
            borderRadius: 8,
            opacity: 0.04,
            transform: `rotate(${12 + frame * 0.08}deg)`,
          }}
        />

        {/* Motion texture - grid pattern */}
        <svg
          style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}
        >
          {Array.from({ length: 20 }, (_, i) => (
            <React.Fragment key={i}>
              <line x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#66BB6A" strokeWidth="0.5" />
              <line x1={0} y1={i * 60} x2={1920} y2={i * 60} stroke="#66BB6A" strokeWidth="0.5" />
            </React.Fragment>
          ))}
        </svg>

        {/* Floating particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y - frame * p.speed * 1.5 + 1080) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#66BB6A' : i % 3 === 1 ? '#FFC107' : '#F5F5F5',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Calculator Card */}
        <div
          style={{
            position: 'absolute',
            top: '8%',
            left: '50%',
            transform: `translateX(-50%) scale(${calcIn})`,
            opacity: calcIn,
            width: 950,
          }}
        >
          {/* Calculator Header */}
          <div
            style={{
              background: 'linear-gradient(135deg, #2E7D32, #1B5E20)',
              borderRadius: '20px 20px 0 0',
              padding: '20px 40px',
              display: 'flex',
              alignItems: 'center',
              gap: 15,
            }}
          >
            <svg width="32" height="32" viewBox="0 0 32 32">
              <rect x="4" y="2" width="24" height="28" rx="4" fill="none" stroke="#F5F5F5" strokeWidth="2" />
              <rect x="8" y="6" width="16" height="8" rx="2" fill="#66BB6A" opacity="0.4" />
              {[0, 1, 2].map((r) =>
                [0, 1, 2].map((c) => (
                  <rect
                    key={`${r}${c}`}
                    x={9 + c * 6}
                    y={17 + r * 4}
                    width="4"
                    height="2.5"
                    rx="0.5"
                    fill="#F5F5F5"
                    opacity="0.6"
                  />
                ))
              )}
            </svg>
            <span
              style={{
                fontFamily: "'Inter', sans-serif",
                fontSize: 26,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              SIP CALCULATOR
            </span>
          </div>

          {/* Calculator Body */}
          <div
            style={{
              background: 'linear-gradient(180deg, #2D2D2D 0%, #1C1C1C 100%)',
              borderRadius: '0 0 20px 20px',
              padding: '35px 45px',
              border: '1.5px solid rgba(102,187,106,0.2)',
              borderTop: 'none',
            }}
          >
            {/* Input Row 1 - SIP Amount */}
            <div
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                padding: '18px 0',
                borderBottom: '1px solid rgba(245,245,245,0.06)',
                opacity: row1,
                transform: `translateX(${interpolate(row1, [0, 1], [80, 0])}px)`,
              }}
            >
              <span style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 30, color: '#9E9E9E' }}>
                Monthly SIP
              </span>
              <span style={{ fontFamily: "'JetBrains Mono'", fontSize: 36, fontWeight: 700, color: '#66BB6A' }}>
                ₹10,000
              </span>
            </div>

            {/* Input Row 2 - CAGR */}
            <div
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                padding: '18px 0',
                borderBottom: '1px solid rgba(245,245,245,0.06)',
                opacity: row2,
                transform: `translateX(${interpolate(row2, [0, 1], [80, 0])}px)`,
              }}
            >
              <span style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 30, color: '#9E9E9E' }}>
                Expected CAGR
              </span>
              <span style={{ fontFamily: "'JetBrains Mono'", fontSize: 36, fontWeight: 700, color: '#FFC107' }}>
                12%
              </span>
            </div>

            {/* Input Row 3 - Target */}
            <div
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                padding: '18px 0',
                borderBottom: '1px solid rgba(245,245,245,0.06)',
                opacity: row3,
                transform: `translateX(${interpolate(row3, [0, 1], [80, 0])}px)`,
              }}
            >
              <span style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 30, color: '#9E9E9E' }}>
                Target
              </span>
              <span style={{ fontFamily: "'JetBrains Mono'", fontSize: 36, fontWeight: 700, color: '#ff5252' }}>
                ₹1,00,00,000
              </span>
            </div>

            {/* Divider */}
            <div style={{ height: 2, background: 'linear-gradient(90deg, transparent, #66BB6A, transparent)', margin: '25px 0', opacity: resultIn }} />

            {/* Result */}
            <div
              style={{
                textAlign: 'center',
                opacity: resultIn,
                transform: `scale(${interpolate(resultIn, [0, 1], [0.7, 1])})`,
              }}
            >
              <div
                style={{
                  fontFamily: "'Noto Sans Devanagari'",
                  fontSize: 28,
                  color: '#9E9E9E',
                  marginBottom: 10,
                }}
              >
                Time Required
              </div>
              <div
                style={{
                  fontFamily: "'JetBrains Mono'",
                  fontSize: 120,
                  fontWeight: 900,
                  color: '#66BB6A',
                  lineHeight: 1,
                  textShadow: '0 0 60px rgba(102,187,106,0.4)',
                }}
              >
                {yearsCount}
              </div>
              <div
                style={{
                  fontFamily: "'Yantra One'",
                  fontSize: 48,
                  color: '#F5F5F5',
                  marginTop: 5,
                }}
              >
                साल
              </div>
            </div>
          </div>
        </div>

        {/* Age badge - bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: '8%',
            left: '50%',
            transform: `translateX(-50%) scale(${ageIn})`,
            opacity: ageIn,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div
            style={{
              background: 'rgba(255,193,7,0.15)',
              border: '2px solid #FFC107',
              borderRadius: 16,
              padding: '18px 35px',
              display: 'flex',
              alignItems: 'center',
              gap: 15,
            }}
          >
            <svg width="36" height="36" viewBox="0 0 36 36">
              <circle cx="18" cy="12" r="8" fill="none" stroke="#FFC107" strokeWidth="2" />
              <path d="M4 34 C4 24 32 24 32 34" fill="none" stroke="#FFC107" strokeWidth="2" />
            </svg>
            <span style={{ fontFamily: "'Yantra One'", fontSize: 40, color: '#FFC107' }}>
              उम्र 48-49 साल
            </span>
          </div>
        </div>

        {/* Stick figure - calculating */}
        <div
          style={{
            position: 'absolute',
            bottom: '20%',
            right: '5%',
            opacity: sp(40),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="90" height="140" viewBox="0 0 90 140">
            <circle cx="45" cy="22" r="15" fill="none" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 22)`} />
            <circle cx="38" cy="20" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="52" cy="20" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="44" y1="20" x2="46" y2="20" stroke="#FFC107" strokeWidth="1" />
            <line x1="45" y1="37" x2="45" y2="82" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Holding calculator */}
            <line x1="45" y1="52" x2="22" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="52" x2="68" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
            <rect x="14" y="55" width="18" height="22" rx="3" fill="none" stroke="#66BB6A" strokeWidth="1.5" />
            <line x1="45" y1="82" x2="30" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="82" x2="60" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene3_CalculationReveal;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene4_StepUpTimeline',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene4_StepUpTimeline: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const camZoom = interpolate(frame, [0, durationInFrames], [1.03, 1]);
  const camY = interpolate(frame, [0, durationInFrames], [-10, 5]);

  const gradAngle = interpolate(frame, [0, durationInFrames], [140, 180]);

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 17 + 3) * 1920,
    y: seededRandom(i * 17 + 4) * 1080,
    size: seededRandom(i * 17 + 5) * 3.5 + 2,
    speed: seededRandom(i * 17 + 6) * 0.5 + 0.15,
    opacity: seededRandom(i * 17 + 7) * 0.2 + 0.1,
  }));

  // Timeline bars data
  const timelineData = [
    { label: 'Normal SIP', years: 21, color: '#ff5252', age: '48-49' },
    { label: '+₹1000/yr', years: 17, color: '#FFC107', age: '45' },
    { label: '+₹1500/yr', years: 15, color: '#66BB6A', age: '43-44' },
  ];

  // Bar animations
  const barWidth = (years: number, delay: number) => {
    const progress = interpolate(frame, [delay, delay + 40], [0, 1], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
      easing: Easing.out(Easing.cubic),
    });
    return progress * (years / 21) * 700;
  };

  // Arrow animation
  const arrowIn = sp(110);

  // Difference highlight
  const diffPulse = Math.sin(frame * 0.08) * 0.15 + 0.85;

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translateY(${camY}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Gradient background - warm amber */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #1A1200 0%, #2D2200 30%, #1C1800 60%, #0D0A00 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Upward arrow shape */}
        <div
          style={{
            position: 'absolute',
            top: '15%',
            right: '8%',
            opacity: 0.05,
            transform: `translateY(${Math.sin(frame * 0.02) * 15}px) rotate(${frame * 0.1}deg)`,
          }}
        >
          <svg width="200" height="400" viewBox="0 0 200 400">
            <path d="M100 380 L100 50 M100 50 L40 120 M100 50 L160 120" fill="none" stroke="#FFC107" strokeWidth="3" />
          </svg>
        </div>

        {/* Circle */}
        <div
          style={{
            position: 'absolute',
            bottom: '5%',
            left: '15%',
            width: 350,
            height: 350,
            border: '1.5px solid #66BB6A',
            borderRadius: '50%',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.018) * 0.06})`,
          }}
        />

        {/* Motion texture - ascending dots */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.04 }}>
          {Array.from({ length: 15 }, (_, i) =>
            Array.from({ length: 8 }, (_, j) => (
              <circle
                key={`${i}${j}`}
                cx={130 + i * 120}
                cy={1080 - j * 140 - (frame * 0.5) % 140}
                r="2"
                fill="#FFC107"
              />
            ))
          )}
        </svg>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y - frame * p.speed * 2 + 1080) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#FFC107' : '#66BB6A',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: '6%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(5), [0, 1], [40, 0])}px)`,
            opacity: sp(5),
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 70,
              color: '#F5F5F5',
              letterSpacing: 3,
            }}
          >
            STEP-UP SIP का <span style={{ color: '#FFC107' }}>जादू</span>
          </div>
        </div>

        {/* Timeline comparison bars */}
        <div
          style={{
            position: 'absolute',
            top: '25%',
            left: '10%',
            width: 1000,
          }}
        >
          {timelineData.map((item, idx) => (
            <div
              key={idx}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 25,
                marginBottom: 50,
                opacity: sp(20 + idx * 20),
                transform: `translateX(${interpolate(sp(20 + idx * 20), [0, 1], [60, 0])}px)`,
              }}
            >
              {/* Label */}
              <div
                style={{
                  width: 200,
                  fontFamily: "'Inter'",
                  fontSize: 26,
                  fontWeight: 600,
                  color: item.color,
                  textAlign: 'right',
                }}
              >
                {item.label}
              </div>

              {/* Bar */}
              <div
                style={{
                  height: 55,
                  width: barWidth(item.years, 30 + idx * 25),
                  background: `linear-gradient(90deg, ${item.color}22, ${item.color}88)`,
                  border: `2px solid ${item.color}`,
                  borderRadius: 12,
                  position: 'relative',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'flex-end',
                  paddingRight: 20,
                }}
              >
                <span
                  style={{
                    fontFamily: "'JetBrains Mono'",
                    fontSize: 28,
                    fontWeight: 700,
                    color: '#F5F5F5',
                  }}
                >
                  {item.years} साल
                </span>
              </div>

              {/* Age badge */}
              <div
                style={{
                  background: `${item.color}20`,
                  border: `1px solid ${item.color}50`,
                  borderRadius: 10,
                  padding: '8px 18px',
                  fontFamily: "'Noto Sans Devanagari'",
                  fontSize: 22,
                  color: item.color,
                  opacity: sp(70 + idx * 15),
                }}
              >
                उम्र {item.age}
              </div>
            </div>
          ))}
        </div>

        {/* Savings difference highlight */}
        <div
          style={{
            position: 'absolute',
            bottom: '12%',
            left: '50%',
            transform: `translateX(-50%) scale(${sp(100)})`,
            opacity: sp(100) * diffPulse,
          }}
        >
          <div
            style={{
              background: 'linear-gradient(135deg, rgba(102,187,106,0.15), rgba(102,187,106,0.05))',
              border: '2px solid #66BB6A',
              borderRadius: 20,
              padding: '22px 50px',
              display: 'flex',
              alignItems: 'center',
              gap: 20,
            }}
          >
            <svg width="40" height="40" viewBox="0 0 40 40">
              <path d="M20 5 L20 35 M20 5 L10 15 M20 5 L30 15" stroke="#66BB6A" strokeWidth="3" fill="none" strokeLinecap="round" />
            </svg>
            <span
              style={{
                fontFamily: "'Yantra One'",
                fontSize: 42,
                color: '#66BB6A',
              }}
            >
              5-6 साल पहले करोड़पती!
            </span>
            <svg width="40" height="40" viewBox="0 0 40 40">
              <circle cx="20" cy="20" r="15" fill="none" stroke="#FFC107" strokeWidth="2" />
              <path d="M14 20 L18 24 L26 16" stroke="#FFC107" strokeWidth="2.5" fill="none" strokeLinecap="round" />
            </svg>
          </div>
        </div>

        {/* Rocket icon */}
        <div
          style={{
            position: 'absolute',
            top: '18%',
            right: '5%',
            opacity: arrowIn,
            transform: `translateY(${interpolate(arrowIn, [0, 1], [80, 0])}px) rotate(-15deg)`,
          }}
        >
          <svg width="80" height="120" viewBox="0 0 80 120">
            <path d="M40 10 C25 30 20 60 20 80 L40 70 L60 80 C60 60 55 30 40 10Z" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="40" cy="45" r="6" fill="#FFC107" opacity="0.5" />
            <path d="M28 80 L20 100 L40 90 L60 100 L52 80" fill="none" stroke="#ff5252" strokeWidth="2" />
            {/* Flame */}
            <path
              d={`M35 ${95 + Math.sin(frame * 0.3) * 3} Q40 ${115 + Math.sin(frame * 0.4) * 5} 45 ${95 + Math.sin(frame * 0.3 + 1) * 3}`}
              fill="#ff5252"
              opacity="0.6"
            />
          </svg>
        </div>

        {/* Stick figure celebrating */}
        <div
          style={{
            position: 'absolute',
            bottom: '30%',
            right: '10%',
            opacity: sp(50),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: -15,
              left: -15,
              width: 120,
              height: 170,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(255,193,7,0.1) 0%, transparent 70%)',
              filter: 'blur(10px)',
            }}
          />
          <svg width="90" height="140" viewBox="0 0 90 140">
            <circle cx="45" cy="20" r="14" fill="none" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 20)`} />
            <circle cx="38" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="52" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="44" y1="18" x2="46" y2="18" stroke="#FFC107" strokeWidth="1" />
            {/* Smile */}
            <path d="M38 24 Q45 30 52 24" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
            <line x1="45" y1="34" x2="45" y2="78" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Arms up */}
            <line x1="45" y1="50" x2="20" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="50" x2="70" y2="35" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="78" x2="28" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="78" x2="62" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.5) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter'",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene4_StepUpTimeline;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene5_WeaponStepUpSIP',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene5_WeaponStepUpSIP: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const camZoom = interpolate(frame, [0, 100, durationInFrames], [1, 1.08, 1.04]);

  const gradAngle = interpolate(frame, [0, durationInFrames], [160, 200]);

  const particles = Array.from({ length: 35 }, (_, i) => ({
    x: seededRandom(i * 19 + 1) * 1920,
    y: seededRandom(i * 19 + 2) * 1080,
    size: seededRandom(i * 19 + 3) * 4 + 1,
    speed: seededRandom(i * 19 + 4) * 0.7 + 0.2,
    opacity: seededRandom(i * 19 + 5) * 0.25 + 0.08,
  }));

  // Shield/weapon reveal
  const shieldIn = sp(10, { damping: 8, stiffness: 180 });
  // Text "WEAPON"
  const weaponTextIn = sp(25);
  // Step up visual bars (staircase)
  const stairIn = sp(45);
  // Steroid flame effect
  const flameIn = sp(80);
  // Screen shake
  const shakeX = frame > 80 && frame < 92 ? Math.sin(frame * 4) * 5 : 0;
  const shakeY = frame > 80 && frame < 92 ? Math.cos(frame * 5) * 4 : 0;

  // Staircase steps data
  const steps = [
    { year: 'Y1', amount: '₹10K' },
    { year: 'Y2', amount: '₹11K' },
    { year: 'Y3', amount: '₹12.1K' },
    { year: 'Y4', amount: '₹13.3K' },
    { year: 'Y5', amount: '₹14.6K' },
    { year: 'Y6', amount: '₹16.1K' },
    { year: 'Y7', amount: '₹17.7K' },
  ];

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translate(${shakeX}px, ${shakeY}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Gradient - intense dark red/crimson */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #1A0000 0%, #2D0A0A 30%, #1C0505 60%, #0D0000 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Cross/plus shape */}
        <div
          style={{
            position: 'absolute',
            top: '20%',
            left: '5%',
            opacity: 0.05,
            transform: `rotate(${frame * 0.15}deg)`,
          }}
        >
          <svg width="250" height="250" viewBox="0 0 250 250">
            <rect x="100" y="20" width="50" height="210" rx="10" fill="none" stroke="#ff5252" strokeWidth="2" />
            <rect x="20" y="100" width="210" height="50" rx="10" fill="none" stroke="#ff5252" strokeWidth="2" />
          </svg>
        </div>

        {/* Star shape */}
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            right: '8%',
            opacity: 0.04,
            transform: `rotate(${-frame * 0.12}deg) scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
          }}
        >
          <svg width="200" height="200" viewBox="0 0 200 200">
            <polygon
              points="100,10 130,80 200,80 145,125 165,195 100,155 35,195 55,125 0,80 70,80"
              fill="none"
              stroke="#FFC107"
              strokeWidth="1.5"
            />
          </svg>
        </div>

        {/* Motion texture - energy lines */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.04 }}>
          {Array.from({ length: 10 }, (_, i) => {
            const startX = 960;
            const startY = 540;
            const angle = (i / 10) * Math.PI * 2 + frame * 0.01;
            const endX = startX + Math.cos(angle) * 800;
            const endY = startY + Math.sin(angle) * 500;
            return (
              <line key={i} x1={startX} y1={startY} x2={endX} y2={endY} stroke="#ff5252" strokeWidth="1" />
            );
          })}
        </svg>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y - frame * p.speed * 2 + 1080) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: i % 3 === 0 ? '50%' : '2px',
              backgroundColor: i % 3 === 0 ? '#ff5252' : i % 3 === 1 ? '#FFC107' : '#F5F5F5',
              opacity: p.opacity,
              transform: `rotate(${frame * 2 + i * 45}deg)`,
            }}
          />
        ))}

        {/* Shield/Weapon Icon */}
        <div
          style={{
            position: 'absolute',
            top: '5%',
            left: '50%',
            transform: `translateX(-50%) scale(${shieldIn}) rotate(${interpolate(shieldIn, [0, 1], [-30, 0])}deg)`,
            opacity: shieldIn,
          }}
        >
          <svg width="140" height="160" viewBox="0 0 140 160">
            <defs>
              <linearGradient id="shieldGrad" x1="0" y1="0" x2="0" y2="1">
                <stop offset="0%" stopColor="#FFC107" />
                <stop offset="100%" stopColor="#FF6F00" />
              </linearGradient>
            </defs>
            {/* Shield shape */}
            <path
              d="M70 10 L130 35 L125 95 C120 130 70 155 70 155 C70 155 20 130 15 95 L10 35 Z"
              fill="url(#shieldGrad)"
              opacity="0.2"
              stroke="#FFC107"
              strokeWidth="2.5"
            />
            {/* Sword */}
            <line x1="70" y1="45" x2="70" y2="120" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="60" x2="85" y2="60" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="70" cy="42" r="5" fill="#FFC107" />
          </svg>
        </div>

        {/* "WEAPON" Title */}
        <div
          style={{
            position: 'absolute',
            top: '20%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(weaponTextIn, [0, 1], [50, 0])}px)`,
            opacity: weaponTextIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 90,
              color: '#ff5252',
              letterSpacing: 8,
              textShadow: '0 0 60px rgba(255,82,82,0.4)',
            }}
          >
            WEAPON
          </div>
          <div
            style={{
              fontFamily: "'Inter'",
              fontSize: 40,
              fontWeight: 700,
              color: '#FFC107',
              marginTop: 5,
            }}
          >
            Step-Up SIP
          </div>
        </div>

        {/* Staircase visualization */}
        <div
          style={{
            position: 'absolute',
            bottom: '15%',
            left: '8%',
            display: 'flex',
            alignItems: 'flex-end',
            gap: 8,
          }}
        >
          {steps.map((step, idx) => {
            const barH = 80 + idx * 45;
            const delay = 50 + idx * 10;
            const barProgress = interpolate(
              frame,
              [delay, delay + 25],
              [0, 1],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.back(1.2)) }
            );

            return (
              <div
                key={idx}
                style={{
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  gap: 8,
                }}
              >
                {/* Amount label */}
                <div
                  style={{
                    fontFamily: "'JetBrains Mono'",
                    fontSize: 18,
                    fontWeight: 600,
                    color: '#FFC107',
                    opacity: barProgress,
                    transform: `translateY(${interpolate(barProgress, [0, 1], [15, 0])}px)`,
                  }}
                >
                  {step.amount}
                </div>
                {/* Bar */}
                <div
                  style={{
                    width: 90,
                    height: barH * barProgress,
                    background: `linear-gradient(180deg, #FFC107 0%, #FF6F00 100%)`,
                    borderRadius: '8px 8px 4px 4px',
                    opacity: 0.8 + idx * 0.03,
                    boxShadow: idx === steps.length - 1 ? '0 0 30px rgba(255,193,7,0.3)' : 'none',
                  }}
                />
                {/* Year label */}
                <div
                  style={{
                    fontFamily: "'Inter'",
                    fontSize: 16,
                    color: '#9E9E9E',
                    opacity: barProgress,
                  }}
                >
                  {step.year}
                </div>
              </div>
            );
          })}
        </div>

        {/* "Steroid" burst effect */}
        {frame > 80 && (
          <div
            style={{
              position: 'absolute',
              top: '50%',
              right: '12%',
              transform: 'translate(50%, -50%)',
            }}
          >
            {Array.from({ length: 12 }, (_, i) => {
              const angle = (i / 12) * Math.PI * 2;
              const dist = interpolate(frame, [80, 120], [0, 200], {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              });
              return (
                <div
                  key={i}
                  style={{
                    position: 'absolute',
                    left: Math.cos(angle) * dist,
                    top: Math.sin(angle) * dist,
                    width: 8,
                    height: 8,
                    borderRadius: '50%',
                    backgroundColor: i % 2 === 0 ? '#FFC107' : '#ff5252',
                    opacity: interpolate(frame, [80, 120], [1, 0], {
                      extrapolateLeft: 'clamp',
                      extrapolateRight: 'clamp',
                    }),
                  }}
                />
              );
            })}
          </div>
        )}

        {/* Compounding on steroid text */}
        <div
          style={{
            position: 'absolute',
            top: '42%',
            right: '5%',
            width: 500,
            textAlign: 'center',
            opacity: flameIn,
            transform: `scale(${interpolate(flameIn, [0, 1], [0.5, 1])})`,
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 50,
              color: '#F5F5F5',
              lineHeight: 1.3,
            }}
          >
            Compounding
          </div>
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 60,
              color: '#ff5252',
              textShadow: '0 0 40px rgba(255,82,82,0.5)',
            }}
          >
            on STEROID! 💪
          </div>
        </div>

        {/* Stick figure - muscular pose */}
        <div
          style={{
            position: 'absolute',
            bottom: '8%',
            right: '25%',
            opacity: sp(65),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: -20,
              left: -20,
              width: 130,
              height: 180,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(255,82,82,0.12) 0%, transparent 70%)',
              filter: 'blur(12px)',
            }}
          />
          <svg width="90" height="140" viewBox="0 0 90 140">
            <circle cx="45" cy="20" r="14" fill="none" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 20)`} />
            <circle cx="38" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="52" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="44" y1="18" x2="46" y2="18" stroke="#FFC107" strokeWidth="1" />
            <line x1="45" y1="34" x2="45" y2="78" stroke="#F5F5F5" strokeWidth="3" />
            {/* Flexing arms */}
            <line x1="45" y1="48" x2="25" y2="38" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="25" y1="38" x2="20" y2="52" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="48" x2="65" y2="38" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="65" y1="38" x2="70" y2="52" stroke="#F5F5F5" strokeWidth="3" />
            {/* Muscle bumps */}
            <circle cx="22" cy="43" r="5" fill="none" stroke="#ff5252" strokeWidth="1.5" opacity="0.6" />
            <circle cx="68" cy="43" r="5" fill="none" stroke="#ff5252" strokeWidth="1.5" opacity="0.6" />
            <line x1="45" y1="78" x2="30" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="78" x2="60" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter'",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene5_WeaponStepUpSIP;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene6_AppSetup',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene6_AppSetup: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.06]);
  const camX = interpolate(frame, [0, durationInFrames], [5, -10]);
  const gradAngle = interpolate(frame, [0, durationInFrames], [120, 170]);

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 23 + 1) * 1920,
    y: seededRandom(i * 23 + 2) * 1080,
    size: seededRandom(i * 23 + 3) * 3 + 1.5,
    speed: seededRandom(i * 23 + 4) * 0.4 + 0.2,
    opacity: seededRandom(i * 23 + 5) * 0.2 + 0.08,
  }));

  // App cards data
  const apps = [
    { name: 'Groww', color: '#00BFA5', icon: 'G' },
    { name: 'Zerodha Coin', color: '#387ED1', icon: 'Z' },
    { name: 'Kuvera', color: '#7C4DFF', icon: 'K' },
  ];

  // Phone mockup entrance
  const phoneIn = sp(10);
  // Toggle switch animation
  const toggleProgress = interpolate(frame, [90, 120], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  // Future self text
  const futureIn = sp(130);

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translateX(${camX}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Gradient background - cool tech blue-purple */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0A0A1A 0%, #151530 30%, #1A1040 60%, #0D0D20 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Rounded rectangle */}
        <div
          style={{
            position: 'absolute',
            top: '25%',
            right: '5%',
            width: 180,
            height: 320,
            border: '1.5px solid #7C4DFF',
            borderRadius: 30,
            opacity: 0.05,
            transform: `rotate(${8 + frame * 0.08}deg) translateY(${Math.sin(frame * 0.02) * 10}px)`,
          }}
        />

        {/* Circle */}
        <div
          style={{
            position: 'absolute',
            top: '5%',
            left: '40%',
            width: 400,
            height: 400,
            border: '1px solid #00BFA5',
            borderRadius: '50%',
            opacity: 0.03,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />

        {/* Motion texture - dots matrix */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.025 }}>
          {Array.from({ length: 12 }, (_, i) =>
            Array.from({ length: 8 }, (_, j) => (
              <circle
                key={`${i}${j}`}
                cx={80 + i * 160}
                cy={60 + j * 140}
                r="2"
                fill="#7C4DFF"
              />
            ))
          )}
        </svg>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (p.x + frame * p.speed) % 1920,
              top: p.y,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#7C4DFF' : i % 3 === 1 ? '#00BFA5' : '#387ED1',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Left side - App Cards */}
        <div
          style={{
            position: 'absolute',
            top: '12%',
            left: '6%',
            width: 550,
          }}
        >
          {/* Title */}
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 52,
              color: '#F5F5F5',
              marginBottom: 35,
              opacity: sp(5),
              transform: `translateY(${interpolate(sp(5), [0, 1], [30, 0])}px)`,
            }}
          >
            सेट करो, <span style={{ color: '#7C4DFF' }}>भूल जाओ</span>
          </div>

          {/* App cards */}
          {apps.map((app, idx) => (
            <div
              key={idx}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 20,
                marginBottom: 22,
                background: `${app.color}10`,
                border: `1.5px solid ${app.color}40`,
                borderRadius: 16,
                padding: '18px 28px',
                opacity: sp(25 + idx * 18),
                transform: `translateX(${interpolate(sp(25 + idx * 18), [0, 1], [-60, 0])}px)`,
              }}
            >
              {/* App icon */}
              <div
                style={{
                  width: 56,
                  height: 56,
                  borderRadius: 14,
                  backgroundColor: app.color,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: "'Inter'",
                  fontSize: 28,
                  fontWeight: 800,
                  color: '#F5F5F5',
                }}
              >
                {app.icon}
              </div>
              <div style={{ flex: 1 }}>
                <div style={{ fontFamily: "'Inter'", fontSize: 26, fontWeight: 700, color: '#F5F5F5' }}>
                  {app.name}
                </div>
                <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 18, color: '#9E9E9E', marginTop: 2 }}>
                  Step-Up SIP Available ✓
                </div>
              </div>
              {/* Checkmark */}
              <svg width="32" height="32" viewBox="0 0 32 32">
                <circle cx="16" cy="16" r="14" fill={`${app.color}30`} stroke={app.color} strokeWidth="2" />
                <path d="M10 16 L14 20 L22 12" stroke={app.color} strokeWidth="2.5" fill="none" strokeLinecap="round" />
              </svg>
            </div>
          ))}
        </div>

        {/* Right side - Phone mockup with toggle */}
        <div
          style={{
            position: 'absolute',
            top: '10%',
            right: '10%',
            opacity: phoneIn,
            transform: `scale(${phoneIn}) rotate(${interpolate(phoneIn, [0, 1], [5, 0])}deg)`,
          }}
        >
          {/* Phone frame */}
          <div
            style={{
              width: 320,
              height: 560,
              background: '#2D2D2D',
              borderRadius: 40,
              border: '3px solid #444',
              padding: '50px 20px 30px',
              position: 'relative',
              boxShadow: '0 30px 80px rgba(0,0,0,0.5)',
            }}
          >
            {/* Notch */}
            <div
              style={{
                position: 'absolute',
                top: 12,
                left: '50%',
                transform: 'translateX(-50%)',
                width: 80,
                height: 6,
                borderRadius: 10,
                backgroundColor: '#555',
              }}
            />

            {/* Screen content */}
            <div style={{ padding: '10px' }}>
              <div style={{ fontFamily: "'Inter'", fontSize: 16, color: '#9E9E9E', marginBottom: 20 }}>
                SIP Settings
              </div>

              {/* Step-Up Toggle */}
              <div
                style={{
                  background: '#1C1C1C',
                  borderRadius: 14,
                  padding: '16px 18px',
                  marginBottom: 15,
                  display: 'flex',
                  justifyContent: 'space-between',
                  alignItems: 'center',
                }}
              >
                <span style={{ fontFamily: "'Inter'", fontSize: 15, color: '#F5F5F5', fontWeight: 600 }}>
                  Step-Up SIP
                </span>
                {/* Toggle switch */}
                <div
                  style={{
                    width: 50,
                    height: 28,
                    borderRadius: 14,
                    background: toggleProgress > 0.5 ? '#66BB6A' : '#555',
                    position: 'relative',
                    cursor: 'pointer',
                    transition: 'background 0.3s',
                  }}
                >
                  <div
                    style={{
                      width: 22,
                      height: 22,
                      borderRadius: '50%',
                      background: '#F5F5F5',
                      position: 'absolute',
                      top: 3,
                      left: 3 + toggleProgress * 22,
                      boxShadow: '0 2px 4px rgba(0,0,0,0.3)',
                    }}
                  />
                </div>
              </div>

              {/* Increment setting */}
              <div
                style={{
                  background: '#1C1C1C',
                  borderRadius: 14,
                  padding: '16px 18px',
                  marginBottom: 15,
                  opacity: toggleProgress,
                }}
              >
                <div style={{ fontFamily: "'Inter'", fontSize: 13, color: '#9E9E9E', marginBottom: 8 }}>
                  Yearly Increment
                </div>
                <div style={{ fontFamily: "'JetBrains Mono'", fontSize: 28, fontWeight: 700, color: '#FFC107' }}>
                  10%
                </div>
              </div>

              {/* Confirm button */}
              <div
                style={{
                  background: 'linear-gradient(135deg, #7C4DFF, #536DFE)',
                  borderRadius: 12,
                  padding: '14px',
                  textAlign: 'center',
                  fontFamily: "'Inter'",
                  fontSize: 16,
                  fontWeight: 700,
                  color: '#F5F5F5',
                  marginTop: 20,
                  opacity: toggleProgress,
                  transform: `scale(${0.95 + toggleProgress * 0.05})`,
                }}
              >
                Activate Step-Up ✓
              </div>
            </div>
          </div>
        </div>

        {/* Bottom - Future self message */}
        <div
          style={{
            position: 'absolute',
            bottom: '8%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(futureIn, [0, 1], [40, 0])}px)`,
            opacity: futureIn,
          }}
        >
          <div
            style={{
              background: 'linear-gradient(135deg, rgba(124,77,255,0.12), rgba(83,109,254,0.08))',
              border: '1.5px solid rgba(124,77,255,0.3)',
              borderRadius: 18,
              padding: '20px 45px',
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: "'Yantra One'",
                fontSize: 38,
                color: '#F5F5F5',
              }}
            >
              Future Self तुम्हें <span style={{ color: '#FFC107' }}>THANK</span> करेगा!
            </div>
          </div>
        </div>

        {/* Stick figure - thumbs up */}
        <div
          style={{
            position: 'absolute',
            bottom: '22%',
            left: '30%',
            opacity: sp(70),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="70" height="110" viewBox="0 0 70 110">
            <circle cx="35" cy="18" r="12" fill="none" stroke="#F5F5F5" strokeWidth="2" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 35, 18)`} />
            <circle cx="29" cy="16" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="41" cy="16" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="35" y1="16" x2="36" y2="16" stroke="#FFC107" strokeWidth="1" />
            <path d="M29 22 Q35 27 41 22" fill="none" stroke="#F5F5F5" strokeWidth="1.2" />
            <line x1="35" y1="30" x2="35" y2="65" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="35" y1="42" x2="15" y2="55" stroke="#F5F5F5" strokeWidth="2" />
            {/* Thumbs up arm */}
            <line x1="35" y1="42" x2="55" y2="32" stroke="#F5F5F5" strokeWidth="2" />
            <circle cx="55" cy="28" r="5" fill="none" stroke="#66BB6A" strokeWidth="1.5" />
            <line x1="55" y1="23" x2="55" y2="19" stroke="#66BB6A" strokeWidth="2" />
            <line x1="35" y1="65" x2="22" y2="90" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="35" y1="65" x2="48" y2="90" stroke="#F5F5F5" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.5) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter'",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene6_AppSetup;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene7_AssetAllocation',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene7_AssetAllocation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);
  const gradAngle = interpolate(frame, [0, durationInFrames], [150, 190]);

  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i * 29 + 1) * 1920,
    y: seededRandom(i * 29 + 2) * 1080,
    size: seededRandom(i * 29 + 3) * 3 + 2,
    speed: seededRandom(i * 29 + 4) * 0.3 + 0.15,
    opacity: seededRandom(i * 29 + 5) * 0.2 + 0.06,
  }));

  // Pie chart animation
  const pieIn = sp(20);
  const equitySlice = interpolate(frame, [30, 70], [0, 252], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  }); // 70% of 360
  const debtSlice = interpolate(frame, [55, 95], [0, 108], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  }); // 30% of 360

  // Shield animation
  const shieldIn = sp(100);

  // "Boring but..." text
  const boringIn = sp(5);
  const butIn = sp(60);

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom})`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Gradient - deep navy blue */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0A0E1A 0%, #0F1A30 30%, #0D1525 60%, #050810 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Large ring */}
        <div
          style={{
            position: 'absolute',
            top: '30%',
            left: '60%',
            width: 500,
            height: 500,
            border: '2px solid #1565C0',
            borderRadius: '50%',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.015) * 0.05}) rotate(${frame * 0.08}deg)`,
          }}
        />

        {/* Diamond */}
        <div
          style={{
            position: 'absolute',
            top: '10%',
            left: '8%',
            opacity: 0.05,
            transform: `rotate(${45 + frame * 0.12}deg) translateY(${Math.sin(frame * 0.025) * 12}px)`,
          }}
        >
          <svg width="180" height="180" viewBox="0 0 180 180">
            <rect x="20" y="20" width="140" height="140" fill="none" stroke="#FFC107" strokeWidth="1.5" rx="8" />
          </svg>
        </div>

        {/* Motion texture - horizontal scan lines */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.02 }}>
          {Array.from({ length: 25 }, (_, i) => (
            <line
              key={i}
              x1={0}
              y1={i * 45 + (frame * 0.3) % 45}
              x2={1920}
              y2={i * 45 + (frame * 0.3) % 45}
              stroke="#4FC3F7"
              strokeWidth="0.5"
            />
          ))}
        </svg>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y + frame * p.speed) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#1565C0' : i % 3 === 1 ? '#FFC107' : '#66BB6A',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Top text - "BORING word" with cross-out */}
        <div
          style={{
            position: 'absolute',
            top: '6%',
            left: '50%',
            transform: `translateX(-50%)`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Inter'",
              fontSize: 36,
              color: '#9E9E9E',
              opacity: boringIn,
              transform: `translateY(${interpolate(boringIn, [0, 1], [20, 0])}px)`,
              position: 'relative',
              display: 'inline-block',
            }}
          >
            "Boring Word"
            {/* Strike through */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 3,
                backgroundColor: '#ff5252',
                width: `${interpolate(frame, [40, 60], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}%`,
              }}
            />
          </div>
        </div>

        {/* Main title */}
        <div
          style={{
            position: 'absolute',
            top: '14%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(butIn, [0, 1], [50, 0])}px)`,
            opacity: butIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 80,
              color: '#F5F5F5',
              letterSpacing: 4,
            }}
          >
            ASSET <span style={{ color: '#1565C0' }}>ALLOCATION</span>
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari'",
              fontSize: 34,
              color: '#FFC107',
              marginTop: 8,
            }}
          >
            Market Crash में तुम्हारी ढाल
          </div>
        </div>

        {/* Pie Chart - center */}
        <div
          style={{
            position: 'absolute',
            top: '38%',
            left: '25%',
            transform: `translateX(-50%) scale(${pieIn})`,
            opacity: pieIn,
          }}
        >
          <svg width="340" height="340" viewBox="-170 -170 340 340">
            {/* Equity slice - blue */}
            <circle
              r="130"
              fill="none"
              stroke="#1565C0"
              strokeWidth="50"
              strokeDasharray={`${equitySlice} ${360 - equitySlice}`}
              strokeDashoffset="90"
              strokeLinecap="round"
            />
            {/* Debt slice - gold */}
            <circle
              r="130"
              fill="none"
              stroke="#FFC107"
              strokeWidth="50"
              strokeDasharray={`${debtSlice} ${360 - debtSlice}`}
              strokeDashoffset={90 - equitySlice}
              strokeLinecap="round"
            />
            {/* Center text */}
            <text
              x="0"
              y="-10"
              textAnchor="middle"
              fill="#F5F5F5"
              fontSize="36"
              fontFamily="Inter"
              fontWeight="800"
            >
              70:30
            </text>
            <text
              x="0"
              y="25"
              textAnchor="middle"
              fill="#9E9E9E"
              fontSize="18"
              fontFamily="Inter"
            >
              Equity : Debt
            </text>
          </svg>

          {/* Legend */}
          <div style={{ display: 'flex', gap: 30, marginTop: 20, justifyContent: 'center' }}>
            {[
              { label: 'Equity 70%', color: '#1565C0' },
              { label: 'Debt 30%', color: '#FFC107' },
            ].map((item, idx) => (
              <div
                key={idx}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  gap: 10,
                  opacity: sp(80 + idx * 15),
                }}
              >
                <div
                  style={{
                    width: 16,
                    height: 16,
                    borderRadius: 4,
                    backgroundColor: item.color,
                  }}
                />
                <span
                  style={{
                    fontFamily: "'Inter'",
                    fontSize: 20,
                    color: '#F5F5F5',
                    fontWeight: 600,
                  }}
                >
                  {item.label}
                </span>
              </div>
            ))}
          </div>
        </div>

        {/* Shield - right side */}
        <div
          style={{
            position: 'absolute',
            top: '35%',
            right: '12%',
            opacity: shieldIn,
            transform: `scale(${shieldIn}) translateY(${Math.sin(frame * 0.03) * 8}px)`,
          }}
        >
          <svg width="260" height="300" viewBox="0 0 260 300">
            <defs>
              <linearGradient id="shieldG2" x1="0" y1="0" x2="0" y2="1">
                <stop offset="0%" stopColor="#1565C0" />
                <stop offset="100%" stopColor="#0D47A1" />
              </linearGradient>
            </defs>
            {/* Shield */}
            <path
              d="M130 15 L245 55 L235 175 C225 240 130 285 130 285 C130 285 35 240 25 175 L15 55 Z"
              fill="url(#shieldG2)"
              opacity="0.2"
              stroke="#1565C0"
              strokeWidth="3"
            />
            {/* Cross/plus inside shield */}
            <line x1="130" y1="80" x2="130" y2="200" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="80" y1="140" x2="180" y2="140" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            {/* Checkmark over */}
            <path
              d="M90 140 L120 170 L175 100"
              fill="none"
              stroke="#66BB6A"
              strokeWidth="5"
              strokeLinecap="round"
              strokeDasharray={interpolate(frame, [110, 140], [0, 200], {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              })}
            />
          </svg>

          {/* Text below shield */}
          <div
            style={{
              textAlign: 'center',
              marginTop: 15,
              fontFamily: "'Yantra One'",
              fontSize: 36,
              color: '#1565C0',
              opacity: sp(120),
            }}
          >
            CRASH PROTECTION
          </div>
        </div>

        {/* Stick figure - holding shield */}
        <div
          style={{
            position: 'absolute',
            bottom: '8%',
            left: '15%',
            opacity: sp(50),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: -15,
              left: -15,
              width: 120,
              height: 170,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(21,101,192,0.1) 0%, transparent 70%)',
              filter: 'blur(12px)',
            }}
          />
          <svg width="90" height="140" viewBox="0 0 90 140">
            <circle cx="45" cy="20" r="14" fill="none" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 20)`} />
            <circle cx="38" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="52" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="44" y1="18" x2="46" y2="18" stroke="#FFC107" strokeWidth="1" />
            <line x1="45" y1="34" x2="45" y2="80" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Left arm holding mini shield */}
            <line x1="45" y1="50" x2="18" y2="55" stroke="#F5F5F5" strokeWidth="2.5" />
            <path d="M10 45 L18 42 L18 68 L10 65 Z" fill="none" stroke="#1565C0" strokeWidth="1.5" />
            {/* Right arm */}
            <line x1="45" y1="50" x2="70" y2="62" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="80" x2="30" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="80" x2="60" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter'",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene7_AssetAllocation;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene8_CovidCrashComparison',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene8_CovidCrashComparison: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Shake effect for crash
  const shakeIntensity = interpolate(frame, [20, 30, 50], [0, 8, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const shakeX = Math.sin(frame * 5) * shakeIntensity;
  const shakeY = Math.cos(frame * 6) * shakeIntensity;

  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 1]);
  const gradAngle = interpolate(frame, [0, durationInFrames], [170, 210]);

  const particles = Array.from({ length: 30 }, (_, i) => ({
    x: seededRandom(i * 31 + 1) * 1920,
    y: seededRandom(i * 31 + 2) * 1080,
    size: seededRandom(i * 31 + 3) * 4 + 1,
    speed: seededRandom(i * 31 + 4) * 0.5 + 0.2,
    opacity: seededRandom(i * 31 + 5) * 0.2 + 0.05,
  }));

  // Crash line animation
  const crashLine = interpolate(frame, [15, 60], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Comparison cards
  const card1In = sp(65);
  const card2In = sp(85);

  // Drop percentages
  const drop100 = Math.round(
    interpolate(frame, [70, 100], [0, 40], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );
  const drop70 = Math.round(
    interpolate(frame, [90, 120], [0, 26], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translate(${shakeX}px, ${shakeY}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Gradient - blood red/dark */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0D0000 0%, #1A0505 25%, #200808 50%, #0A0000 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Broken circle */}
        <div
          style={{
            position: 'absolute',
            top: '5%',
            right: '10%',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        >
          <svg width="300" height="300" viewBox="0 0 300 300">
            <circle cx="150" cy="150" r="130" fill="none" stroke="#ff5252" strokeWidth="2" strokeDasharray="30 20" />
          </svg>
        </div>

        {/* Downward arrow shape */}
        <div
          style={{
            position: 'absolute',
            bottom: '15%',
            left: '5%',
            opacity: 0.06,
            transform: `translateY(${Math.sin(frame * 0.02) * 10}px)`,
          }}
        >
          <svg width="100" height="300" viewBox="0 0 100 300">
            <path d="M50 0 L50 250 M50 250 L20 200 M50 250 L80 200" stroke="#ff5252" strokeWidth="2" fill="none" />
          </svg>
        </div>

        {/* Motion texture - falling dots */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}>
          {Array.from({ length: 40 }, (_, i) => (
            <circle
              key={i}
              cx={seededRandom(i * 7) * 1920}
              cy={(seededRandom(i * 7 + 1) * 1080 + frame * 3) % 1080}
              r="2"
              fill="#ff5252"
            />
          ))}
        </svg>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y + frame * p.speed * 3) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#ff5252' : '#FFC107',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* COVID CRASH 2020 Title */}
        <div
          style={{
            position: 'absolute',
            top: '4%',
            left: '50%',
            transform: `translateX(-50%) scale(${sp(5)})`,
            opacity: sp(5),
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Inter'",
              fontSize: 28,
              color: '#9E9E9E',
              letterSpacing: 6,
              marginBottom: 5,
            }}
          >
            MARCH 2020
          </div>
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 80,
              color: '#ff5252',
              letterSpacing: 4,
              textShadow: '0 0 50px rgba(255,82,82,0.4)',
            }}
          >
            COVID CRASH
          </div>
        </div>

        {/* Crash chart line */}
        <div
          style={{
            position: 'absolute',
            top: '25%',
            left: '10%',
            width: '80%',
            height: 200,
          }}
        >
          <svg width="100%" height="200" viewBox="0 0 1600 200" preserveAspectRatio="none">
            <defs>
              <linearGradient id="crashGrad" x1="0" y1="0" x2="1" y2="0">
                <stop offset="0%" stopColor="#66BB6A" />
                <stop offset="40%" stopColor="#FFC107" />
                <stop offset="100%" stopColor="#ff5252" />
              </linearGradient>
            </defs>
            {/* Market line - high to crash */}
            <path
              d={`M0 60 Q200 50 400 55 Q600 45 800 40 Q900 42 1000 80 Q1100 130 1200 170 Q1300 185 1400 180 Q1500 175 1600 165`}
              fill="none"
              stroke="url(#crashGrad)"
              strokeWidth="4"
              strokeDasharray={2000}
              strokeDashoffset={2000 - crashLine * 2000}
              strokeLinecap="round"
            />
            {/* Drop arrow */}
            {crashLine > 0.6 && (
              <>
                <line x1="800" y1="40" x2="1200" y2="170" stroke="#ff5252" strokeWidth="1.5" strokeDasharray="8 6" opacity="0.6" />
                <text x="950" y="100" fill="#ff5252" fontSize="24" fontFamily="JetBrains Mono" fontWeight="700" opacity={crashLine}>
                  -40%
                </text>
              </>
            )}
          </svg>
        </div>

        {/* Comparison Cards */}
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            gap: 50,
          }}
        >
          {/* Card 1 - 100% Equity */}
          <div
            style={{
              width: 400,
              background: 'linear-gradient(135deg, rgba(255,82,82,0.1), rgba(45,45,45,0.8))',
              border: '2px solid #ff5252',
              borderRadius: 20,
              padding: '30px 35px',
              opacity: card1In,
              transform: `translateY(${interpolate(card1In, [0, 1], [50, 0])}px)`,
            }}
          >
            <div style={{ fontFamily: "'Inter'", fontSize: 22, color: '#9E9E9E', marginBottom: 8 }}>
              100% Equity Portfolio
            </div>
            <div
              style={{
                fontFamily: "'JetBrains Mono'",
                fontSize: 80,
                fontWeight: 900,
                color: '#ff5252',
                lineHeight: 1,
              }}
            >
              -{drop100}%
            </div>
            <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 24, color: '#ff5252', marginTop: 10 }}>
              आधा हो गया 😰
            </div>
            {/* Danger icon */}
            <svg width="40" height="40" viewBox="0 0 40 40" style={{ position: 'absolute', top: 15, right: 15 }}>
              <polygon points="20,4 38,36 2,36" fill="none" stroke="#ff5252" strokeWidth="2.5" />
              <text x="20" y="30" textAnchor="middle" fill="#ff5252" fontSize="20" fontWeight="900">!</text>
            </svg>
          </div>

          {/* VS */}
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              fontFamily: "'Inter'",
              fontSize: 36,
              fontWeight: 900,
              color: '#FFC107',
              opacity: sp(75),
            }}
          >
            VS
          </div>

          {/* Card 2 - 70:30 */}
          <div
            style={{
              width: 400,
              background: 'linear-gradient(135deg, rgba(102,187,106,0.1), rgba(45,45,45,0.8))',
              border: '2px solid #66BB6A',
              borderRadius: 20,
              padding: '30px 35px',
              opacity: card2In,
              transform: `translateY(${interpolate(card2In, [0, 1], [50, 0])}px)`,
              position: 'relative',
            }}
          >
            <div style={{ fontFamily: "'Inter'", fontSize: 22, color: '#9E9E9E', marginBottom: 8 }}>
              70% Equity + 30% Debt
            </div>
            <div
              style={{
                fontFamily: "'JetBrains Mono'",
                fontSize: 80,
                fontWeight: 900,
                color: '#66BB6A',
                lineHeight: 1,
              }}
            >
              -{drop70}%
            </div>
            <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 24, color: '#66BB6A', marginTop: 10 }}>
              Manageable! ✅
            </div>
            {/* Shield icon */}
            <svg width="40" height="44" viewBox="0 0 40 44" style={{ position: 'absolute', top: 15, right: 15 }}>
              <path d="M20 2 L38 10 L36 28 C34 36 20 42 20 42 C20 42 6 36 4 28 L2 10 Z" fill="none" stroke="#66BB6A" strokeWidth="2" />
              <path d="M14 22 L18 26 L26 18" stroke="#66BB6A" strokeWidth="2.5" fill="none" strokeLinecap="round" />
            </svg>
          </div>
        </div>

        {/* Stick figure - scared on left, calm on right */}
        <div
          style={{
            position: 'absolute',
            bottom: '35%',
            left: '22%',
            opacity: sp(70),
          }}
        >
          <svg width="60" height="90" viewBox="0 0 60 90">
            {/* Scared figure */}
            <circle cx="30" cy="15" r="10" fill="none" stroke="#ff5252" strokeWidth="2" />
            {/* Scared eyes */}
            <circle cx="26" cy="13" r="2" fill="#ff5252" />
            <circle cx="34" cy="13" r="2" fill="#ff5252" />
            <path d="M25 20 Q30 23 35 20" fill="none" stroke="#ff5252" strokeWidth="1.2" />
            <line x1="30" y1="25" x2="30" y2="55" stroke="#ff5252" strokeWidth="2" />
            {/* Hands on head */}
            <line x1="30" y1="35" x2="15" y2="18" stroke="#ff5252" strokeWidth="2" />
            <line x1="30" y1="35" x2="45" y2="18" stroke="#ff5252" strokeWidth="2" />
            <line x1="30" y1="55" x2="20" y2="75" stroke="#ff5252" strokeWidth="2" />
            <line x1="30" y1="55" x2="40" y2="75" stroke="#ff5252" strokeWidth="2" />
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            bottom: '35%',
            right: '22%',
            opacity: sp(90),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="60" height="90" viewBox="0 0 60 90">
            {/* Calm figure */}
            <circle cx="30" cy="15" r="10" fill="none" stroke="#66BB6A" strokeWidth="2" />
            <circle cx="26" cy="13" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="34" cy="13" r="4" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <path d="M25 19 Q30 23 35 19" fill="none" stroke="#66BB6A" strokeWidth="1.2" />
            <line x1="30" y1="25" x2="30" y2="55" stroke="#66BB6A" strokeWidth="2" />
            <line x1="30" y1="35" x2="12" y2="45" stroke="#66BB6A" strokeWidth="2" />
            <line x1="30" y1="35" x2="48" y2="45" stroke="#66BB6A" strokeWidth="2" />
            <line x1="30" y1="55" x2="20" y2="75" stroke="#66BB6A" strokeWidth="2" />
            <line x1="30" y1="55" x2="40" y2="75" stroke="#66BB6A" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter'",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene8_CovidCrashComparison;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene9_SurviveRule',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene9_SurviveRule: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera orbit effect
  const camX = Math.sin(frame * 0.008) * 15;
  const camY = Math.cos(frame * 0.006) * 8;
  const camZoom = interpolate(frame, [0, 120, durationInFrames], [1, 1.1, 1.05]);

  const gradAngle = interpolate(frame, [0, durationInFrames], [160, 200]);

  const particles = Array.from({ length: 35 }, (_, i) => ({
    x: seededRandom(i * 37 + 1) * 1920,
    y: seededRandom(i * 37 + 2) * 1080,
    size: seededRandom(i * 37 + 3) * 5 + 1,
    speed: seededRandom(i * 37 + 4) * 0.4 + 0.15,
    opacity: seededRandom(i * 37 + 5) * 0.2 + 0.05,
  }));

  // Heart beat pulse for "survive"
  const heartBeat =
    1 +
    Math.sin(frame * 0.15) * 0.03 *
      interpolate(frame, [100, 120], [0, 1], {
        extrapolateLeft: 'clamp',
        extrapolateRight: 'clamp',
      });

  // SURVIVE text massive reveal
  const surviveIn = sp(90, { damping: 8, stiffness: 200 });

  // Shield glow pulse
  const glowPulse = 0.3 + Math.sin(frame * 0.06) * 0.15;

  // Rule text
  const ruleIn = sp(130);

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translate(${camX}px, ${camY}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Gradient - deep dark with subtle warmth */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0A0A0A 0%, #1A1510 30%, #151210 60%, #080808 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Large heart shape outline */}
        <div
          style={{
            position: 'absolute',
            top: '10%',
            left: '50%',
            transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
            opacity: 0.04,
          }}
        >
          <svg width="600" height="500" viewBox="0 0 600 500">
            <path
              d="M300 450 C300 450 50 280 50 150 C50 70 120 20 200 20 C250 20 280 50 300 80 C320 50 350 20 400 20 C480 20 550 70 550 150 C550 280 300 450 300 450Z"
              fill="none"
              stroke="#ff5252"
              strokeWidth="2"
            />
          </svg>
        </div>

        {/* Triangle */}
        <div
          style={{
            position: 'absolute',
            bottom: '20%',
            right: '5%',
            opacity: 0.04,
            transform: `rotate(${frame * 0.15}deg)`,
          }}
        >
          <svg width="200" height="200" viewBox="0 0 200 200">
            <polygon points="100,10 190,180 10,180" fill="none" stroke="#FFC107" strokeWidth="1.5" />
          </svg>
        </div>

        {/* Motion texture - pulse rings from center */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}>
          {Array.from({ length: 8 }, (_, i) => {
            const radius = (i * 100 + frame * 0.5) % 800;
            return (
              <circle
                key={i}
                cx="960"
                cy="540"
                r={radius}
                fill="none"
                stroke="#ff5252"
                strokeWidth="0.8"
                opacity={1 - radius / 800}
              />
            );
          })}
        </svg>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y + frame * p.speed) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#ff5252' : i % 3 === 1 ? '#FFC107' : '#F5F5F5',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Top section - Asset allocation power */}
        <div
          style={{
            position: 'absolute',
            top: '8%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(sp(5), [0, 1], [30, 0])}px)`,
            opacity: sp(5),
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari'",
              fontSize: 40,
              color: '#9E9E9E',
            }}
          >
            Asset Allocation की ताकत
          </div>
        </div>

        {/* Two columns - what it does / doesn't do */}
        <div
          style={{
            position: 'absolute',
            top: '20%',
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            gap: 60,
          }}
        >
          {/* Doesn't give */}
          <div
            style={{
              width: 380,
              opacity: sp(20),
              transform: `translateX(${interpolate(sp(20), [0, 1], [-40, 0])}px)`,
            }}
          >
            <div
              style={{
                background: 'rgba(158,158,158,0.08)',
                border: '1.5px solid rgba(158,158,158,0.2)',
                borderRadius: 18,
                padding: '25px 30px',
                textAlign: 'center',
              }}
            >
              <svg width="50" height="50" viewBox="0 0 50 50" style={{ marginBottom: 10 }}>
                <circle cx="25" cy="25" r="20" fill="none" stroke="#9E9E9E" strokeWidth="2" />
                <line x1="15" y1="15" x2="35" y2="35" stroke="#9E9E9E" strokeWidth="2.5" />
              </svg>
              <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 28, color: '#9E9E9E' }}>
                सबसे ज्यादा return
              </div>
              <div style={{ fontFamily: "'Inter'", fontSize: 20, color: '#666', marginTop: 5 }}>
                NOT THE GOAL
              </div>
            </div>
          </div>

          {/* Does give */}
          <div
            style={{
              width: 380,
              opacity: sp(40),
              transform: `translateX(${interpolate(sp(40), [0, 1], [40, 0])}px)`,
            }}
          >
            <div
              style={{
                background: 'rgba(102,187,106,0.1)',
                border: '2px solid rgba(102,187,106,0.4)',
                borderRadius: 18,
                padding: '25px 30px',
                textAlign: 'center',
                boxShadow: `0 0 ${30 + glowPulse * 20}px rgba(102,187,106,${glowPulse * 0.2})`,
              }}
            >
              <svg width="50" height="55" viewBox="0 0 50 55" style={{ marginBottom: 10 }}>
                <path d="M25 5 L45 15 L43 35 C41 45 25 52 25 52 C25 52 9 45 7 35 L5 15 Z" fill="none" stroke="#66BB6A" strokeWidth="2.5" />
                <path d="M17 28 L22 33 L33 22" stroke="#66BB6A" strokeWidth="2.5" fill="none" strokeLinecap="round" />
              </svg>
              <div style={{ fontFamily: "'Noto Sans Devanagari'", fontSize: 28, color: '#66BB6A', fontWeight: 600 }}>
                बुरे वक्त में ज़िंदा रखती है
              </div>
              <div style={{ fontFamily: "'Inter'", fontSize: 20, color: '#66BB6A', marginTop: 5, opacity: 0.7 }}>
                SURVIVAL = EVERYTHING
              </div>
            </div>
          </div>
        </div>

        {/* BIG SURVIVE text */}
        <div
          style={{
            position: 'absolute',
            top: '52%',
            left: '50%',
            transform: `translateX(-50%) scale(${surviveIn * heartBeat})`,
            opacity: surviveIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 180,
              color: '#F5F5F5',
              letterSpacing: 15,
              textShadow: '0 0 80px rgba(245,245,245,0.2), 0 0 150px rgba(102,187,106,0.15)',
              lineHeight: 1,
            }}
          >
            SURVIVE
          </div>
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 50,
              color: '#FFC107',
              marginTop: 10,
              opacity: sp(110),
            }}
          >
            करो
          </div>
        </div>

        {/* Rule text bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: '8%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(ruleIn, [0, 1], [30, 0])}px)`,
            opacity: ruleIn,
          }}
        >
          <div
            style={{
              background: 'rgba(255,193,7,0.08)',
              border: '1.5px solid rgba(255,193,7,0.25)',
              borderRadius: 16,
              padding: '18px 45px',
              fontFamily: "'Noto Sans Devanagari'",
              fontSize: 34,
              color: '#FFC107',
              textAlign: 'center',
            }}
          >
            बाक़ी सब <span style={{ color: '#F5F5F5' }}>बाद में</span> होगा
          </div>
        </div>

        {/* Stick figure - warrior stance */}
        <div
          style={{
            position: 'absolute',
            bottom: '22%',
            right: '8%',
            opacity: sp(60),
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: -20,
              left: -20,
              width: 130,
              height: 180,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(255,193,7,0.12) 0%, transparent 70%)',
              filter: 'blur(12px)',
            }}
          />
          <svg width="90" height="140" viewBox="0 0 90 140">
            <circle cx="45" cy="20" r="14" fill="none" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 20)`} />
            <circle cx="38" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="52" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="44" y1="18" x2="46" y2="18" stroke="#FFC107" strokeWidth="1" />
            {/* Determined face */}
            <line x1="38" y1="25" x2="52" y2="25" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="45" y1="34" x2="45" y2="80" stroke="#F5F5F5" strokeWidth="3" />
            {/* Wide stance */}
            <line x1="45" y1="52" x2="15" y2="62" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="52" x2="75" y2="62" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="80" x2="22" y2="120" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="80" x2="68" y2="120" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter'",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene9_SurviveRule;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Chapter7Scene10_PracticalSteps',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter7Scene10_PracticalSteps: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera pan up slowly
  const camY = interpolate(frame, [0, durationInFrames], [20, -25]);
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  const gradAngle = interpolate(frame, [0, durationInFrames], [130, 180]);

  const particles = Array.from({ length: 26 }, (_, i) => ({
    x: seededRandom(i * 41 + 1) * 1920,
    y: seededRandom(i * 41 + 2) * 1080,
    size: seededRandom(i * 41 + 3) * 3.5 + 1.5,
    speed: seededRandom(i * 41 + 4) * 0.35 + 0.15,
    opacity: seededRandom(i * 41 + 5) * 0.2 + 0.08,
  }));

  // Steps data
  const steps = [
    {
      num: '01',
      title: 'SIP आज ही शुरू करो',
      desc: '₹500 या ₹1000 — बस शुरू करो',
      color: '#4FC3F7',
      icon: 'play',
    },
    {
      num: '02',
      title: 'Step-Up SIP Activate',
      desc: 'हर साल 10% बढ़ाओ — Automatic',
      color: '#66BB6A',
      icon: 'up',
    },
    {
      num: '03',
      title: 'Emergency Fund बनाओ',
      desc: '6 महीने का खर्चा — Liquid Fund में',
      color: '#FFC107',
      icon: 'shield',
    },
    {
      num: '04',
      title: 'Portfolio Review हर 6 महीने',
      desc: 'Panic नहीं — Asset Allocation चेक करो',
      color: '#E040FB',
      icon: 'search',
    },
    {
      num: '05',
      title: 'Expenses Track करो',
      desc: 'जब तक पता नहीं कहाँ जा रहा — बचा नहीं सकते',
      color: '#ff5252',
      icon: 'list',
    },
  ];

  // Title entrance
  const titleIn = sp(5);

  return (
    <AbsoluteFill
      style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}
    >
      <div
        style={{
          transform: `scale(${camZoom}) translateY(${camY}px)`,
          width: '100%',
          height: '100%',
          position: 'absolute',
        }}
      >
        {/* Gradient - dark teal/emerald */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0A1A15 0%, #0D2218 30%, #0A1D14 60%, #050F0B 100%)`,
          }}
        />

        {/* Shape layers */}
        {/* Checklist shape */}
        <div
          style={{
            position: 'absolute',
            top: '8%',
            right: '3%',
            opacity: 0.04,
            transform: `rotate(${5 + frame * 0.05}deg) translateY(${Math.sin(frame * 0.02) * 10}px)`,
          }}
        >
          <svg width="200" height="280" viewBox="0 0 200 280">
            <rect x="10" y="10" width="180" height="260" rx="15" fill="none" stroke="#66BB6A" strokeWidth="2" />
            {[0, 1, 2, 3, 4].map((i) => (
              <React.Fragment key={i}>
                <rect x="30" y={40 + i * 48} width="20" height="20" rx="4" fill="none" stroke="#66BB6A" strokeWidth="1.5" />
                <line x1="65" y1={50 + i * 48} x2="170" y2={50 + i * 48} stroke="#66BB6A" strokeWidth="1.5" opacity="0.5" />
              </React.Fragment>
            ))}
          </svg>
        </div>

        {/* Circle */}
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            left: '5%',
            width: 300,
            height: 300,
            border: '1px solid #4FC3F7',
            borderRadius: '50%',
            opacity: 0.03,
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        />

        {/* Motion texture - check marks floating */}
        <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.03 }}>
          {Array.from({ length: 8 }, (_, i) => (
            <path
              key={i}
              d={`M${seededRandom(i * 50) * 1920} ${(seededRandom(i * 50 + 1) * 1080 + frame * 0.5) % 1080} l5 5 l10 -10`}
              fill="none"
              stroke="#66BB6A"
              strokeWidth="2"
              strokeLinecap="round"
            />
          ))}
        </svg>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: (p.y - frame * p.speed * 1.5 + 1080) % 1080,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor:
                i % 5 === 0
                  ? '#4FC3F7'
                  : i % 5 === 1
                    ? '#66BB6A'
                    : i % 5 === 2
                      ? '#FFC107'
                      : i % 5 === 3
                        ? '#E040FB'
                        : '#ff5252',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: '3%',
            left: '50%',
            transform: `translateX(-50%) translateY(${interpolate(titleIn, [0, 1], [30, 0])}px)`,
            opacity: titleIn,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One'",
              fontSize: 64,
              color: '#F5F5F5',
              letterSpacing: 3,
            }}
          >
            <span style={{ color: '#66BB6A' }}>5</span> PRACTICAL STEPS
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari'",
              fontSize: 28,
              color: '#9E9E9E',
              marginTop: 5,
            }}
          >
            आज से शुरू करो
          </div>
        </div>

        {/* Steps List */}
        <div
          style={{
            position: 'absolute',
            top: '17%',
            left: '50%',
            transform: 'translateX(-50%)',
            width: 1100,
          }}
        >
          {steps.map((step, idx) => {
            const stepDelay = 15 + idx * 22;
            const stepIn = sp(stepDelay);
            // Checkmark appears after step
            const checkIn = interpolate(
              frame,
              [stepDelay + 35, stepDelay + 50],
              [0, 1],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            );

            return (
              <div
                key={idx}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  gap: 22,
                  marginBottom: 14,
                  opacity: stepIn,
                  transform: `translateX(${interpolate(stepIn, [0, 1], [80, 0])}px)`,
                }}
              >
                {/* Number circle */}
                <div
                  style={{
                    width: 60,
                    height: 60,
                    borderRadius: '50%',
                    border: `2.5px solid ${step.color}`,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    fontFamily: "'JetBrains Mono'",
                    fontSize: 24,
                    fontWeight: 800,
                    color: step.color,
                    flexShrink: 0,
                    background: `${step.color}10`,
                  }}
                >
                  {step.num}
                </div>

                {/* Content */}
                <div
                  style={{
                    flex: 1,
                    background: `linear-gradient(135deg, ${step.color}08, ${step.color}03)`,
                    border: `1px solid ${step.color}30`,
                    borderRadius: 14,
                    padding: '14px 25px',
                  }}
                >
                  <div
                    style={{
                      fontFamily: "'Noto Sans Devanagari'",
                      fontSize: 28,
                      fontWeight: 700,
                      color: '#F5F5F5',
                    }}
                  >
                    {step.title}
                  </div>
                  <div
                    style={{
                      fontFamily: "'Noto Sans Devanagari'",
                      fontSize: 20,
                      color: '#9E9E9E',
                      marginTop: 3,
                    }}
                  >
                    {step.desc}
                  </div>
                </div>

                {/* Checkmark */}
                <svg
                  width="36"
                  height="36"
                  viewBox="0 0 36 36"
                  style={{
                    opacity: checkIn,
                    transform: `scale(${interpolate(checkIn, [0, 1], [0.5, 1])})`,
                    flexShrink: 0,
                  }}
                >
                  <circle cx="18" cy="18" r="16" fill={`${step.color}20`} stroke={step.color} strokeWidth="2" />
                  <path
                    d="M11 18 L16 23 L25 14"
                    stroke={step.color}
                    strokeWidth="2.5"
                    fill="none"
                    strokeLinecap="round"
                    strokeDasharray={30}
                    strokeDashoffset={30 - checkIn * 30}
                  />
                </svg>
              </div>
            );
          })}
        </div>

        {/* Bottom CTA */}
        <div
          style={{
            position: 'absolute',
            bottom: '4%',
            left: '50%',
            transform: `translateX(-50%) scale(${sp(140)})`,
            opacity: sp(140),
          }}
        >
          <div
            style={{
              background: 'linear-gradient(135deg, #66BB6A, #388E3C)',
              borderRadius: 16,
              padding: '16px 50px',
              fontFamily: "'Yantra One'",
              fontSize: 36,
              color: '#F5F5F5',
              textAlign: 'center',
              boxShadow: '0 10px 40px rgba(102,187,106,0.3)',
            }}
          >
            शुरू करो — <span style={{ color: '#FFC107' }}>आज!</span>
          </div>
        </div>

        {/* Stick figure - running/action pose */}
        <div
          style={{
            position: 'absolute',
            bottom: '14%',
            right: '5%',
            opacity: sp(30),
            transform: `translateX(${interpolate(sp(30), [0, 1], [30, 0])}px) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: -15,
              left: -15,
              width: 120,
              height: 170,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(255,193,7,0.1) 0%, transparent 70%)',
              filter: 'blur(10px)',
            }}
          />
          <svg width="100" height="150" viewBox="0 0 100 150">
            <circle cx="50" cy="22" r="15" fill="none" stroke="#F5F5F5" strokeWidth="2.5" transform={`rotate(${Math.sin(frame * 0.04) * 3}, 50, 22)`} />
            <circle cx="43" cy="20" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <circle cx="57" cy="20" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            <line x1="49" y1="20" x2="51" y2="20" stroke="#FFC107" strokeWidth="1" />
            {/* Smile */}
            <path d="M43 26 Q50 31 57 26" fill="none" stroke="#F5F5F5" strokeWidth="1.2" />
            <line x1="50" y1="37" x2="50" y2="82" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Running arms */}
            <line x1="50" y1="52" x2="30" y2="42" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="50" y1="52" x2="72" y2="65" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Running legs */}
            <line x1="50" y1="82" x2="30" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="30" y1="110" x2="22" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="50" y1="82" x2="70" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="70" y1="115" x2="78" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Speed lines */}
            {[0, 1, 2].map((i) => (
              <line
                key={i}
                x1={85 + i * 5}
                y1={50 + i * 15}
                x2={95 + i * 5}
                y2={50 + i * 15}
                stroke="#FFC107"
                strokeWidth="1.5"
                opacity={0.4 + i * 0.15}
              />
            ))}
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter'",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Chapter7Scene10_PracticalSteps;
```

